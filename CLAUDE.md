# Stock Autopilot — agent context & REMOTE TRADE-APPROVAL playbook

This file is auto-loaded by any Claude Code session that opens this repo. It exists so a fresh remote session (e.g. Ryan on his phone) has full context and the safety rules, even with no prior memory.

> **Resuming an in-progress session?** Read **`HANDOFF.md`** first (if present) — it carries the current working state and any corrections to the notes below (scheduling, FMP access, in-flight stop tasks).

## Standing principle: fix issues at the root — don't just flag them
When you hit a recurring problem, a stale artifact, a misleading signal, or anything that makes the system worse than it should be, **fix the root cause and improve the process**, don't just report it run after run. Surfacing an issue once is fine; surfacing the *same* issue every session means the fix was never made. Concretely: when you find the cause, (1) correct the immediate symptom, (2) make the durable fix (update the data, the ledger, this playbook, or the code), and (3) write the lesson back into CLAUDE.md so the next session inherits it. Trading actions still need Ryan's approval, and you still won't push to `master` or take irreversible outward actions without his OK — but proactively *propose and prepare* the fix (e.g. open the PR) rather than leaving it as a standing annoyance. Leave the system better than you found it.

## What this project is
Read-only daily stock analysis. `report.py` (run by scheduled routines) produces reports through the trading day (a morning full scan then hourly intraday refreshes, ~09:00–14:00 CT, Mon–Fri — see `docs/cron-job-setup.md`) flagging Connors RSI(2) swing setups + a 12-1 momentum ranking (entries), **SELL/EXIT signals on positions in the `holdings.json` ledger**, and auto concentration/sizing analysis. The scripts NEVER trade.

**Portfolio engine:** the report reads `holdings.json` (the positions ledger) and judges **every** position on each run with a per-name action — **take-profit / trail / hold / thesis-check** (no position is parked; there is no `legacy` bucket). Take-profit fires on a target hit or an RSI2≥70 swing bounce; winners that have run far enough get a **trailing-stop ratchet** (~15% below the high, up only, floored at breakeven); names below the 200-day MA or out of the momentum top decile are flagged **REVIEW / THESIS-CHECK** (sell only if the thesis is dead); underwater names with intact theses **HOLD** with no price stop and are culled at the monthly rebalance. Plus a "better-play" rotation list. These are SIGNALS — confirm with a live quote and approve each action per the HARD RULES (esp. the trailing-stop / thesis policy in RULE 5).

## Your job in a remote session: help Ryan APPROVE and PLACE trades

When Ryan says something like "run today's report and let's trade" / "what are today's trades" / "accept the trades":

1. **Get the findings.** Read the latest committed report at the repo root: **`latest_morning.md`** or **`latest_intraday.md`** (whichever is newer / matches the time of day). These are produced by the **GitHub Actions** scheduled job (which has internet + the FMP key) and committed here. **Do NOT run `report.py` in this environment** — it can't reach financialmodelingprep.com (network allowlist) and would produce an empty/invalid report. If the `latest_*.md` is missing, stale, or its header says **`>>> DATA ERROR <<<`**, STOP and tell Ryan — do not trade off a stale or invalid report. (Newest data is also on his ntfy alert; he can paste it.)
2. **Present the proposed orders.** From the report's swing setups, propose concrete orders sized per the rules below (dollar amounts, with the stop level). Show Ryan a short list: ticker, $ size, % of account, stop, one-line reason. Flag the concentration/sizing warnings the report surfaced.
3. **Approve and place, one at a time** — see HARD RULES. Review each, get his explicit "yes", place it, confirm the fill, move to the next.

This makes one phone session self-contained: findings -> approval -> execution.

### Ryan's usual entry point: he pastes the ntfy alert
The scheduled job's ACTION notification is paste-able and is often the whole prompt
you get. Format (pipe-separated names, max 10 per line):

```
TAKE PROFIT / SELL: <sym> <price> (<reason>) | ...
SET TRAILING STOP: <sym> <price> (<pnl>) -> 15% native, floor ~<level> | ...
THESIS CHECK: <sym> <price> (<reason: below 200MA / out of decile>) | ...
BUY: <sym> <price> stop <stop> tgt <target> [HELD/SPEC] | ...
```
(The report judges the WHOLE book each run, so these lines come straight from the
per-position actions — `TAKE PROFIT / SELL` = bank the gain, `SET TRAILING STOP` = a name
just went green enough (≥~+17.6%) so **Ryan sets a 15% native trailing stop in-app** (the
agent never places stops), `THESIS CHECK` = research the name and sell only if the thesis is dead.)

When a message looks like that, don't ask what he wants — prepare the whole batch,
then take ONE approval (see "One-tap batch approval" below):
1. Cross-check against the committed `latest_*.md` (freshness, `DATA ERROR`,
   anything the alert truncated) and reconcile `holdings.json` vs `get_equity_positions`.
2. **SELL lines first** — reconcile each against live positions; cancel any resting
   stop that blocks the sell.
3. **TRAIL STOP lines** — prepare the stop replacement (cancel old GTC stop, place new).
4. **BUY lines** — check real buying power via `get_portfolio` (pending deposits are
   usually NOT spendable), size each per HARD RULE 4. Skip `[HELD]` names unless Ryan
   explicitly wants to add; respect the SPEC-sleeve cap for `[SPEC]` names. Prefer
   whole-share quantities so the position can carry a resting GTC stop (Ryan's stated
   preference).
5. **ALWAYS run the live market-wide dip scan (standing rule, set 2026-06-30).** EVERY time
   Ryan pastes the ntfy alert, also call `run_scan` on the saved Robinhood scanner
   **`14a2ec7a-cf7a-4b99-b0a2-5b62f8c5b05f`** ("Connors RSI(2) oversold dip — market-wide":
   `RSI(2) < 10` + `last > $5` + `market cap > $2B`). It surfaces oversold setups OUTSIDE the
   report's ~220-name universe. The scan is the oversold **TRIGGER only** — gate each hit like a
   BUY line: confirm price > a **RISING 200-day MA** (a dip in an uptrend, NOT a falling knife)
   **+** the HARD RULE 7 news/thesis check, then fold the survivors into the proposed batch
   tagged "(live dip scan — outside report universe)". Respect the sector steer (de-emphasized
   oil) and the SPEC cap. If the saved scan 404s on a fresh account, recreate it with
   `create_scan` using those exact filters (no "price > MA200" filter exists in the scanner, so
   the uptrend confirmation is always the in-session step).
For every SELL and BUY line, run the news/thesis check (HARD RULE 7) before listing it.
All HARD RULES below still apply — the alert is the findings, never the approval.

**`[ERN <date>]` on a BUY line = earnings inside the hold window → treat as NO-ENTRY** (added
2026-08-13). The RSI(2) screen is purely technical and knew nothing about earnings dates, so it
proposed TJX on 2026-08-11 with the print 8 days out and ROST on 2026-08-13 with it 7 days out —
two manual vetoes on consecutive alerts, which is the "same issue every session" trigger. `report.py`
now annotates each setup from the FMP earnings calendar and tags the alert. A 1-3 week swing
straddles the print and the suggested stop cannot protect an overnight gap: **a name can beat and
still gap down** (TPR beat EPS on 2026-08-13 and fell ~16% the same session). Skip these unless the
earnings move IS the thesis. The flag is advisory, not a filter — the name still appears, so say
plainly why it's excluded. **Absence of the flag is not proof of no earnings**: the calendar degrades
to `{}` on any API failure, and an unflagged name is "unknown", so still sanity-check the date on any
name you're about to actually buy.

### One-tap batch approval (Ryan's chosen semi-auto mode)
Robinhood has no trading API and the account can't expose a TOTP seed, so execution
must happen in a session — but Ryan wants ONE approval, not a per-order Q&A. So:
- Do ALL the prep above silently, then present a SINGLE consolidated list of every
  proposed order: ticker, side, $ size / shares, fresh live quote (from
  `review_equity_order`), stop, and a one-line thesis-gate verdict (intact/weakened/
  broken). Show vetoed and over-cap names separately as "excluded, reply to override".
- Then wait for ONE explicit **"approve"**. On that single word, place the whole batch
  in order (sells → trails → approved buys), auto-placing every stop (HARD RULE 5),
  with NO further per-order prompts. Report all fills at the end and sync `holdings.json`.
- Never place a name you didn't list; never silently include a vetoed/over-cap name.
  Fall back to per-order "yes" whenever Ryan asks or for a one-off ad-hoc order.

## DEFENSIVE POSTURE — Aug-Oct 2026 drawdown watch (Ryan adopted 2026-08-05)
Ryan agrees with the Street's Aug-Oct drawdown warnings (Lee: possible 10-20% air pocket before year-end strength) and adopted a four-layer defensive posture. This stands until a >12% SPY drawdown plays out and re-entry completes, or Ryan lifts it (~Nov 1 review otherwise):
- **Agentic cash floor ≥45%** through October. Swing-exit proceeds ACCUMULATE, do not auto-recycle. New swing entries: A-grade setups only, sized $400-500 by default. Mechanical exits unchanged.
  - **🚫 WEEKLY SWING QUOTA REMOVED (Ryan, live turn 2026-08-19: "Remove swing quota. We don't need that limiting our options.").** The prior "Max ONE new swing/week" cap is VOID — do not gate a BUY on how many swing entries already happened this calendar week, and do not re-derive a count-based limiter from this file's history. The cash floor below and the per-name/A-grade discipline are the controls that remain; the count cap is gone. (Void text, kept for institutional memory only: "Max ONE new swing/week... check the most recent swing entry_date against the current week before pricing anything" — this reasoning blocked entirely valid setups on 2026-08-18/19 purely on a same-week count, which is exactly the over-restriction Ryan cut.)
  - **Denominator (pinned 2026-08-11 — two sessions have now had to re-derive this, so it is settled here):** the floor is measured on the **EQUITY BOOK ONLY** — `cash ÷ (cash + equity_value)` from `get_portfolio`, **excluding** `options_value`. The options sleeve is a separate book with its own $1,500 budget (HARD RULE 8) and must not be double-counted against the equity cash floor. This is the reading the 2026-08-10 ACGL entry used ("equity-book cash ~46% >= 45% floor"); measuring against `total_value` instead makes the account look permanently sub-floor and would wrongly freeze all entries.
  - **Check it BEFORE proposing a size, not after.** Max compliant buy = `cash − 0.45 × (cash + equity_value)`. If that number is below the ~$400 practical entry minimum, there is **no compliant entry today** — say so plainly and list the setups as excluded rather than proposing a size that breaches the floor. (2026-08-11: cash $1,447.47 / equity $1,371.64 → 51.3% cash, max compliant buy $178.87 — below the $400 band, so every BUY line was excluded on the floor alone.)
- **Calibration (Ryan, live turn 2026-08-06: "Not all our plays need to be defensive... we have to watch the market and let the market tell us what is happening."):** the posture is a VOLATILITY EXPECTATION plus a levels-watching discipline, NOT a lockdown. Strong A-grade setups — including growth names — are still taken; do not reflexively skip good signals for being high-beta. The support levels ARE the tell: above SPY 746-748 = business as usual under the caps; a closing break below = shift firmly defensive and lean on the tranche plan.
- **Winners defended by native trails, not sales:** NVDA trail fires at $230.40 green-enough trigger (Ryan sets 15% native in-app); IREN trigger ~$50.60. No pre-emptive selling of conviction holds.
- **Agentic put hedge authorized by Ryan (2026-08-05):** single-leg long SPY/QQQ put, Oct-Dec expiry, sized per the approved batch within the $1,500 options budget (2026-08-05 sizing amendment, HARD RULE 8; joint account gets NO puts per Ryan). Managed under HARD RULE 8 exits except: a hedge put is NOT subject to the mechanical premium-stop cuts (it is insurance, expected to decay in a melt-up; hold to ~21 DTE then roll/close decision with Ryan).
- **Joint de-risk list (Ryan-approved 2026-08-05, he executes in-app, into strength):** SELL ALL TQQQ + PATH; TRIM HALF MU, HOOD, FIG; TRIM ⅓ WULF after its earnings settle. Target ~$10-12k (~10%) joint cash. CORE UNTOUCHED: MSFT/GOOGL/AMZN/META/NOW/ADBE/ZTS/ISRG/RBRK/V/JPM/QQQI (QQQI stays 100% — covered-call income is the defensive engine).
- **CRITICAL CONTEXT (2026-08-05, post-execution):** Ryan's joint sells (~$50k+) were primarily DELEVERAGING — paying off the margin balance — not building a spendable cash pile. The joint account is now unlevered; re-entry tranches are therefore partially RE-MARGIN decisions and should be sized conservatively (margin only at tranche-2/-3 levels, if at all). Ryan explicitly rejected re-margining to buy QQQI pre-drawdown (income doesn't cover drawdown participation + interest + forced-sale risk). **QQQI is the designated FIRST BUY of tranche 1** (cash, post-drop: lower basis + fatter high-IV distributions).
- **Key support levels (computed 2026-08-05; recompute weekly, report every alert cycle):** SPY last 772: 20d 748.5 / 50d 746.4 / July-low shelf 716.6 / rising 200d 702.0. QQQ last 723: 20d 701.2 / 50d 715.1 / July low 661.1 / rising 200d 646.8. Tranche triggers SPY 718 / 680 / 633. NOTE the confluence: tranche 1 (718) ≈ July low (716.6); Ryan's "aggressive profit-taking to the 200 SMA" scenario lands between tranches 1-2. A CLOSING loss of SPY 746-748 is the early-warning signal.
- **Pre-committed re-entry tranches (execute mechanically, not emotionally):** SPY −7% from highs → deploy ⅓ of raised cash (quality screen at limits: MSFT <$400, NOW <$100, ADBE <$230); SPY −12% → next ⅓ (add NVDA <$185, META <$500, ISRG <$340); SPY −18% → final ⅓ into whatever gates A-grade. Joint tranches are recommendations; Agentic tranches use the normal batch-approval flow.

## Sector focus — Ryan's standing steer (set 2026-06-17)
**De-emphasize oil-related energy for NEW entries.** Ryan's view: the oil complex's
upside is capped by political/market forces and it is not a trending, growing sector.
So for future alerts:
- **Do NOT propose new oil-energy buys by default.** This covers E&P/upstream, oilfield
  services, refiners, and integrated majors (e.g. HAL, SLB, EOG, XOM, CVX, COP, OXY,
  PSX, VLO, MPC). When such names show up as RSI2/momentum signals, list them as
  **excluded ("de-emphasized sector, reply to override")** rather than as proposed orders.
- **Prefer non-energy and trending/growth-sector names** when choosing which signals to
  surface as actual entries.
- **Not a hard ban:** Ryan can override per alert ("add CVX", etc.). Honor it for that order only.
- **Existing energy positions:** as of 2026-06-17 Ryan chose to KEEP what's held (PSX, XOM)
  and manage them on their stops — the steer is about new focus, not forced liquidation.
  Don't dump existing energy without an explicit instruction or a fired exit signal.

## HARD RULES (do not break, even if asked to "just do it")
1. **Account:** trade ONLY the Robinhood account with `agentic_allowed=true` (nickname "Agentic", account 718757339 — **`limited_margin` and `option_level_3` since 2026-08-21; it was a `cash` / `option_level_2` account before that, and any note in this file assuming a cash account predates the upgrade**). Confirm it via `get_accounts` every session. NEVER place orders on any other account — the others reject agentic orders anyway.
2. **Approval before placing — one-tap batch is Ryan's chosen default (SCOPE NARROWED 2026-08-26: this rule governs LIVE-SESSION batches and anything outside the HARD RULE 6 autonomous envelopes — signal-sourced equity trades and the options sleeve now run autonomously per HARD RULE 6/8, and those need no per-trade approval):** show Ryan a fresh live quote (`review_equity_order`), estimated shares, and total cost for EVERY order before any `place_equity_order`. Ryan has opted into ONE-TAP approval: present the full priced batch (see "One-tap batch approval" above) and WAIT for a single explicit **"approve"**; that one word authorizes the whole listed batch and you place it without pausing per order. NEVER place before showing the priced batch, never place a name not on the list, never include a vetoed/over-cap name unless Ryan adds it. (Per-order "yes" remains available on request and for one-off ad-hoc orders. A bare "place it" is still never a bypass of the review/listing.)
3. **Order type:** dollar-based **market** orders, `market_hours=regular_hours` (fractional only works this way). If the market is closed, the order queues for the next open — tell Ryan that.
4. **Sizing / risk:** per-name ≤ ~15-20% of account value; total SPECULATIVE-sleeve exposure ≤ ~25% (spec = quantum/nuclear/uranium/space/eVTOL/drones/photonics/AI-infra small caps). Respect settled buying power (cash account: sale proceeds take ~1 day to settle). Keep a cash buffer.
5. **Stops & exits — NATIVE trailing stops set by Ryan; the agent NEVER places a stop (policy set 2026-06-17, reaffirmed 2026-06-18):**
   - **No fixed loss-stops, ever. The agentic/automated side places NO stop orders of any kind.** We do NOT rest a stop under cost basis (those turn normal pullbacks into realized losses — that's how HAL got chopped and how the JNJ fixed stop slipped in). On a BUY fill, do **not** auto-place any `stop_market`.
   - **Winners get a ~15% NATIVE trailing stop, set by RYAN in the app.** A name is **"green enough"** when price ≥ entry ÷ 0.85 (≈ **+17.6%**), so a 15%-below-high stop clears breakeven. `report.py` watches this every run and fires a **`SET TRAILING STOP`** alert when a name crosses it — that alert is Ryan's cue to set a **native 15% trailing stop in the Robinhood app** (it auto-follows the price up, protecting between sessions). Record it in the ledger as `"native_trail_pct": 15` (and `stop: null`); the report then marks the name protected and stops re-alerting it.
   - **Ryan sets native trailing stops on every green name in the book and every future buy once it's green enough.** The agent's only job here is to surface the green-trigger alert and keep the ledger in sync — it does not place, raise, or rest stops itself (the trade API can't do native trailing types anyway). Fractional / sub-1-share positions can't carry a broker stop → **monitored, not automatic**.
   - **A winner can then only ever be sold for a locked-in gain.**
   - **If a trailing stop fires but momentum/thesis is still alive, plan a BUYBACK lower** (below the exit price) rather than abandoning the name.
   - **Underwater / not-yet-green names carry NO price stop.** Manage them by **thesis** (HARD RULE 7): research the news for a thesis break and **sell only if the thesis is dead**; otherwise hold and cull at the monthly rebalance.
   - **Take profits when it makes sense** — this is an income / grow-the-balance account, not buy-and-forget. Bank gains on target hits / RSI2-overbought swing bounces, and let the native trailing stop harvest extended runners.
6. **Autonomy — bounded, BOTH BOOKS (equities added 2026-08-26; options since 2026-07-01; replaces the prior blanket no-autonomy rule):**
   - **✅ EQUITIES: BOUNDED AUTONOMY PERMITTED (Ryan, REAL live turns 2026-08-26 — genuinely interactive session).** He asked "Can we have the agent also trade equities autonomously?", was presented the design with three parameter choices and explicit recommendations, and chose the WIDE setting on all three: **(a) scope = FULL — autonomous entries, mechanical exits (target hits / RSI2≥70 banking), AND thesis-break sells; (b) sizing = up to the per-name cap (~15–20% of account value) per entry; (c) throttle = max 3 autonomous equity entries per trading day** (1 per run; exits never throttled). Recorded honestly: (a) and (b) exceed the desk's stated recommendation (entries + mechanical exits only, $400–500 during the posture); Ryan chose with the recommendation in front of him, which is his call to make. The envelope:
     - **Signal-sourced only:** autonomous equity trades come from the committed report's signals on `master` (RSI2 swing setups, momentum entries, TAKE-PROFIT/SELL fires, thesis-check flags researched to a broken verdict) — never from an unattended run's own free-form idea. Anything outside the report's signals still goes through the live-session one-tap batch (HARD RULE 2).
     - **Every OTHER gate stands, none were loosened:** the HARD RULE 7 news/thesis check with evidence logged BEFORE any autonomous buy or sell; the Aug–Oct posture's **≥45% equity-book cash floor computed BEFORE sizing** (max compliant buy = cash − 0.45×(cash+equity_value); below the ~$400 practical minimum = no entry today) and its **A-grade-only** bar; the sector steer (no new oil energy); **`[ERN <date>]` = NO-ENTRY**; per-name ≤15–20% and spec-sleeve ≤25% caps; HARD RULE 5 in full (the agent still NEVER places stops — winners get Ryan's native trails on the SET TRAILING STOP alert); HARD RULE 3 order mechanics; no margin (unleveraged_buying_power bounds BOTH books' combined deployment, ≥$250 reserve).
     - **Thesis-break sells are autonomous but evidence-gated and notified:** a sell requires the full HARD RULE 7 research written to the journal at decision time (verdict "broken" with sources) — a technical flag alone (below 200MA, out of decile) is never sufficient; that alone is still HOLD/REVIEW. Every autonomous sell notifies Ryan once, immediately after execution, with the evidence.
     - **⚖️ THE MECHANICAL EXIT IS A *PROFIT-BANKING* AUTHORITY — GATE IT ON `price > entry`. IT MAY NEVER REALIZE A LOSS ON A TECHNICAL BOUNCE (measured 2026-08-26T18:15Z, on the very first run that held the authority).** v8's equity clause reads *"a target hit or RSI2≥70 swing bounce on a ledger position = bank it autonomously"*, and read literally that fires on ANY position whose RSI2 crosses 70 — winners and losers alike. It fired immediately on **PNC at RSI2 78.5 while the position sat 0.34% UNDERWATER**, so the literal reading would have sold a loser into strength on its first day. That is not what the trigger is. HARD RULE 5 defines it as a **TAKE-PROFIT** ("Take-profit fires on a target hit or an RSI2≥70 swing bounce"), states that "a winner can then only ever be sold for a locked-in gain", and routes underwater names to **THESIS** management with no price stop at all. `report.py` already encodes the distinction in its own label — `EXIT-INTO-STRENGTH (underwater — optional)` with "policy default is hold-on-thesis" — so the ledger, the playbook and the report all agree, and only the prompt's compressed wording is ambiguous. **An RSI2≥70 print on an underwater position is an OPTIONAL exit-into-strength and needs the same discretionary judgment as any other loss-taking — under HARD RULE 5 that means a HARD RULE 7 thesis break, never a technical trigger.**
       - **The transferable shape, and it is this file's most-repeated one: a newly-granted authority arrives with its wording compressed for brevity, and the first run to hold it is the run most likely to read the compression literally.** When a new prompt's one-line summary of a rule is BROADER than the rule it summarizes, **the RULE governs** — the governance clause says so ("where either is stricter, the stricter rule applies"). Check the summarized rule before exercising a power for the first time.
     - **📅 RUN THE EARNINGS CALENDAR OVER THE *SECTOR*, NOT ONLY THE TICKER — A DOMINANT SAME-COMPLEX PRINT IS AN `[ERN]` FLAG (measured 2026-08-26T18:15Z).** The `[ERN]` rule is written around a name's OWN earnings date, and the report's Earnings column was correctly blank for all six RSI2 setups that day — `get_earnings_calendar` showed no own-print inside 21 days for AEHR, AMAT, INTC, KHC, LLY or PNC. Read narrowly, that cleared three semis for entry. It should not have: **NVDA reported that same evening** (verified, est $2.09), with MRVL 08-27, DELL 09-01 and AVGO 09-02 behind it, and AMAT/INTC/AEHR are direct NVDA-complex beta whose overnight gap risk from that print is indistinguishable from their own. **The `[ERN]` rule's actual mechanism is "a swing straddles a binary event the entry has no edge on, and policy forbids the stop that could protect the gap" — and that mechanism is agnostic about WHOSE print it is.**
       - **Corollary that keeps this from becoming a blanket veto: it is a DEFERRAL, never a permanent exclusion.** The event resolves and the names re-price — often into a better entry, though **a gap-up is not a gift to a mean-reversion setup and can just as easily consume the edge**. Re-grade from scratch on the post-event numbers; never carry the deferral forward as though it were a thesis judgment, and never re-use the pre-event grade.
     - **Ownership gate RETAINED (not on the 2026-08-26 ballot; Ryan's 2026-07-21 standing instruction):** equity positions with `placed_agent: "user"` — bought by Ryan himself in-app — are NEVER sold autonomously; detect, record, notify once, wait. Positions entered via approved batches or autonomous entries are the automation's to manage.
     - **Monthly rebalance stays a PROPOSAL to Ryan** (momentum re-rank, concentration trims) — except individual thesis-dead culls, which fall under autonomous thesis sells above.
     - ~~Effective for unattended runs only once Ryan pastes routine prompt **v8**~~ — **✅ CONDITION MET, OBSERVED 2026-08-26T18:15Z. v8 IS LIVE AND THE EQUITY BOOK IS AUTONOMOUS ON SCHEDULED RUNS. STOP REPORTING THE BAN.** The 18:15Z scheduled run was invoked with a stored prompt carrying the line-1 stamp *"prompt v8, pasted 2026-08-26"* and the full THE EQUITY BOOK section; every earlier run that day recorded v6. Recorded exactly as the v4 and v6 confirmations were: **a run may read the prompt it was invoked with; it may NOT claim, quote or paraphrase a Ryan turn.** No Ryan turn is asserted, no flag cleared, no approval implied — the paste happened outside any run. HARD RULE 9 intact. **The stamp on your own invocation outranks this file's description of which prompt is live** — do not re-derive the ban from the historical text above.
   - **Options sleeve: bounded autonomy PERMITTED.** The agent MAY enter and exit single-leg LONG options (HARD RULE 8) *without* per-order approval — including on scheduled runs — because options need faster reaction than a phone approval allows. This is STRICTLY limited to the defined-risk options sleeve (worst case = premium paid) under the caps + the **Options automation ruleset** in HARD RULE 8.
   - **The "best-interest-of-the-port" gate REPLACES Ryan's approval.** No human confirms each autonomous move, so the agent must **question every move before acting**: run the pre-trade checklist (HARD RULE 8) and execute ONLY if the trade clearly serves the mandate — **make as much as possible, minimize losses**. Any uncertainty → **don't trade** (a skipped trade is free; a bad autonomous options trade is not).
   - **Equities vs options loss policy (Ryan, 2026-07-01):** stock positions can sit at an unrealized loss **as long as the thesis is intact** — no hard price stop; cut only when the HARD RULE 7 thesis breaks (see RULE 5). **Options are the opposite: disciplined, rule-based loss-cutting** (theta doesn't forgive) — enforce the HARD RULE 8 exit engine mechanically (2026-08-05 exit-method amendment: setup-break primary, DTE-scaled premium backstops, 21-DTE review — smarter than a flat −50% print, but the backstops are still unconditional).
   - **Honest limit:** the agent is NOT a live tick-by-tick process — it runs only when invoked (a session or a scheduled cron). "Automation" = periodic scheduled checks, NOT continuous monitoring. Sizing + exits must assume gaps between runs — hence the **hard DTE floor of 7** even under the 2026-08-03 day-trading amendment (a 0–1 DTE contract can go to zero between 15-min checks; that stays banned no matter how active the sleeve gets).
7. **News / thesis check on EVERY alerted action:** before proposing any alerted SELL or BUY, evaluate it against **recent news, current analyst price targets/ratings, and whether the underlying thesis is still strong** — the report's signal is purely technical (RSI2 / momentum / MA), so confirm the fundamentals haven't broken it. Web-search the name (last few days/weeks), read what's actually driving the move, and weigh it against the macro/sector backdrop (e.g. a falling-oil tape undercuts an oversold E&P "bounce"). Present a one-line thesis verdict (intact / weakened / broken) with the key facts + sources alongside each proposed order, and recommend skipping signals whose fundamental thesis no longer holds. This gates the technical signal — a clean RSI2 print is not a buy if the news says otherwise.
8. **Options sleeve (LONG single legs + DEBIT SPREADS, defined risk) — separate book:** Trade options ONLY on the Agentic account, and ONLY while `get_accounts` shows it at `option_level_2`+ (if `option_level` is empty, do NOT attempt — tell Ryan to enable it at `applink.robinhood.com/upgrade_options?account_number=718757339`). **Since 2026-08-21 the account is `option_level_3`, so long calls/puts AND debit spreads are permitted — spreads legged one order at a time, long leg in first and short leg out first, never a short leg without its long. Naked/uncovered short options remain banned regardless of level.** Max loss is the premium paid (single leg) or the net debit (spread).

   **🚫 SPREADS ARE UNEXECUTABLE ON THIS ACCOUNT — THE PLATFORM REFUSES THEM, THE OPTIONS LEVEL IS NOT THE PROBLEM (measured 2026-08-24T16:03Z, by actually sending one). READ THIS BEFORE EVERY BLOCK BELOW, ALL OF WHICH ASSUME SPREADS WORK.** The 16:05Z run armed, triggered, priced, reviewed and SENT a MSFT 2026-09-18 490/510 call vertical. `place_option_order` returned **API error 400: "Multi-leg options orders aren't supported in Robinhood agentic accounts yet."** This is an **AGENTIC-API limitation, not an account limitation**: `get_accounts` confirms 718757339 at `limited_margin` / `option_level_3` in the same run. Level 3 is real; the agentic order endpoint will not accept a multi-leg order at any level.
   - **DO NOT ARM A SPREAD.** Every "prefer debit spreads" instruction below — and the v4 routine prompt's CORE-track *"PREFER A DEBIT SPREAD"* and its SURVEY ALL STRUCTURES duty — is **currently unexecutable**. Price a vertical only as a *comparison* when surveying structures; never as the intended vehicle.
   - **⚠️ `review_option_order` IS A FALSE GREEN AND THIS IS THE DANGEROUS PART.** It **accepts** the identical multi-leg payload and returns a complete, healthy-looking preview — both legs echoed, live quotes, greeks, an itemised fee block (`total_fee`, `is_gold`), a collateral block, and only a routine wide-spread alert. **Nothing in the review hints the order is unplaceable.** The review and place endpoints do not share this validation. **A clean multi-leg review is not evidence of executability; only a successful multi-leg FILL is.**
   - **THE LEGGING PROTOCOL DOES NOT RESCUE IT — it is the hazard, not the workaround** *(⚠️ the COLLATERAL half of this bullet is corrected 2026-08-26 two bullets below: with the long leg HELD, the short leg reviews at $0 collateral. The PRICE half below — leg two uncontrolled — stands in full).* Measured on the same trade: buying the 490C at the ask and selling the 510C at the bid nets a **$855 debit against the setup's $800 ceiling**, i.e. legging breaches the ceiling on entry, because **leg two's price is uncontrolled once leg one has filled.** And a rejected or missed second leg leaves the sleeve holding a **$1,425 ATM single leg at theta −$29.10/day — 5.6× the vertical's −$5.18** — which is precisely the *"unintended, full-premium single leg it never decided to own"* failure the historical level-2 block below already describes.
   - **✅ CORRECTED 2026-08-26T16:24Z — THE 08-24 LEGGING TEST WAS RUN WITHOUT HOLDING THE LONG LEG, AND ITS CONCLUSION WAS OVERBROAD. WITH THE LONG LEG HELD FIRST, THE SHORT LEG REVIEWS CLEAN AT $0 COLLATERAL. RYAN CAUGHT THIS (live turn 2026-08-26: "You should be able to buy a call then sell a put or call separately to create a spread"), and the free re-test proves him right.** Measured: with the SPY 2026-11-20 700P held (the hedge), `review_option_order` on a sell-to-open SPY 2026-11-20 650P ×1 (limit 3.10) returned **`order_checks: {}` — no alerts — and `collateral.cash.amount 0.0000, infinite: false`**. The margin engine PAIRED the short with the held long and margined the pair as a debit put spread; the long leg IS the collateral. The 08-24 test's $67,500 demand was real but tested a different thing: that afternoon the book held no QQQ long put (Ryan had closed the 680Ps at 14:32Z), so the reviewed 675P short was genuinely uncovered and cash-secured margining was the CORRECT broker response — not evidence about legging long-first. **The transferable lesson, this file's fifth measurement-bug instance and a new sub-class: a test must reproduce the protocol's PRECONDITIONS, or it tests something else.** The protocol was "long first, then short"; the test reviewed the short with no long held, and three documents then carried "legging is arithmetically impossible" for two days.
     - **✅ PROVEN BY FILLS 2026-08-26T17:24–17:26Z — RYAN-DIRECTED LIVE TEST, EVERY PATH MEASURED WITH REAL ORDERS (Ryan, live turns: "can we live test it then update the routine prompt and md file"; "approved. also We need to make sure we understand how best to unload the one leg spread as well... If it needs to be done one leg at a time or if it can be done in multiple different ways we need to determine this").** The desk legged a SPY 2026-09-18 740/735 put vertical END TO END on the agentic account: BUY 740P at the 3.22 ask (filled), short-leg review-gate returned `order_checks {}` / collateral $0 with the long held, SELL-TO-OPEN 735P at the 2.64 bid (FILLED, 31 seconds leg-to-leg) — **spread on at $58 net debit, margined as a vertical**. Exits, all three candidate paths: **(a) single-ticket multi-leg CLOSE → REJECTED, same 400 as entry** ("Multi-leg options orders aren't supported in Robinhood agentic accounts yet"), with `review_option_order` false-greening the identical closing payload minutes earlier — the multi-leg block above is re-confirmed in both halves, close orders included; **(b) wrong order (sell the long first, short still on) → BLOCKED by the margin engine at review** (`OPTION_NOT_ENOUGH_BP_FOR_COLLATERAL`, $3,500 collateral / $226.70 deposit demanded for the naked short that would remain — note this limited-margin naked-put figure differs from the 08-24 no-long-held $73.5k cash-secured demand; different margin paths, don't over-generalize either number); **(c) correct order — BUY-TO-CLOSE the short 735P first (filled 2.67, price-improved), then SELL-TO-CLOSE the long 740P (filled 3.22) → flat in 16 seconds at $55 net.** **The ONE executable unload path for a legged spread is legging out SHORT LEG FIRST; the ordering is enforced by the broker's collateral check, not just by doctrine.** The naked-short rule from 08-24 stands: a short sent first or left uncovered demands collateral or is banned.
     - **💰 THE EXECUTION PROTOCOL THE TWO TEST CYCLES TEACH — touch-priced marketable limits, never mid-resting, and the numbers say why:** cycle 1 legged with mid-priced RESTING limits and never completed the spread — the short chased a falling market through three limits while the filled long bled, costing **$13 in 7 minutes without ever holding the position** (~20% of the intended vertical's whole $65 max loss; the orphan's delta was ~$146/1% SPY). Cycle 2 crossed the touch on both legs seconds apart and ran the ENTIRE round trip — in at $58, out at $55 — for **$3 plus $0.32 fees**. On a penny-wide chain the half-spread paid by crossing (~$1–2/leg) is an order of magnitude cheaper than measured inter-leg drift. Binding protocol for any legged entry: pre-commit the max net debit off the TOUCH prices; leg one marketable at the touch; **re-review the short and require $0 collateral + empty order_checks before sending it**; leg two marketable at the touch within seconds; if the cap can't be met, **abort by selling the long back — never chase** (the two legs are ~95% correlated, so the credit shrinks for the same reason the long is losing; a chase cannot win); exits leg out short-first, both marketable. Same-type, same-expiry, long-strike-covers-short pairs only.
     - **What legging still costs even now (the 08-24 MSFT measurement stands):** leg two's price is uncontrolled once leg one has filled — crossing both spreads at market netted $855 against that setup's $800 ceiling. So any legged entry must (a) price BOTH legs and pre-commit a MAX NET DEBIT before sending leg one, with leg two worked as a limit at/near mid, and (b) only be attempted when the long leg STANDING ALONE is a position the desk is willing to hold for a session — because a short leg that doesn't fill at the limit leaves exactly that. And only SAME-TYPE pairs offset (put/put, call/call, same expiry, long strike covering short): a long call plus a short put is not a spread — the put margins alone as cash-secured.
     - **✅ POLICY GATE CLOSED 2026-08-26 — RYAN AUTHORIZED LEGGING, LIVE.** He directed the live test and the prompt/doc update in the same typed turns quoted above; the open question the 17:00Z run's flag posed is answered YES (that flag — raised correctly by an unattended run that could not see this session — is resolved per HARD RULE 9(a) in `holdings.json._resolved_flags`). **The legging protocol above is codified in `docs/routine-prompt-v7.md`; unattended runs are bound by v6's NEVER-LEG line until Ryan pastes v7, and authorized to leg CORE verticals under the full protocol from the moment he does.** The spec-verticals-for-Ryan flow remains available for anything the protocol doesn't cover (multi-leg tickets stay impossible; TACTICAL still never uses spreads).
   - ~~**🚫 LEGGING IS NOT MERELY RISKY — IT IS ARITHMETICALLY IMPOSSIBLE ON THIS ACCOUNT. LEG TWO ALWAYS FAILS ON COLLATERAL.**~~ **← SUPERSEDED IN PART by the 2026-08-26 correction above: true ONLY when the long leg is not already held. Historical record follows.** MEASURED 2026-08-24T16:35Z BY REVIEWING ONE (Ryan, REAL live turn 2026-08-24: "If you want to do a spread do the preview and know what you want then just do the two legs of the spread separately."). His instruction is sound in principle and is how a human legs a spread; it cannot work through this API, for a reason neither he nor the desk anticipated, and the desk tested it rather than assuming either way. `review_option_order` on a STANDALONE sell-to-open of the QQQ 2026-09-18 675P (the natural short leg against a 695P long) returns **`OPTION_NOT_ENOUGH_BP_FOR_COLLATERAL`, `depositAmount` $64,026.50, and a collateral block demanding $67,500 cash** — the full strike value (675 × 100), against an account holding $3,023.56.
     - **THE MECHANISM, and it is the whole lesson: a spread's risk offset exists ONLY INSIDE a multi-leg order.** Sent on its own, leg two is not "the short leg of a spread" — the broker has no way to know a long 695P is meant to cover it, so it is margined **standalone as a cash-secured put** and demands the entire strike in collateral. **Legging destroys the exact property that makes a vertical capital-efficient.** For a CALL spread it is worse still: a standalone short call is a NAKED call (`cash.infinite=true`), which this playbook bans outright at any level.
     - **THE SEQUENCE THIS PRODUCES, so no run ever starts it:** leg one fills (full premium at risk, correctly, since long-first is the safe ordering) → leg two is rejected for collateral → the sleeve is left holding **the orphaned full-premium single leg it never decided to own**, at 4–9× the vertical's theta. The bullet above rated that outcome a *risk*; this measurement upgrades it to a **certainty**. Long-first ordering prevents a naked short; it does NOT prevent the orphan.
     - **REVIEW IS TRUSTWORTHY HERE, UNLIKE THE MULTI-LEG CASE — know why, and do not over-generalise the false-green rule.** The multi-leg false green happens because *multi-leg support* is checked only at the place endpoint. This failure is a **collateral/buying-power** check, which review does perform and did surface. So: a clean review still proves nothing about multi-leg support, but a review that RAISES a collateral alert is real and must be honoured.
     - **WHAT TO DO INSTEAD — the spread economics are still available, just not through this API.** Do NOT leg it, and do NOT quietly substitute a single leg and call it the same trade. When a thesis genuinely wants a vertical: **fully spec it — both legs, net debit, max profit, breakevens, invalidation — and hand it to Ryan to place in the Robinhood app**, where multi-leg works normally. The desk does the analysis, he does the execution; that preserves the 4–9× carry advantage that made spreads the CORE-track preference. The permanent fix remains agentic multi-leg support.
     - **The transferable lesson, which is the same one this file learned three days ago and is now confirmed from the other direction:** the desk twice reasoned from a plausible model of the platform instead of testing it — first "level 3 means spreads work", now "legging routes around it". **One review call settled a question that three days of doctrine had assumed.** Test the cheap thing first; a capability is proven by an accepted order, and an *incapability* is often provable for free.
   - **CONSEQUENCE FOR THE CORE TRACK, and state it plainly rather than papering over it: CORE is single-leg-only, so CORE candidates must clear a HIGHER bar.** Every one now carries 4–9× the theta the "prefer spreads" doctrine assumed when it set the bar. **A thesis that grades A-minus as a vertical is not automatically A-minus as a single leg.** Worked example from the same run: the MSFT 500C (0.41 delta, $935, 3.21% of mid, perfectly liquid and affordable) was declined not on cost but on **breakeven — 509.35, essentially AT the vertical's 510 short strike**, so the single leg pays ~zero at exactly the price where the graded thesis said to bank $1,205. Substituting the only executable vehicle is fitting the trade to the platform, and **a cheap, liquid vehicle supplies no thesis** applies with full force to one chosen because it is all that is left.
   - **HOW LONG THIS WAS LATENT, and the transferable lesson: THREE TRADING DAYS.** `get_option_orders` shows **every order on this account since 2026-08-19 is single-leg and `placed_agent:"user"` — no multi-leg order had ever been attempted.** The entire "spreads are available" doctrine below was adopted on 2026-08-21 from **the `option_level_3` flag plus a theta measurement**, and the executability assumption underneath it was never tested. **A capability inferred from a permission flag is a hypothesis, not a fact — the only proof a venue accepts an order type is a filled order of that type.** This is the same shape as the PDT error recorded below (reasoning from a secondary indicator instead of the primary one), and it cost three days of arming setups the desk could not place.
   - **Revival:** only a successful multi-leg fill. If Ryan can get agentic multi-leg enabled, the MSFT spec is preserved verbatim in `holdings.json._BLOCKED_SETUP` and can be revived intact.

   **✅ SUPERSEDED IN PART 2026-08-24 — THE LEVEL-3 UPGRADE IS REAL BUT ITS SPREAD BENEFITS ARE UNREACHABLE THROUGH THE AGENTIC API; READ THE BLOCK ABOVE FIRST. Original text follows.** **✅ RESOLVED 2026-08-21T14:45Z — THE ACCOUNT IS NOW `option_level_3` AND `limited_margin`. SPREADS ARE AVAILABLE. READ THIS BEFORE THE OBSOLETE BLOCK BELOW.** Ryan reported the upgrade in a live turn and `get_accounts` was RE-FETCHED and confirms it: **718757339 — `type: limited_margin` (was `cash`), `option_level: option_level_3` (was `option_level_2`), `agentic_allowed: true`.** That satisfies the evidentiary bar the old block set ("no run may act as though the upgrade exists until `get_accounts` actually returns `option_level_3`"). Everything from here to the end of the struck-through block is HISTORY — kept because it records what the constraint cost and how it was measured, NOT as current policy. Two consequences, and the second is the one that bites:
   - **GAINED: debit spreads, and the carry relief they were measured to give.** The 2026-08-14 measurement stands as the reason to prefer them: **4–9× lower net theta at roughly half the capital at risk** versus an ATM single leg. Re-measured live on 2026-08-21 on the desk's own QQQ position: selling the 2026-09-11 660P against the held 680P cuts theta from **−$23.88/day to −$5.67/day (−76%)** and cuts capital at risk from $405 to $179. ~~The standing preference *"Prefer debit spreads over single-legs at this account size"* is now EXECUTABLE and is back in force.~~ **← the blanket preference is RETIRED 2026-08-26 (Ryan, live turn — structure chosen neutrally per setup; see the VEHICLE SELECTION amendment below). The carry measurement stands as the vertical's case, not as a default.** The legging protocol still applies in full: **long leg in first, short leg out first, never short an option without its long leg even momentarily.**
   - **🔧 VEHICLE SELECTION — MATCH THE STRUCTURE TO THE HOLD PERIOD. A SPREAD IS NOT A UNIVERSAL UPGRADE (set 2026-08-21, when Ryan asked for more scalping and more spread usage in the same turn).** These two asks pull in OPPOSITE directions and a run that reaches for a spread on every trade will scalp badly. The rule:
     - **SCALP / DAY TRADE (hours to ~2 sessions) → SINGLE LEG, high delta (~0.40–0.60), nearest liquid expiry above the DTE-7 floor.** Over hours, **theta is nearly irrelevant and delta is everything.** A debit spread deliberately sells away delta and gamma to buy carry relief you will not be around long enough to collect — on the 2026-08-21 QQQ example the spread cut theta 76% but also cut delta from −0.188 to −0.084, so a 1% index move pays less than half. **Do not scalp with spreads.**
     - **SWING (roughly 1–4 weeks) → single leg OR debit vertical, chosen per setup — the old "SWING → DEBIT SPREAD" default is VOID (Ryan, live turn 2026-08-26, confirming v7's neutrality: the prompt must not push the desk to mostly do spreads — "It should trading any and all ways it is allowed to in the agentic account").** The carry math is real (4–9× theta reduction at ~half the capital, where the hold is long enough to collect it) and is the case FOR a vertical; the case FOR a single leg is uncapped tail, one-fill execution, and no legging cost. **The SURVEY decides trade by trade, with a SYMMETRIC honesty test:** a single leg must pay at the thesis target (breakeven scrutiny — the MSFT 500C lesson), and a vertical's capped max profit must fit the target and earn its legging cost (four crossings round trip plus abort risk). A book that expresses nearly every thesis the same one way is drifting, not being consistent — the Friday review checks for it. The blanket preference *"Prefer debit spreads over single-legs at this account size"* is retired wherever it appears in this file's history; measurements citing it stand as the vertical's carry case, not as a default.
     - **THE SPREAD IS THE REAL SCALPING TAX, NOT THETA.** Scalping means more round trips, and every round trip pays the bid/ask twice. At a 10%-of-mid market the underlying must move ~5% just to break even; at 2% it needs ~1%. So **a scalping mandate makes the liquidity gate STRICTER, not looser** — for same-session work require **≤3% of mid**, which in practice means SPY, QQQ, and a handful of mega-caps (NVDA), and essentially nothing from the equity report's mid-cap universe. Widening the gate to find more scalps manufactures losers; this is the same trap CLAUDE.md already names under SOURCING, and higher frequency multiplies it rather than diluting it.
     - **Frequency does not lower the bar for a setup.** More trades must come from more *opportunities cleared*, never from grading the same marginal setup higher because the desk wants to be busy. Boredom is not a catalyst — that rule survives every mandate change.
   - **⚠️ PDT IS GONE — AND A PRIOR VERSION OF THIS VERY BULLET SAID OTHERWISE. READ THE CORRECTION (2026-08-21T15:05Z).** For about twenty minutes this file carried a bullet claiming the upgrade cost the account its PDT exemption, imposed a $25,000 day-trading threshold, and effectively banned day trades. **That was WRONG and Ryan caught it in the same live session.** The facts, verified:
     - **FINRA Regulatory Notice 26-10 (published 2026-04-20) eliminated the day-trade count, the pattern-day-trader designation, the $25,000 minimum equity requirement and the 90-day restriction in their entirety**, replacing them with a real-time **intraday margin framework** under Rule 4210. **Effective 2026-06-04**, with an 18-month phase-in ending 2027-10-20. **Robinhood implemented it day one, 2026-06-04** — no day-trade counter, no PDT flags, no day-trade calls on margin accounts.
     - **So the 2026-08-03 day-trading amendment's CONCLUSION stands: day trades are permitted.** Its stated *premise* ("cash account, no PDT") is obsolete, but the answer is unchanged for a different reason. Do NOT re-derive a day-trade cap from this file's history.
     - **What DOES bind now, and it is not nothing:** (a) a **$2,000 minimum equity** for margin accounts — the account is ~$3,900, above it but only ~2x, and dropping under $2,000 forfeits intraday margin eligibility; (b) **Intraday Margin Deficits** — firms monitor exposure in real time against maintenance requirements, and a deficit must be cured promptly or the account is restricted. On a small options-heavy book that is the new thing to watch, and it is a *position-risk* constraint rather than a *trade-count* one; (c) good-faith / free-riding violations are a cash-account concept and **no longer apply** here.
     - **The DTE floor of 7 and the "never 0–1 DTE" ban are UNAFFECTED.** They exist because a 15-minute cadence cannot manage expiry-day gamma — nothing to do with PDT — and they stand.
     - **🔍 THE RESEARCH LESSON, which is the transferable part.** The bad bullet came from running one web search, getting **Robinhood's own support article still describing the old rule**, and treating a brokerage help page as authoritative. The same result set contained a source titled *"PDT Rule | Requirements, Limits, & 2026 Changes"* and it was not opened. **A brokerage help page is not a primary source for a rule that recently changed — vendor docs lag regulators by months.** When a rule is load-bearing enough to gate the automation's behavior: find the regulator's notice or the SEC filing, check whether an effective date has passed, check whether *this* broker has implemented it, and treat a "20XX changes" headline in your own results as a stop sign rather than noise. And the meta-lesson: this file's authority comes from being checked, so **a confidently-written rule is not evidence — a bullet asserting a hard constraint with a scary number in it deserves MORE scrutiny than a soft one, not less.**
   - **GAINED: instant settlement.** `unsettled_funds` is now tradeable rather than frozen, so FOUR LAWS #4 ("settled cash only... never risk a good-faith violation") relaxes: good-faith violations are a cash-account concept. **The rule's INTENT survives unchanged — never deploy money that does not exist.** Size against `get_portfolio`'s `buying_power`, and note that on 2026-08-21 `buying_power` equalled `unleveraged_buying_power` ($1,942.95), i.e. **no borrowed funds were being extended.**
   - **🚫 ANSWERED AND CLOSED — NO MARGIN BORROWING, EVER. THE ACCOUNT IS MARGIN-CAPABLE; THE DESK IS NOT MARGIN-AUTHORIZED.** Ryan, REAL live turn 2026-08-21: *"I know it's a margin account now but I do not want to actually use margin."* This is now a HARD RULE, not a preference, and it is not overridable by any thesis:
     - **Total deployment may never exceed `unleveraged_buying_power` from `get_portfolio`.** Check that field, not `buying_power` — on 2026-08-21 they were equal ($1,942.95), but they diverge the moment the broker extends margin, and `buying_power` is the one that silently includes borrowed funds. If they differ, **the smaller number is the budget.**
     - This is consistent with his 2026-08-05 posture, where he deliberately de-levered the joint account and explicitly rejected re-margining to buy into a pre-drawdown tape. The upgrade changed what the account *can* do, not what the desk is *allowed* to do.
     - **Corollary that matters under the intraday-margin framework:** never construct a position whose maintenance requirement could exceed unleveraged equity intraday. Long options and debit spreads are fully paid at entry, so this is automatic for the permitted vehicles — which is one more reason the naked-short ban is absolute.
   - **UNCHANGED by the upgrade:** the $1,500-per-trade max, the −$200 daily realized loss cap, max 3 open agentic positions / 2 per correlated theme, the DTE floor, the exit engine, the trend-maturity gate, the ownership/exit gate, the equity autonomy ban (HARD RULE 6), and the Aug–Oct defensive posture. **A better vehicle is not a looser gate** — level 3 changes HOW a thesis is expressed, never WHETHER a setup is worth taking.

   **📜 HISTORICAL — THE BLOCK BELOW DESCRIBES THE PRE-2026-08-21 LEVEL-2 CONSTRAINT AND IS NO LONGER OPERATIVE. Do not act on it.** ~~🔒 SINGLE-LEG ONLY — THE AGENTIC ACCOUNT CANNOT TRADE DEBIT SPREADS (verified 2026-08-14T16:33Z). CHECK THIS BEFORE SENDING LEG ONE.~~ `get_accounts` shows 718757339 at **`option_level_2`**, and the connector's own `get_option_level_upgrade_info` documentation states it plainly: *"option_level_2 enables long calls/puts, covered calls, and cash-secured puts; option_level_3 adds spreads and complex strategies."* So the routine prompt's authorization of "debit spreads — legged ONE order at a time" and the standing preference *"Prefer debit spreads over single-legs at this account size"* are **not executable here**. Treat spreads as UNAVAILABLE, not as a preference to override.
   - **It fails UNSAFELY, which is why this is a hard warning and not a footnote.** The legging protocol sends the LONG leg first. On a level-2 account that leg **fills**, and the short leg is then **rejected** for insufficient options level — leaving the sleeve holding an unintended, full-premium, naked single-leg it never decided to own, at up to the $1,500 per-trade max, carrying exactly the theta profile the spread was chosen to avoid. A run that reaches for a spread without checking `option_level` first does not get a failed order; it gets a position.
   - **What the missing permission costs, measured (2026-08-14, AVGO 2026-08-28 board, 14 DTE, spot 394.825).** Vehicle economics — not budget, not thesis — killed the week's best candidate twice. Single legs: 395C mid $12.85 (theta −3.67%/day, and $1,285 = 86% of the per-trade max), 410C mid $6.98 (−6.17%/day), 420C mid $4.375 (−8.31%/day). The verticals that would have replaced them, at mid: **395/415 debit $735 → net theta −1.02%/day**; **395/410 debit $587 → −0.70%/day**; **400/420 debit $607.50, max profit $1,392.50 → −1.62%/day**. That is a **4–9× reduction in carry** on the one variable that keeps disqualifying setups, at roughly **half the capital at risk** versus the ATM single leg.
   - **Recommendation standing for Ryan** (recorded by an unattended run — HARD RULE 9: no approval is claimed or implied): apply for `option_level_3` on the agentic account at `applink.robinhood.com/upgrade_options?account_number=718757339`. His individual account 5ST95513 already carries level 3, so the level-2 setting is per-account, not an account-holder limitation. Note the honest framing when discussing it: the 2026-08-14 sizing amendment gave the desk **more money**; it did not give it a **cheaper way to carry a position**. Level 3 would — this is the more binding constraint of the two.
   - **No run may act as though the upgrade exists until `get_accounts` is RE-FETCHED and actually returns `option_level_3`.** A stale or remembered level is not evidence. Both directions: calls on bullish confluence, puts on bearish. Use the **free Robinhood chain** (`get_option_chains` / `get_option_quotes` for strikes, greeks, IV) — no paid feed. Require the stack: our technical signal (momentum>200MA or RSI2) **+** the HARD RULE 7 news/thesis gate (which carries extra weight here, standing in for an order-flow check) **+** IV sanity (skip extreme IV / imminent earnings unless that's the thesis) **+** liquidity (tight spread, real OI). Default contract ~30–45 DTE, ~0.35 delta; shorter DTE (floor 7) allowed for day trades per the 2026-08-03 amendment below. Sizing (**2026-08-14 sizing amendment — SUPERSEDES the 2026-08-05 reading; Ryan, live turn: "the 1500 is a max per trade. However it can utilize more that 1500. The only thing it cant do is use non-existent funds or touch the equity trades"**): **$1,500 is a PER-TRADE maximum, NOT a total-sleeve budget.** Total agentic-placed premium-at-risk may exceed $1,500; it is bounded by **settled cash**, not by a fixed sleeve cap. Ryan's stated intent was explicitly to give the desk MORE ammunition and MORE decision-making latitude, because the prior reading was producing an entry freeze — so do NOT re-derive a total cap from this rule. Two hard limits only, in his words: (a) never use funds that do not exist (settled cash only — cash account, T+1, no good-faith violations), and (b) never touch equities (HARD RULE 6 autonomy ban, reaffirmed by him in the same turn). *Prior reading, now void: "total AGENTIC-placed premium-at-risk ≤ $1,500" — the 2026-08-05 turn ("it can use up to 1500 for placing 1 option or multiple") was read as a sleeve-wide budget; Ryan clarified on 2026-08-14 that he meant per trade. The old reading also replaced the ≤$150/trade cap and the 15%-of-account sleeve cap, and those stay dead.* Size must still scale with conviction: the bigger the premium, the more decisively the full pre-trade checklist (esp. the honesty test) must pass — "quality, thought-out" is the condition Ryan attached. Max loss = premium; still its OWN sleeve, separate from the equity caps AND from Ryan's manually-placed option positions (see the 2026-08-03 amendment). Settled cash always bounds actual deployment (at amendment time the account was ~$4.0k total / ~$1,584 settled BP, so the full budget ≈ all spendable cash — deploying it all at once leaves nothing for the next setup or an equity batch; stagger entries unless one setup is truly exceptional). Review with `review_option_order` (show quote, greeks, fees, **max loss**) and use the one-tap batch approval. Exits: per the **2026-08-05 exit-method amendment** in the automation ruleset below — setup-break primary + DTE-scaled premium backstops on losers, pop-banking + trailing ratchet on winners, 21-DTE management review. Log fills in `holdings.json` under `sleeve:"options"`. Full spec: `docs/options-strategy.md`.

   **Options automation ruleset (autonomous per HARD RULE 6; set 2026-07-01, day-trading amendment 2026-08-03).** When running the options sleeve without per-order approval:
   - **Day-trading amendment (Ryan, live turn 2026-08-03: "Not alot of activity for this agent. It's ok if this agent day trades options... expected to see more trades" → approved this spec with a single "Approve").** Context: the automation had been rule-locked since 2026-07-22 — Ryan's own in-app option buys (~$691 premium) consumed the whole 15% sleeve cap, and all open positions were his (exit-gated), so 26 runs/day each correctly did nothing. The fix + activity unlock:
     - **Agentic sub-sleeve:** the premium-at-risk accounting (**$1,500 per trade**, total bounded by settled cash, per the 2026-08-14 sizing amendment; was a $1,500 sleeve-wide budget, and before that 15%-of-account) counts ONLY agentic-placed positions. Ryan's manually-placed options (`placed_agent: "user"`) do NOT count against the automation's budget — they're his book, tracked and flagged but not a lockout.
     - **Day trades allowed:** same-day round trips are permitted. Minimum DTE lowered from ~30 to a **hard floor of 7** — never 0–1 DTE (15-min cadence blind spot, see the honest limit in HARD RULE 6).
     - **Short-DTE cycle rules (contracts < ~21 DTE):** take-profit +30–50%; −50% cut, confirmed by one re-check the following run (2026-08-05 exit-method amendment); **always flat by DTE 2** (no riding into expiration week's gamma). The 30–45 DTE swing style follows the 2026-08-05 exit-method amendment below (setup-break primary, DTE-scaled backstops, ratchet winners, 21-DTE review).
     - **Entry throttle:** still max ONE new entry per run, now up to **3 new entries per day** (was effectively 1). Per-trade premium cap: **$1,500 per trade** (2026-08-14 sizing amendment — the old ≤$150/trade cap stays dead; the 2026-08-05 "no per-trade limit / $1,500 total" reading is void). Total deployment is bounded by settled cash, not by a sleeve budget.
     - **Unchanged and non-negotiable:** ~~settled-cash only (T+1 option settlement means ~2–4 round trips/day before waiting on settlement — cash account, no PDT)~~ **← the PREMISE is obsolete but the CONCLUSION stands (corrected 2026-08-21T15:05Z). The account is `limited_margin`, so settlement is instant and the settled-cash gate no longer binds; and FINRA's PDT regime was abolished effective 2026-06-04 (Robinhood implemented day one), so there is no day-trade count, no PDT flag and no $25,000 threshold. Day trades remain PERMITTED. What binds instead is the $2,000 margin-equity minimum and real-time intraday margin deficits — see the PDT correction above. An earlier version of this line claimed day trades were effectively banned; that was wrong.** Still unchanged: the daily loss cap (−$400 since the 2026-08-05 sizing amendment; was −$150), the full pre-trade checklist below on every entry, and the exit gate on Ryan-placed positions.
   - **Pre-trade "best-interest" checklist — ALL must pass or SKIP the trade:** (1) max loss = premium, AND this entry's premium ≤ **$1,500 (PER TRADE)** per the 2026-08-14 sizing amendment — there is NO total-sleeve cap to check against; total deployment is bounded by settled cash only (Ryan's manual positions remain excluded from agentic accounting per the 2026-08-03 amendment; larger premium ⇒ proportionally stronger conviction required, and only an A++ setup should use the full $1,500); (2) full entry stack passes (technical signal + HARD RULE 7 news/thesis + IV sanity + liquidity: tight spread, real OI); (3) **settled** buying power covers it (cash account — never trade unsettled funds); (4) the daily loss cap is not hit; (5) the honesty test — "would I approve this if Ryan asked, purely on the port's interest?" If not a clear yes, don't trade.
   - **Trend-maturity gate — read past + present to project FORWARD; catch turns early, never chase a trend that looks complete (Ryan, live turn 2026-08-10).** Incident this rule fixes: the 2026-08-10 morning run bought a ZTS Sep 70P on a textbook breakdown signal (mom12-1 −51.2%, below 200MA, guidance-cut news) — but the stock was already −42% YTD, the bad news was 2 sessions old and fully priced, and that same morning the tape was turning (+2% day, heavy call volume at the Oct 80 strike, analyst targets still far above spot). Ryan sold it hours later at −$87: "we were trying to follow a trend that had already completed." The signal described the PAST; the trade needed a reason the move would CONTINUE. Rule — before ANY directional options entry (puts and calls alike), all three maturity checks must pass or SKIP:
     - **(a) How far/long has the move already run?** % off the 52-week high/low and days since the driving catalyst. A short entry after a −40%+ decline (or a long after a huge run) is late-stage by default and needs extraordinary justification.
     - **(b) Is the catalyst already priced?** If the news that justifies the trade is more than a session or two old and the stock has fully reacted, the edge is gone — post-catalyst drift fades fast. Don't re-trade old news.
     - **(c) Does the PRESENT tape confirm or diverge?** Same-day price action, options-flow skew (call vs put volume), and the direction of the NEWEST analyst revisions must not contradict the position. A bearish entry into a green tape with heavy call buying (the ZTS case exactly) is a divergence = SKIP — and often a candidate for the OPPOSITE side once the full entry stack confirms the turn.
     The mandate is anticipation: enter where the next move is starting, not where the last move already happened. A late-stage trend with a diverging tape is never an A-grade setup regardless of how clean the technical print looks.
   - **Exits enforced on EVERY run (this is where losses are minimized) — but ONLY on positions the automation itself opened. 2026-08-05 EXIT-METHOD AMENDMENT (Ryan, live turn: don't sell a loser on a −50% premium print alone — "the next day it could be back to positive"; on winners "take big pops" but "if structurally there are so many good variables telling us will go up more we let it ride"; approved the spec with a single "Approve"). Replaces the flat −50% cut and the fixed +50–100% TP:**
     - **LOSERS — thesis-first, patience scaled to time-to-expiry** (the pro toolkit: underlying-based stops, the ~21-DTE management rule, time stops):
       - **PRIMARY exit = setup break, not premium %:** sell when the UNDERLYING invalidates the entry — closes through the `setup_invalidation` level recorded at entry, or the HARD RULE 7 thesis breaks. A premium drawdown with the setup intact is leverage/vol noise, not a sell signal.
       - **DTE-scaled premium levels:** **>45 DTE:** −50% = alert + thesis re-check only, hold if the setup is intact; hard backstop **−70%**. **21–45 DTE:** −50% = re-check; sell only if it holds ≤−50% for ≥2 consecutive runs AND the thesis re-check fails; hard backstop **−65%**. **7–21 DTE (short-DTE entries):** the −50% cut stands, confirmed by one re-check the following run — no deeper patience, there's no time left to recover.
       - **Hard backstops are unconditional** (they exist so a true loser can't ride toward zero) — only a `manual_hold_override` or the hedge-put exemption suspends them.
       - **Time stop / 21-DTE management review (replaces the old DTE<~14 roll-off):** at **21 DTE** every swing position gets a roll/close decision; a position flat or losing at 21 DTE is closed or rolled, never held into the accelerating theta/gamma window. Flat-by-DTE-2 for short-DTE entries unchanged. Pre-earnings close/roll unchanged.
     - **WINNERS — pop-banking + trailing ratchet (no fixed TP ceiling):**
       - **POP-BANK:** a one-day gain ≥ **+80%** (or a blow-off spike into overbought) → sell into strength and recycle the capital into the next trade.
       - **RATCHET:** once up **+50%** a profit lock ARMS: track `peak_premium` in `_current_state` every run and exit if the gain gives back **~40% of its peak** (peak +100% → exit floor ≈ +60%). The floor only ratchets UP.
       - **LET IT RIDE:** while the full entry stack stays intact (trend + momentum + HARD RULE 7 thesis), do NOT sell at a fixed target — the pop rule, the ratchet, and the 21-DTE review are the only winner exits. Short-DTE entries keep the quicker +30–50% banking.
     - **Ledger duties for this engine:** on every entry record `setup_invalidation` (the underlying price level that kills the trade, + one line on why); every run overwrite `peak_premium` and the mark in `_current_state`. Options still do NOT get equities' open-ended "hold underwater on thesis" treatment — the backstops and the 21-DTE review are unconditional.
   - **Exit order pricing — don't just cross the spread (Ryan, live turn 2026-07-21).** A sell-to-close should NOT default to a fully marketable price (essentially hitting the bid) — that gives up the spread on every exit for no reason. Price the closing limit order at/near the **midpoint** of the live bid/ask, only stepping down toward the bid if it isn't filling and the exit is time-sensitive. Full detail: `docs/options-strategy.md` ("Order pricing on exits").
   - **Exit gate by who placed the entry (added 2026-07-21, Ryan live turn: "If I make the trade not you, then ask before you sell. Any trades you make on your own you can do as you please following your guide.").** Before executing ANY exit (mechanical premium-backstop stop, pop-bank/ratchet take-profit, broken-thesis/setup-break close, time-stop/DTE/earnings roll-off) on an option position, check who opened it — `placed_agent` on the fill / the reconciliation `_note` in `holdings.json`:
     - **`placed_agent: "agentic"` (the automation opened it itself):** full autonomy per this ruleset — enter AND exit mechanically, no approval needed, exactly as below.
     - **`placed_agent: "user"` (Ryan placed the entry himself in the app):** the automation may still *detect* and flag a fired exit condition, but must NOT auto-execute the sell. Record the trigger in `holdings.json` (which threshold fired, current mark/%, thesis note) and notify Ryan, then wait for his explicit go-ahead in a live session — treat it like an equity approval, not a mechanical cut.
     - This does not change entries — new autonomous entries still run the full checklist below unchanged. It only gates the *exit* side, and only for positions Ryan placed himself.
     - **Incident this rule fixes:** the 2026-07-21 run closed Ryan's own RBRK 90C add (`placed_agent: "user"`) at the mechanical −50% stop without asking; Ryan wanted a heads-up first on a position he'd chosen to place himself, even though the same mechanical rule is exactly what he wants applied automatically to positions the automation opens on its own.
   - **Manual hold override (added 2026-07-16, incident: WULF 24C closed at -50% despite Ryan having told an interactive session to hold it — that instruction was never written to the repo, so this automation had no way to see it and closed it mechanically).** Before enforcing the **mechanical premium-stop** (any DTE tier — the −50% short-DTE cut or a −65/−70% backstop, per the 2026-08-05 exit-method amendment) on any option position, check that position in `holdings.json` for a `manual_hold_override` field. If present, **skip the premium-stop exit only** for this run and don't re-litigate it. Shape: `"manual_hold_override": {"set_utc": "...", "note": "Ryan, live turn 2026-0X-XX: '<quote or paraphrase>' — holding through the drawdown on <reason>"}`.
     - **Same evidentiary bar as HARD RULE 9: only a genuinely interactive session with a real, live Ryan turn may add or remove this field — an automated/cron-triggered run can never set one for itself.** Letting automation grant its own holds would recreate exactly the fabricated-approval loophole HARD RULE 9 closes, just aimed at losses instead of unauthorized buys.
     - It suspends the premium stop ONLY — the **broken-thesis / setup-break** exit and the **21-DTE review / pre-earnings** exit still apply in full. This is a "let Ryan keep watching a name past the mechanical trigger" valve, not a blanket loss-control bypass.
     - **Any interactive session that gets a verbal hold/override instruction from Ryan on an options position MUST write it into `holdings.json` and push to `master` before the session ends.** A decision that only exists in chat is invisible to the next unattended automation run — this is the "write the lesson back" discipline applied to trade-management instructions, not just process fixes.
   - **Daily loss cap:** if realized options losses hit **−$400 in a day** (Ryan's choice 2026-08-05, raised from −$150 alongside the $1,500 budget; ~10% of the then-$4.0k account), STOP opening new option trades until the next session. Exits still run — the cap gates NEW entries only.
   - **Size:** up to **$1,500 per trade**, with total deployment bounded by settled cash rather than a sleeve budget (2026-08-14 sizing amendment; the 2026-08-05 "$1,500 total" reading is void). The ~0.35-delta default profile is now affordable on most candidates — prefer the liquid, right-delta strike over a cheap illiquid one (the old $150 cap forced exactly that bad trade-off; see `docs/options-strategy.md` "Known friction", now resolved). Scale size to setup quality: an A-grade full-stack setup can take $500–1,000+; a marginal one stays small or is skipped. **Interplay with the Aug–Oct defensive posture (both set 2026-08-05):** the ≥45% cash floor governs the EQUITY book and the $1,500 options budget is authorized alongside it — but during the drawdown watch, lean the sleeve toward defensive structures (puts/hedges) and remember full deployment consumes essentially all settled cash. Log every fill to `holdings.json` (`sleeve:"options"`) immediately, with entry, max loss, and the exit plan.
   - **🎯 SOURCING — hunt where the CHAINS are liquid, not only where the equity screen points (root fix, 2026-08-14; Ryan approved the diagnosis in the same live turn as the sizing amendment).** The week of 2026-08-10 produced ZERO entries, and the post-mortem showed the binding constraint was **liquidity, not budget**: of five candidates worked, four died primarily on bid/ask — IREN 14.5–20.6% of mid, ROST 12.8% on **zero** open interest, LIN 17–48%, ACGL 25.6–50.7% on a $5-wide strike ladder — against a <10%-of-mid preference. Only INTC had a tradeable chain all week, and it simply never triggered. The cause is a **sourcing mismatch**: the v3.1 watchlist rule sends the hunt to the equity report's ~220-name screen, which is tuned for *stock* setups and is mostly mid-caps with thin or dead options chains. Meanwhile the core IV list (SPY, QQQ, NVDA, AMD, TSM, AVGO, MSFT, TSLA) — all penny-wide, deep-OI chains — was logged daily but never *hunted*. Fix, applied to every run's HUNT phase:
     - **Check candidate sources in this order: (1) the core IV list and other genuinely liquid chains (mega-cap tech, major ETFs, high-volume single names); (2) the committed equity report's fresh RSI2 / momentum / breakdown signals; (3) macro & catalyst work.** The report stays a real source — it is the only comprehensive screen we have — but it is no longer the FIRST place to look, because its hit rate into a tradeable options chain is demonstrably poor at this account size.
     - **Screen liquidity EARLY, not last.** A candidate's chain should be checked before the full thesis workup, not after. Four of this week's five write-ups spent full research effort on names that a 30-second spread check would have killed. That is wasted run budget and it crowds out real candidates.
     - **Do NOT solve the freeze by loosening the spread preference.** At a 25% spread the underlying must move ~5% before the round trip breaks even — widening the gate manufactures losers rather than opportunities. The <10%-of-mid preference stays; the SOURCING changes.
     - **A liquid chain is a prerequisite, never a thesis.** Being tradeable does not make a setup worth taking, and the full entry stack (trend-maturity gate, HARD RULE 7 news/thesis, IV sanity, "why NOW") applies unchanged. Expect this fix to raise the *quality of the candidate funnel*, not to guarantee more fills.
     - **💰 AFFORDABILITY TIERS — the core IV list is NOT uniformly buyable at this account size, and half of it is a research trap (measured 2026-08-14T17:03Z).** The sourcing fix above sent the hunt to the core IV list, and the runs of 2026-08-14 then priced only the *high-priced single names* on it — AMD 2026-09-11 505C **$3,308**, AMD 530C **$2,280**, AVGO earnings-clean ATM **$1,370**, INTC 2026-09-18 105C **$938**. From four unaffordable quotes in a row the desk drifted into treating *"the core list is too expensive for a $1,500 per-trade cap"* as settled fact, and stopped looking. **Nobody had measured the index ETFs.** Measured live: **QQQ 2026-09-18 (35 DTE) 750C = 8.80 × 8.87, mid 8.835, spread 0.79% of mid, OI 41,640, delta 0.339, IV 17.89% → $883.50/contract; the 710P = 9.47 × 9.53, mid 9.50, spread 0.63% of mid, OI 25,761, delta −0.308, IV 20.62% → $950.00/contract.** That is a right-delta, mid-band-DTE contract **in either direction at ~60% of the per-trade max**, on a market roughly 20× tighter than the 12–50% spreads that killed four of the prior week's five candidates.
       - **The mechanism, so no run re-derives it:** premium scales with **spot × IV × √time**, so the core list splits on the *IV* term, not the share price. ETFs carry 18–21% IV; the high-priced semis carry 60–90%. AMD at **$503** spot costs $3,308 for a right-delta call while QQQ at a **higher $729** spot costs $883 — because QQQ's vol is a third of AMD's. **Spot price alone is not the affordability test**, and reasoning from it is exactly what produced the false conclusion.
       - **The tiers.** AFFORDABLE at ~0.35 delta / 30–45 DTE inside the $1,500 per-trade max: **QQQ, SPY, NVDA (~$225 spot), INTC, MSFT**. UNAFFORDABLE in any real-delta single leg: **AMD, AVGO, TSM** (TSLA borderline) — for these the debit spread that *would* fit was structurally unavailable at `option_level_2`. **THIS TIER LIST IS NOW PARTLY OBSOLETE (2026-08-21): with `option_level_3`, the debit spread that fits AMD/AVGO/TSM/TSLA is available, so "unaffordable in any real-delta single leg" no longer means "untradeable." Re-price these as verticals before excluding them — but re-measure at the target delta first, and remember a cheap, liquid vehicle still supplies no thesis.**
         - **MSFT MOVED TIERS — measured 2026-08-17T14:32Z, correcting this file's own error.** MSFT was listed UNAFFORDABLE on the strength of ATM quotes alone (the 2026-09-18 485C at $1,517.50, which does exceed the max). Nobody had priced a **~0.35-delta** strike, which is the tier test as written. Measured: the **2026-09-18 475P — bid 10.90 / ask 11.20, mid 11.05, spread 2.7% of mid, delta −0.395, OI 977, IV 26.47% → $1,105/contract, 74% of the per-trade max.** Liquid, right-delta, comfortably affordable. This is the SAME mistake the tier note itself was written to fix — reasoning about affordability from the wrong quote (there, spot price; here, the ATM strike) and generalizing to "the name is priced out." **The tier test is a ~0.35-delta contract, not the ATM one**; an ATM leg on a $480 stock will often breach the max while the 0.35-delta strike sits far inside it. Re-measure at the target delta before tiering any name, and note the corollary that always applies: proving a vehicle is cheap and liquid supplies **no thesis whatsoever** (this same run measured MSFT's chain and still declined the trade).
         - **🔁 STOP WRITING THIS LESSON PER-INSTANCE AND STATE THE CLASS: BEFORE EXCLUDING ANY NAME ON LIQUIDITY OR PRICE, QUOTE THE *MONTHLY* EXPIRY AT THE *TARGET DELTA*. THIRD OCCURRENCE, THIRD DIFFERENT WRONG QUOTE (measured 2026-08-24T15:03Z).** The failure is always the same shape — a name is declared untradeable from a quote that was never the tier test — and only the wrong quote changes: **spot price** (2026-08-14, "AMD is priced out"), then the **ATM strike** (2026-08-17, "MSFT is priced out", the bullet directly above), and now the **wrong EXPIRY BOARD**. On 2026-08-24 the 13:39Z IV sweep recorded MSFT at *"spread 1.80 on 15.00 mid = 12.0%, failing both liquidity gates"* — measured on the **2026-09-25** board. The **2026-09-18 MONTHLY** 490C the same morning: **12.10 × 12.40, mid 12.25, 2.45% of mid, OI 7,013, 643 traded** — roughly **5× tighter**, and the difference between "untradeable" and the best-structured candidate on the desk. Non-monthly weeklies on a mega-cap are thin; the monthly carries the open interest.
           - **The detector, which is the part to keep:** if a name on the core IV list appears to quote **wider than ~5% of mid**, suspect the **board** and the **strike** before believing the name is illiquid. Mega-cap monthlies do not quote at 12%. A surprising illiquidity reading on a liquid name is a measurement bug until a monthly-at-target-delta quote says otherwise.
           - **The subtler half, because the IV sweep was NOT careless:** the IV method legitimately specifies the expiry nearest **30 DTE** (on 2026-08-24 that is 9/25 at 32 DTE, not 9/18 at 25). The IV measurement was correct. The error was letting a spread **observed incidentally during an IV sweep** stand as a **liquidity verdict on the name**. Those are different questions asked of different boards — **never let an IV-sweep spread disqualify a candidate; re-quote the board you would actually trade.** Do not "fix" this by changing the IV method.
       - **Why this matters more than it looks:** the sleeve was never capital-constrained out of the liquid universe — it was **research-constrained into the unaffordable half of it**. Ryan lifted the sleeve cap to end an entry freeze; a desk that keeps quoting $3,000 AMD calls re-creates that freeze under a different name. **Check the affordable tier FIRST**, and re-measure before assuming a name is priced out — but note the corollary that cuts the other way: proving a vehicle is cheap and liquid supplies **no thesis whatsoever**, and the temptation to fit a trade to a newly-affordable contract is the same cherry-picking this playbook already forbids.
   - **⏱️ A MEASURED CRUSH IS NOT A MEASURED BARGAIN — AND ON THE DAY OF A PRINT THE ARTIFACT ABOVE IS *PENDING*, NOT PRESENT, SO IT IS PREDICTABLE RATHER THAN MERELY DETECTABLE (measured 2026-08-27T13:33Z, the morning after NVDA reported).** The bullet below catches a gap that is ALREADY INSIDE the trailing window. The mirror case is the day the gap happens: it has not closed yet, so it is NOT in the window, and the ratio is briefly honest — which is exactly when a run is most likely to quote it and carry the number forward. Measured: NVDA ATM IV on the 2026-09-25 board fell **0.4144 → 0.341758 across the print, −7.26 vol points (−17.5%)**, the crush the five-session ramp had been accumulating toward. That crush was real. It did **not** make premium cheap: against realized the post-crush IV read **0.9457 raw but 1.0710 EX-GAP**, i.e. fair-to-slightly-rich, and ex-gap governs because that board expires long before the next print. **"IV crushed 17.5%" and "premium is now cheap" are different claims and only the first was true.**
     - **The predictable half, and it is the reason to write this down rather than re-detect it: compute what the gap does to the window BEFORE it lands.** With the +6.79% gap not yet closed, NVDA's rv_30d was 0.3614. Adding that one day raises it ~12% to ~0.4036 and drops the RAW ratio to **~0.8468 — it will read about 15% cheap tomorrow, on unchanged implied vol.** The ex-gap ratio barely moves (1.0710 → ~1.0335). So on print day, **state the next session's raw ratio in advance and label it an artifact**, so the following run inherits the warning instead of rediscovering it a day late — or, more likely, not at all.
     - **Detector:** on any day a name in the hunt gaps hard, the trailing window is stale-LOW until that day closes. Quote the ex-gap ratio as the decision input, and quote the projected raw ratio only to disarm it.
     - **✅ THE FORECAST ABOVE WAS TESTED THE NEXT SESSION AND HELD IN DIRECTION, MISSED IN MAGNITUDE — AND THE MISS IS INSTRUCTIVE BECAUSE IT LANDED ENTIRELY ON THE LEG THE FORECAST HELD FIXED (measured 2026-08-28T15:31Z).** Predicted for 08-28: raw ratio **~0.8468**, ex-gap **~1.0335**. Actual, NVDA 2026-09-25 board at the 225 strike: raw **0.7856**, ex-gap **1.0203** at 13:41Z. **Ex-gap was accurate to 1.3%** — the artifact-disarming half of the method works. **Raw came in 7.2% BELOW the forecast**, and the reason is the forecast's own stated assumption: it projected the window change *"on unchanged implied vol"*, and implied vol did not hold still — NVDA's ATM IV kept bleeding after the print (0.341758 on 08-27 → 0.336915 at 13:41Z → **0.324920 on the settled 15:31Z re-measure**). **A ratio has two moving parts; a forecast that freezes one will be right about the artifact and wrong about the level.** Say which leg is held fixed when stating one, so the next run knows where the error will land.
     - **📉 AND THE READ THIS PRODUCES TODAY, WHICH RETIRES A STANDING OBJECTION: NVDA'S EX-GAP RATIO HAS CROSSED BELOW 1.0 (0.9840 SETTLED). THE FOUR-SESSION PRE-PRINT VOL RAMP IS FULLY UNWOUND — STOP CITING IT.** The 2026-08-25 finding recorded NVDA's event premium ramping four sessions to an ex-gap **1.2459**, the richest reading in the file, and that has been the standing reason to refuse long NVDA premium. Settled at 15:31Z: atm_iv **0.324920** (call 0.321493 / put 0.328347) against rv_30d 0.4289 and ex-gap rv 0.3302 → raw **0.7576**, ex-gap **0.9840**. Rich → essentially fair-to-marginally-cheap. This is the same class as the 08-28T14:45Z retirement of the QQQ objection, and it earns the same caveat, which is the more important half: **IV sanity is a NECESSARY condition and never a thesis — fair vol supplies no reason whatsoever to own anything.** Both objections are now discharged and CORE still declines, on thesis.
     - **🔁 THE OPENING-WINDOW ATM INFLATION IS NOW CONFIRMED ON A THIRD NAME AND A HIGH-VOL ONE.** Re-measures against the same day's 13:41Z rows: SPY **−2.53%**, QQQ **−4.37%** (both 14:47Z), NVDA **−3.56%** (15:31Z), against the 2026-08-27 audit's −4.23%. Four readings, one direction, a tight −2.5% to −4.4% band spanning a 12-vol index and a 32-vol single name. **Treat any pre-14:00Z ATM row as an upper bound and re-measure before it decides anything** — and note that the duty is per-name: the 14:45Z run discharged the caveat for SPY and QQQ and NVDA's row sat un-discharged for two hours while being the one name with a real move on the tape.
   - **💱 AN UNPRICEABLE QUOTE IS NOT AN UNINFORMATIVE ONE — REFUSING TO PRICE OFF A WIDE MARKET IS CORRECT; CONCLUDING THE MOVE IT IMPLIES IS FAKE IS A SEPARATE CLAIM NEEDING SEPARATE EVIDENCE (measured 2026-08-27T13:32Z).** The 2026-08-26T21:20Z run recorded PNC's after-hours print of 241.1315 as *"a thin print, not a −1.46% move"* and valued the position off the 244.71 official close instead. **The procedure was right and the inference was wrong.** That quote sat on a 240.00 / 247.30 market — $7.30 wide — and genuinely could not be priced against; but the next session opened at **241.19 and traded 240.99 within two minutes**, landing within six cents of the print the desk had dismissed. The wide market was uninformative about the *exact* level and quite informative about the *direction*.
     - **The distinction to keep:** "I cannot value the book off this" and "this tells me nothing" are different findings. The first is about the spread; the second is about the signal, and a spread is not evidence against a signal. Report the honest version — *"marking to the official close; note the after-hours print implies roughly −1.4% and is worth watching at the open"* — rather than converting a valuation refusal into a directional dismissal.
     - **Why it matters beyond one ledger row:** the same reflex applied to an OPTION mark or a structural level would silently discard a real move. This is the family the file already names — a check that passes for the wrong reason — arriving as an over-broad conclusion drawn from a correct procedure.
   - **📉 THE `iv_rv_ratio` LIES AFTER AN EARNINGS PRINT — read the pair, not the headline (measured across the whole core list 2026-08-20).** `rv_30d` as the IV method defines it is a **30-sample** stdev, so a single earnings gap inside the window dominates it and makes implied vol look artificially CHEAP. Measured live at 13:31Z on 2026-08-20, dropping just the two largest absolute daily moves from each window: **SPY 1.06→1.23, QQQ 0.89→1.10, NVDA 1.03→1.14, TSM 0.89→1.26, AVGO 1.12→1.34, TSLA 0.72→1.24, MSFT 0.52→1.01, RBRK 1.20→1.40** — six of nine names flipped from "cheap premium" to "fairly-to-richly priced", and only AMD stayed below 1.0. The error is **directional**: the mechanical number systematically invites buying premium that is not actually cheap, and it does so hardest right after a print, which is exactly when the desk is hunting.
     - **The two worst offenders were the most inviting.** MSFT's headline 0.52 — the most extreme "half-price vol" print the desk has ever logged — is almost entirely one **+14.42% gap on 2026-07-30**; ex-gap realized is 25.9% against 26.1% implied, i.e. premium is priced almost exactly **fair**. TSLA's 0.72 is one **−15.69% gap on 2026-07-23**; ex-gap the ratio is **1.24, rich**. A run reasoning from the headline would have concluded "MSFT and TSLA vol is on sale" and been wrong on both.
     - **Log BOTH numbers and read them as a pair**, which `iv_history.json` now does (`rv_30d`, `rv_30d_ex_top2`, both ratios, and `largest_1d_move_in_window_pct`). Neither is "the" answer: for a contract that **expires before** the next print, the ex-gap ratio is the more honest gauge; for a contract that **spans** a print, the raw ratio is, because the buyer genuinely bears that gap. Trimming the top two moves is itself a crude estimator that **understates** fat-tail risk — do not swap one headline number for another.
     - **Precedent, and why this is written at the file level rather than in one row:** the 2026-08-13 LIN row in `iv_history.json` already caught this exact artifact on one name ("ex-gap rv_30d is 0.1936 and the ratio flips to 1.09... an artifact of one event day, not a standing edge") and nobody generalized it, so five later runs kept citing raw ratios. A lesson recorded only against a single ticker does not survive the relay.
   - **⚠️ ROUTINE-PROMPT CONFLICTS — resolve them THIS way until Ryan edits the routine (recorded 2026-08-14, live turn).** The routine's stored prompt was rewritten by Ryan (the "JUDGMENT-FIRST / FOUR LAWS" version) some time after 2026-08-12, and `docs/options-automation-setup.md` still described the older v3.1 — so an instance reading the docs got the wrong numbers. His stated purpose in rewriting it: **"I have been trying to give the agent more ammo to make trades because i think our governance is too strict and no trades are happening... I was trying to give more decision making and autonomy to the agent."** Read every ambiguity in that prompt in the direction of MORE latitude, not less. Specific conflicts, with the settled reading:
     - **"Prefer max ~$750 total premium deployed" (live prompt) vs. no total cap (this file, 2026-08-14 amendment): the ~$750 line is VOID as a total-sleeve ceiling.** It is arithmetically incompatible with Ryan's own $749 authorized SPY hedge — it left ~$1 of headroom and made *every* new entry an override before its merits were considered, which is the exact freeze he rewrote the prompt to end. Do NOT log an "override" for exceeding it and do NOT let it gate an entry. Treat position-level sizing discipline (conviction-scaled premium, only A++ takes the full $1,500) as the real constraint.
     - **"Max $1500 premium per new position" (live prompt) — CORRECT, and it always was.** This file's old "$1,500 total" reading was the error; the live prompt had it right. Per trade.
     - **Daily realized loss cap: the live prompt says −$200, this file says −$400. Apply the STRICTER −$200** — runs have been doing this correctly and Ryan did not change it in the 2026-08-14 turn, so it stands until he says otherwise. Loosening the sizing rule is not a licence to loosen the loss cap. **Known tension, flagged to Ryan 2026-08-14 and awaiting his decision:** Law 1 allows $1,500 on one position while Law 2 caps daily realized loss at −$200, so a max-size position cut at the preferred −50% realizes −$750 — nearly 4x the cap. **State the mechanics accurately when discussing this: the cap gates NEW ENTRIES only and NEVER delays or blocks an exit.** (An earlier framing of this as "the desk can size into a position it cannot cut" overstated it — the cut always happens; it just halts further entries for the day, which is arguably the correct response to a −$750 day.) So the tension is a *restrictiveness* question, not a contradiction: at −$200 with max-size positions, one real loser ends the trading day. Until Ryan rules, the −$200 stands and no run may raise it.
     - **Duties that exist ONLY in this file, not in the live prompt: the heartbeat stamp and the daily EOD report.** The rewritten prompt's task list (READ STATE → BRIEF → MANAGE → HUNT → IV → LOG + Friday review) omits both. They are still REQUIRED — this is the same drift that once caused `daily_options_report.md` to never be written at all. Do not skip a duty just because the prompt's numbered list lacks it.
     - **Unchanged by the 2026-08-14 turn** (he named only sizing, settled funds, and the equity ban): the −$200 cap, max 3 open agentic positions / 2 per theme, DTE floor 7, the exit engine, the trend-maturity gate, the ownership/exit gate, HARD RULE 9, and the Aug–Oct defensive posture. More ammunition is not a repeal of the gates that decide *whether* a setup is worth taking.
   - **Cadence:** a market-hours scheduled cron checks entries + manages open-option exits **every 15 min** (9:30 AM–4:00 PM ET, Mon–Fri, ~26 runs/day; Ryan's choice 2026-07-01); it is NOT tick-level, so never rely on catching an intraday spike between runs.
   - **🧮 A LOT-COUNT CHANGE INVALIDATES EVERY DERIVED FIELD AT ONCE — repair them as a SET, and ASSERT the invariant rather than eyeballing it (root fix 2026-08-23, after the FOURTH consecutive repair to one position from one change).** Ryan's third QQQ 680P lot filled 2026-08-21T19:37Z, and four separate runs each fixed exactly the one stale field their own incident surfaced: 19:50Z the `contracts` count, 20:19Z the `_exit_ladder` trigger levels, 08-22T12:20Z the `ownership` prose string, 08-23T21:19Z `entry_premium_blended`/`total_cost_basis_usd` — and the 08-23T22:19Z run then found `lots[]` still holding only two of the three lots, summing to qty 2 / $761.00 against a broker-verified 3 / $1,106.00. Each repair was correct and each one wrote a lesson; none of them SWEPT. **When any lot count changes (an add, a partial close, an assignment), rewrite the whole derived set in one pass: `contracts`, `lots[]`, `entry_premium_blended`, `total_cost_basis_usd`, `max_loss`, `_exit_ladder` (every level plus `agentic_lot_only_levels`), `ownership`, and `_current_state.basis_used`.**
     - **The detector, which is the transferable part — assert, don't read.** Before committing any ledger change touching an options position: `sum(l['qty'] for l in lots) == contracts`, `sum(l['cost_usd'] for l in lots) == total_cost_basis_usd == max_loss`, and `total_cost_basis_usd / contracts / 100 == entry_premium_blended`. Three lines of arithmetic catch every variant of this bug, including the ones not listed above; a human reading the record does not, which is precisely why four careful runs missed it in a row. This is the same shape as the `git diff --stat` rule: **trust the check, not the enumerated field list.**
     - **Why these survive so many reconciliations:** a stale field whose CONCLUSION is still right is invisible to a check that only reads the conclusion. `ownership` said "MIXED — ask-first" while understating the position by a contract; `lots[]` gave the agentic share as 1-of-2 (50%) instead of 1-of-3 (33%), which is the number that decides how much of a mixed position the desk may close WITHOUT Ryan under the ownership gate. A reconciliation that stops at "still mixed, still ask-first" passes for the wrong reason — this file's recurring failure mode.
     - **Corollary: the broker is the arbiter.** `get_option_positions` returns `quantity` and `average_price`; any ledger field that disagrees with `quantity × average_price` is wrong by definition, no matter how well-reasoned the prose around it reads.
   - **Ledger notes: overwrite current state, don't append a new paragraph every run (fixed 2026-07-30).** At ~26 runs/day, a per-position `_note` that appends one more prose sentence each check turns into 10+ stacked paragraphs by afternoon (this happened to TE/ACHR/WULF on 2026-07-30) — unreadable, and it buries the one fact that matters (why this position is or isn't gated) under a wall of "unchanged, no action" history. Instead: keep a short static `_note` for the facts that don't change run-to-run (who placed it, why it's gated), and track the moving state (mark, % from entry, last-checked timestamp, status) in a small `_current_state` object that each run **overwrites in place** — same pattern `exit_trigger_pending.last_checked_utc` already used for WULF. Only append new prose when something actually changes (a threshold crosses, a new fact like an earnings date surfaces) — routine "still unchanged" checks update the timestamp/mark and nothing else.
     - **🔑 THE SAME RULE BINDS AT THE TOP-LEVEL KEY, AND THAT IS WHERE IT IS ACTUALLY BEING BROKEN — ONE SESSION-SCOPED KEY PER DAY, NOT ONE PER RUN (root fix 2026-08-25T17:05Z, measured).** The bullet above stopped the per-position `_note` from growing a paragraph every 15 minutes, and runs have obeyed it. The identical failure then migrated one level up: instead of overwriting a shared key, each run **appended a NEW dated top-level key** — `_CORE_SCREEN_2026-08-25`, `_CORE_TRACK_BLOCKED_2026-08-25`, `_TACTICAL_NO_REARM_...15:01Z`, `_EVENT_PREMIUM_MEASURED_...16:19Z`, `_CORE_SCREEN_COMPLETE_...16:22Z`, `_CORE_SIDESTEP_EXISTS_...16:37Z`, `_PREPRINT_COIL_AND_CORE_WINDOW_...16:45Z`, plus two union-merge notes and `_TACTICAL_TRIGGER_WATCH`. **Eleven keys, 25.3 KB, 24% of the entire ledger — all answering the same two questions (is TACTICAL armed? is CORE available?), all expiring at one bell**, against a `positions` array of 3.8 KB. The ledger was 96% commentary and 4% book.
       - **The cost is not disk, it is WRONG ANSWERS, and it was paid twice in that one session.** The 16:22Z run had to correct a five-name claim built on a two-name sample, and the 16:37Z run had to correct a universe-level claim built on that same five-name list. Both corrections were needed *because the state was scattered across nine places*: a later run read one key, could not see that another key had already narrowed it, and restated a conclusion whose scope no longer matched what had been measured. Scattered state is state that gets re-derived wrongly — this file's recurring "a check that passes for the wrong reason", arriving through the ledger's shape rather than through any one run's reasoning.
       - **The pattern to use: `_SESSION_<YYYY-MM-DD>`, created by the first post-open run and DELETED by the first run after the bell.** Inside it, `_current_state` is **overwritten in place** every run (timestamp, tape, book, capital, reconciliation, verdict); the `tactical` and `core` sub-objects gain an entry **only when a run measured something new**; and a `durable_findings` sub-object holds what outlives the day. **Promote `durable_findings` to real top-level keys before deleting the session key** — that promotion step is what keeps the compaction from eating institutional memory, and it is the half a careless cleanup will skip.
       - **A new key is not free just because it is additive.** The instinct that produced these eleven — never overwrite another run's work — is a good instinct aimed at the wrong object. Preserve the *finding*; do not preserve the *container*. If your contribution narrows, corrects, or supersedes an existing key, **edit that key** and say what changed; mint a new one only for a genuinely new subject.
       - **Corollary for the relay: a key dated with a session must carry its own expiry and be honoured.** Every one of the eleven said "expires END OF SESSION" and none was ever removed, because deletion was nobody's job. Make it the first post-bell run's job, explicitly, in the key itself.
   - **`trade_journal.json` is big because the desk THINKS a lot, not because it holds junk — READ ITS TAIL, don't rotate its substance (measured 2026-08-17T07:45Z, correcting the flag left at 07:05Z).** The 07:05Z run flagged the file at ~1.09 MB / 186 entries and proposed rotating "entries older than ~30 days" into `trade_journal_archive_YYYY-MM.json`. **That fix would have recovered nothing and must not be implemented as written**: the entire file spans **2026-08-11 → 2026-08-17**, i.e. under 7 days, so a 30-day cutoff archives zero entries. The same flag's "~112 are no-action logs" is also misleading as a bloat diagnosis — measured, only **9** entries are closed-market/overnight no-ops totalling **0.03 MB (3%)**. The real weight is the **market-hours no-action entries averaging ~6 KB, the largest 16–19 KB** — and those are precisely the `grades_considered` / why-I-skipped records that HARD RULE 8's daily report calls "the most educational section". Archiving them would delete the desk's memory to save disk nobody is paying for.
     - **What to actually do: change the READ, not the file.** No run needs all 186 entries; it needs the last handful plus any weekly review. Read the tail (e.g. `python3 -c "import json; e=json.load(open('trade_journal.json'))['entries']; ..."` slicing `e[-6:]`) instead of paging the whole file into context. The cost the flag correctly identified is **context per run**, and tail-reading fixes that today without touching a byte of history.
     - **When rotation DOES become right:** once the file genuinely spans >30 days, archive by age into `trade_journal_archive_YYYY-MM.json`, keeping every fill, exit, weekly review and flag in the live file regardless of age. Until then it is premature.
     - **Root cause is still upstream and still Ryan's:** the redundant trigger config (cron-job.org every 15 min + the native hourly backstop) fires all night and manufactures entries — 55 of them across the 2026-08-15/16/17 weekend alone. An agent can only make each extra firing cheap; it cannot stop the firing.
     - **The transferable lesson:** a prior instance's flag is evidence, not a verdict. This one named a real cost and attached a fix that the data does not support, and an interactive session implementing it would have logged the problem "solved" while the file kept growing. **Measure before you remediate an inherited flag** — and write the correction down, or the wrong fix becomes doctrine.
   - **✅ The automation runs SERVER-SIDE as a Claude Code Routine (live 2026-07-07).** Routine "Options autopilot" (`trig_01NzocNeZGHw31LmsJdbJ1Jy`) executes on Anthropic-managed cloud with the Robinhood connector attached and NO permission prompts. Triggers: cron-job.org fires its API endpoint every 15 min, 8:00–14:45 CT Mon–Fri (job "Options autopilot fire") + a native hourly schedule as backstop. Full setup/rebuild spec: `docs/options-automation-setup.md`. **Sessions must NOT arm an in-session CronCreate automation anymore** — the old session-scoped cron died silently 3+ times in one day and is retired. **Routine prompt v3.1 (2026-08-11, Ryan's discretionary-trader rewrite + same-day amendments — canonical text in `docs/options-automation-setup.md`):** adds a daily market brief (`market_brief.json`: macro/regime/catalysts/watchlist, built first run of the day; **v3.1: the watchlist draws candidates from the committed equity report `latest_*.md` — the ~220-name comprehensive screen — first, then macro/catalyst work, then the core IV list; report signals are candidates only, all entry gates unchanged**), IV self-tracking (`iv_history.json`: own ATM-IV history + realized-vol ratio/percentile — never cite an IV rank not computed from that file), a written-thesis trade journal (`trade_journal.json`) with a conviction gate (score ≥7/10 or skip) and Friday weekly reviews, max 3 open agentic positions (hedge excluded) / max 2 per correlated theme, a weekly −12% circuit breaker, and debit spreads legged one order at a time (long leg first in, short leg first out — never naked). All existing numbers and gates unchanged: $1,500 budget, −$400 daily cap, DTE floor 7, 2026-08-05 exit engine, trend-maturity gate, ownership/exit gate, hedge exemption. A run-DUTY change needs Ryan to re-paste the routine prompt (agents can't edit it); a RULES change still flows via master.
   - **✅ SUPERSEDED 2026-08-26: ROUTINE PROMPT v6 IS NOW LIVE** (Ryan pasted `docs/routine-prompt-v6.md`; confirmed by scheduler-fired runs recording its line-1 version stamp from 2026-08-26T11:38Z — see the CLOSED note in the tactical-band block below). v6 = v4 dual-track with the 2026-08-24 corrections: tactical band $300–1,000, single-leg-only SCOPE (no spread arming/legging), spec-verticals-for-Ryan from any name, CORE breakeven scrutiny, daily report at 14:15 CT + post-close fallback. The v4 block below is kept as the record of what was live 08-21→08-26. **ROUTINE PROMPT v4 — DUAL-TRACK — WAS LIVE. CONFIRMED 2026-08-21T16:24Z BY DIRECT OBSERVATION OF THE ROUTINE'S OWN STORED PROMPT. v3.1 NO LONGER BINDS.** The 16:19Z automation run was invoked by the scheduler with a stored prompt whose text is the v4 dual-track version — "OPTIONS AUTOMATION RUN — JUDGMENT-FIRST, DUAL-TRACK", the TWO TRACKS section, max 5 open positions (2 tactical / 3 core), max 3 per correlated theme, 2 entries/run and 8/day, the −$400 daily realized loss cap, and the SURVEY ALL STRUCTURES duty — matching `docs/routine-prompt-v4.md` below its `---` divider. **This is the exact evidentiary bar the pending-paste bullet set** ("A session may confirm it is live only by observing the routine's own prompt"), and it is satisfied by observation, not by assertion: the prompt text was delivered by the scheduler into the run's own context. **This is NOT a HARD RULE 9 violation and must not be mistaken for one.** HARD RULE 9 bars an unattended run from *claiming or quoting a Ryan turn*; it does not bar a run from reading the prompt it was invoked with. No Ryan turn is claimed here, no flag is cleared, and no approval is quoted — only the observable fact of which prompt version the routine now stores. The paste itself was Ryan's approval action, taken outside any run.
     - **What is now in force** (superseding the v3.1 numbers wherever they differ): the DUAL-TRACK book — TACTICAL SCALP (hours to ~2 sessions, single leg ~0.40–0.60 delta, ≤3%-of-mid chains, $300–600, +20–40% TP / −30% stop / hard time stop flat by the next session's close, max 2 open) running ALONGSIDE CORE SWING (1–4 weeks, debit spread preferred, 21–45 DTE, ≤10%-of-mid, $500–1,000 typical and $1,500 only for A++, max 3 open). **The tracks do NOT share slots.** Max 5 open (hedge excluded), 3 per correlated theme, 2 entries/run, 8/day. **The −$200/−$400 daily-cap conflict logged 2026-08-14 is RESOLVED: −$400 stands**, because the stricter-of-prompt-vs-CLAUDE.md rule now has both documents agreeing at −$400 for the first time.
     - **UNCHANGED, and a looser prompt repeals none of it:** the ownership/exit gate, HARD RULE 9 in full, the equity autonomy ban (HARD RULE 6), the DTE-7 floor and 0–1 DTE ban, the trend-maturity gate, the 2026-08-05 exit engine, the HARD RULE 7 news/thesis gate, the hedge exemption, the Aug–Oct defensive posture, and **NO MARGIN BORROWING** (deploy against `unleveraged_buying_power`, never `buying_power`, and keep ≥$250 unencumbered). **More frequency is not a lower bar: more trades must come from more opportunities CLEARED, never from grading a marginal setup higher.**
     - **The honest constraint, restated because new limits do not create cash:** on 2026-08-21 at 16:24Z `unleveraged_buying_power` was $1,156.87 against $2,330 already deployed, leaving ~$907 after the $250 reserve. A five-slot book is arithmetically unreachable until existing positions close. Say that plainly rather than implying the desk can suddenly run five positions.
     - **🚫 THE TACTICAL TRACK IS A SPY-ONLY TRACK AT THIS ACCOUNT SIZE — IT NAMES QQQ AS A PRIMARY HUNTING GROUND AND SIZES IT $300–600, AND THOSE TWO RULES CANNOT BOTH BE SATISFIED ON QQQ. MEASURED 2026-08-24T17:03Z, ON A DAY THE TRIGGER ACTUALLY FIRED.** v4 TACTICAL specifies delta 0.40–0.60, chains ≤3% of mid, size $300–600, and says *"in practice this means SPY, QQQ, NVDA and a handful of mega-caps."* Priced at ~0.43–0.49 delta on the 2026-09-04 board (11 DTE, the nearest clearing the DTE-7 floor with room): **SPY 762P $535.50 (0.56% of mid, IV 0.1257) — the ONLY one that fits; QQQ 705P $788.00 (+31% over), QQQ 708P $911.50 (+52%), NVDA 210P $740.00 (+23%).** Every chain was superb (0.56–1.35% of mid), capital was $2,773.56 deployable, all five slots were free and the loss cap sat at a **+$286 credit** — and a live, volume-confirmed failed-bounce trigger on QQQ still could not be traded.
       - **The $300–600 band sits inside THE FOUR LAWS ("no thesis, no reasoning, no exception ever overrides these"), so NO run may relax it, size a $788 tactical, or re-label the trade CORE to escape the ceiling.** The CORE reroute fails on its own terms anyway — CORE needs DTE 21–45, so the only qualifying board (09-25, 32 DTE) prices ~$1,605, i.e. A++ money for an A-minus setup. Refuse it on the merits AND on principle: re-labelling to dodge a limit is a governance failure even when the numbers work.
       - **The trap this creates, and it is the one to actually watch: the only affordable vehicle is usually the wrong one.** On a Nasdaq-specific de-rating (QQQ −0.775% vs SPY −0.216%, SPY above both its MAs with no level breaking), SPY is affordable *because* it is the leg that is holding — low IV and low realized move are the same fact. Buying it because it fits the band is fitting the trade to the constraint, which this file already forbids under *"a cheap, liquid vehicle supplies NO thesis."* **Declining is the correct output; so is saying plainly that the ruleset, not the tape, was binding.**
       - **✅ RESOLVED 2026-08-24 — RYAN WIDENED THE BAND TO $300–1,000. THE SPY-ONLY PROBLEM IS FIXED AND THE BULLETS ABOVE ARE NOW HISTORY, NOT CURRENT POLICY.** Authority: Ryan, REAL live turn 2026-08-24 (genuinely interactive session, typed user turn), responding to the recommendation this bullet used to carry: *"So what could we do this works next time widen the band to 300-1000. It can look at any name for spreads."* He chose the top of the recommended range and rounded it up. **TACTICAL SCALPS ARE NOW SIZED $300–1,000.** The prior recommendation text (widen to ~$300–950) is superseded by his number.
         - **What this unblocks, measured on the day he decided it:** every vehicle that failed the old ceiling now fits — QQQ 705P $788.00 (was +31% over), NVDA 210P $740.00 (was +23% over), QQQ 708P $911.50 (was +52% over). TACTICAL is a genuinely multi-name track again rather than a SPY-only one, which is what the v4 prompt always intended when it named *"SPY, QQQ, NVDA and a handful of mega-caps."*
         - **EVERY OTHER TACTICAL GATE IS UNCHANGED, and a wider band repeals none of them:** delta 0.40–0.60, chains **≤3% of mid** (stricter than CORE's, and never widened to find more scalps), nearest liquid expiry above the **DTE-7 floor**, +20–40% profit / −30% stop, the **hard time stop — flat by the close of the next session**, and **max 2 open**. The $1,500 per-position max still stands above it, and $1,500 remains reserved for A++ CORE only. **More money per scalp is not a lower bar: a marginal setup does not become good because the desk can now afford it.**
         - **📊 MEASURE CONFIRMATION VOLUME AGAINST THE TRAILING 30–60 MINUTES, NOT THE SESSION AVERAGE — the session average is dominated by the opening hour and turns a participation filter into a time-of-day filter (measured 2026-08-25T16:05Z, after the clause vetoed FOUR level-fires in one session).** Own 30-bar QQQ 5-minute series that day: **first-hour mean 254,448/bar vs post-first-hour mean 118,481/bar — a 53% decay** (session mean 172,868; ex-open 161,097). So a clause worded "volume at or above the session average" requires a midday bar to trade **~1.5× the prevailing local rate**, and ~2.1× the first hour's, merely to qualify. After roughly 10:30 ET it can almost never fire whatever the tape does — which is indistinguishable, from the outside, from a filter that is simply broken.
           - **The correction:** compare the candidate bar to the mean of the **trailing 30–60 minutes of the same session**. **Pair it with the price clause staying strict** — a COMPLETED close beyond the level AND holding beyond it on the following bar — because a locally-calibrated threshold is *easier* to clear, and this is a fix to **how confirmation is measured, never a loosening of what qualifies**. A run that adopts the local baseline and quietly relaxes the price clause has used a calibration note to talk itself into midday noise.
           - **The honest half, and the reason this is filed as calibration rather than a missed trade: correcting the baseline would have changed NO decision on the day it was found.** Against the local baseline (123,868) the 15:25Z breakout bar traded 96,113 = **77.6%** and still fails, so that veto stands on both measures. The one bar that DID clear a corrected threshold — the 15:30Z extension at 173,453 = **140% of local rate but only 100.3% of the session average** — produced a complete retracement within three bars. On that day's evidence the corrected filter confirms exactly one break, and the break fails.
           - **⚠️ AND THE CORRECTION THAT COMPLETES IT, MEASURED 2026-08-26T15:52Z: A TRAILING-LOCAL BASELINE INVERTS IN A *DECAYING* TAPE — THE CLAUSE GETS EASIER EXACTLY WHERE IT SHOULD GET HARDER. THE RULE ABOVE IS RIGHT AND STAYS; IT ONLY FIXED ONE END.** The session-average denominator was too STRICT because the opening hour inflated it. Nobody checked the other end: a trailing-local mean *tracks a draining tape downward*, so as participation collapses the bar to clear falls with it. Measured on QQQ's first clean bullish fire of 2026-08-26 — the 15:40–15:44Z bucket traded **98,957 shares and read 149.6% of the settled trailing baseline (66,144), while sitting at 80.1% / 84.8% / 83.9% of the SAME SESSION's own earlier baselines (123,470 at 13:45–14:14, 116,660 at 14:05–14:34, and the 117,990 that morning's 15:00Z fire cleared at 209.6%), and at 39.8% of the actual 14:25Z flush bucket of 248,511.** The identical absolute volume FAILS the clause at 13:45Z and PASSES it at 15:40Z, on a tape where participation fell rather than rose. **Nothing arrived; the denominator halved.**
             - **The mechanism, which is the part to keep:** in a pre-event vacuum the clause stops asking *“did participation arrive”* and starts asking *“is this bar busier than the vacuum”* — which any drift back toward normal satisfies. The filter becomes self-fulfilling precisely when the tape is least informative.
             - **THE PREDICTION THIS REFUTES, WRITTEN FIFTEEN MINUTES EARLIER BY THIS DESK.** The 2026-08-26T15:30Z run measured participation halving to 51% and concluded that *“even a CLEAN price fire this afternoon would fail the confirmation clause on volume”* and that a break arriving WITH volume would be *“far more informative than usual, precisely because the baseline it must clear is real.”* **Both halves are backwards.** A collapsing baseline makes the clause easier, not harder, so a fire into a vacuum is **LESS** informative, not more. A run inheriting that framing reads a vacuum fire as high-conviction — the exact inversion of the truth.
             - **Detector, and it costs one extra line of arithmetic:** report the candidate bar's volume in **ABSOLUTE** terms alongside the ratio, and test it against the session's **own earlier baselines** as well as the trailing-30 mean. Confirmation requires **BOTH** — at or above the trailing local mean **AND** not materially below the session's established bucket rate. A bar clearing the local baseline while sitting under the session's typical bucket is **baseline collapse, not participation**; decline it and say which of the two it was.
             - **Why neither defect is visible from outcomes, which is why both survived so long:** they point in OPPOSITE directions and each *looks* like the filter behaving well. A session-average baseline vetoes everything and reads as discipline; a collapsed local baseline confirms everything and reads as the filter finally working. **Counting fires cannot distinguish either one — only measuring the denominator can.**
             - **Honest scope, same standard the calibration finding above set for itself: this changed no decision on the day it was found.** The fire was declined on five independent merits (magnitude +0.310 = **0.044%**, less than half the 0.106% break declined on 2026-08-25; a level sitting mid-range in a 3.76-point session band that price had oscillated around for two hours; both moving averages overhead and untouched, spot 1.98 under the 50-day; ex-gap IV/RV **1.1697**; and no catalyst answering “why NOW” on the eve of an NVDA print). Follow-through reverted to **103% of baseline within three minutes** with price flat and the 711.18 high given back — reproducing the same session's 15:00Z air-pocket diagnostic exactly. **A filter's inputs can only be audited credibly on a day the audit does not change the answer.**
           - **The transferable lesson: a filter that rejects everything must be audited on its BASELINE, not on its outcomes.** Four vetoes in a row look like discipline and look like a defect from the same distance; the only way to tell them apart is to measure the denominator. Do this before a fifth veto, not after.
         - **📦 THE PRICE CLAUSE GOES STALE EXACTLY LIKE THE VOLUME BASELINE — RE-DERIVE BOTH HALVES OF AN INTRADAY TRIGGER EVERY RUN, OFF THE SAME WINDOW (measured 2026-08-25T17:22Z, the same session as the calibration finding above).** The 17:05Z run wrote its trigger as *"a completed 5-minute close outside the 708.77–710.55 box"* while, **in the same key**, measuring the live 12-bar range as **710.23–711.29**. The written box is exactly 1.78 points wide — it is the *15:01Z* box, carried forward verbatim across two hours while the range migrated ~1.5 points UP and tightened 40% to 1.065 points. Consequence, measured: **nine of the twelve 16:00–16:55Z closes were ALREADY above the written 710.55 upper edge.** The price half of the trigger had been continuously satisfied on the upside for most of an hour.
           - **Why the pair is worse than either defect alone, and why neither is visible from the outcome:** the two stale denominators point in OPPOSITE directions and mask each other. A stale-low box makes the upside clause permanently **already-satisfied**; a session-average volume baseline makes confirmation permanently **unreachable**. Netted, the trigger looks disciplined — it never fires — while sitting one volume spike away from firing a **long into a pre-catalyst drift on a box the desk no longer believed in.** The outcome is "no trade", which is also what correct behaviour looks like.
           - **The rule:** any trigger written with numbers in it must state the **window those numbers came from**, and every run must **re-derive them off the last closed bars before testing it**. Price and volume clauses must be measured over the **SAME window** — a 15:01Z box tested against 17:00Z volume is not one trigger, it is two unrelated assertions. **A run that inherits a numbered clause it did not re-derive has not tested the trigger; it has quoted it.**
           - **The detector, which costs nothing:** compute the live range off the last closed hour and overlay it on the written box. If they do not substantially coincide, the trigger is stale — rewrite it before testing it. That is the same `get_equity_historicals` call the run needs anyway.
           - **READ IT WITH ITS SIBLING, measured independently by a concurrent run the same minute: a TACTICAL trigger must reference a STRUCTURAL level — a moving average, the session high or low, the prior-day close or range edge, a gap edge, a prior swing point — NEVER a range the desk drew around recent bars.** A self-drawn micro-range on a quiet tape gets broken constantly, and a locally-calibrated volume threshold makes it *easier* to clear, not harder. Two fires on desk-drawn boxes this session, two failures (15:30Z cleared 140% of local rate and retraced in three bars; 17:00Z cleared 146% and retraced in one). **The two findings compound:** a self-drawn box is weak, and a self-drawn box carried forward two hours is weak *and* silently already-fired. Applying only one leaves the trigger broken — a structural level quoted from a stale session is just as untested, and a freshly re-derived micro-range is still meaningless. And when a corrected filter's first clean fire fails immediately, **do not re-tune the filter — audit what it is measuring against.** A well-calibrated measurement of a meaningless level is more dangerous than a badly-calibrated one, because it now looks rigorous.
           - **🛑 AND THE CAPSTONE, WHICH CLOSES THIS WHOLE TUNING ARC: BOTH PRESCRIBED FIXES WERE APPLIED IN FULL AND THE BREAK STILL FAILED — SO THE REMAINING VARIABLE IS THE REGIME, NOT THE FILTER AND NOT THE LEVEL. STOP TUNING (measured 2026-08-26T17:19Z).** The 16:37Z SPY break is the first fire of this arc that satisfied *every* correction above simultaneously: the level was **structural** (session low 764.680 ≡ the 20-day SMA 764.7985, 0.12 apart — exactly what the sibling rule demands, not a desk-drawn box), the clause was **freshly re-derived**, and the volume confirmed against **every baseline including the inflated first hour** (160,261 = 200.8% of trailing-30 local, 149.7% of the post-first-hour mean, 102.3% of the first hour itself). Price broke on a completed close, held eight 1-minute closes, and stepped down 764.45 → 763.93. **It then failed completely in 23 minutes.** By 17:00Z SPY had reclaimed the 20-day and printed **four consecutive completed 5-minute closes above it** (764.885, 765.050, 765.110, 765.325), ending at 765.415 — **above** where the break started, and −0.065% on the day against −0.243% at the break.
             - **This is the fifth consecutive failed break in two sessions, and the quality of the setups has been RISING while the outcome stayed identical** (08-25 15:30Z and 17:00Z on desk-drawn boxes; 08-26 15:00Z and 15:40Z on a collapsed baseline; now a structural level confirmed on every measure). Each prior failure was answered by tuning the measurement, and each tuned filter then failed on a better setup. **When successive fixes to the instrument leave the result unchanged, the instrument was not the problem.**
             - **The mechanism: pre-event positioning flow is not directional.** Into a scheduled catalyst, participation genuinely arrives at a level — de-risking is real volume — and then simply stops, because nobody is expressing a *view* before the number. The volume clause is working exactly as designed; it correctly identified a real participation event. A breakout strategy asks "did the level give way", and in an event vacuum the honest answer is "yes, and it means nothing."
             - **THE RULE: do not open TACTICAL breakout scalps into an unresolved scheduled catalyst, however clean the fire.** Volume confirmation cannot rescue a break the regime will not follow through on. Wait for the event to clear — the whole point of the tracks being separate is that the desk can be patient in one while working the other.
             - **📅 SCREEN THE CATALYST WINDOW AGAINST *THIS TRACK'S* HOLD PERIOD, NOT AGAINST THE CALENDAR — AND A FRIDAY TACTICAL ENTRY'S WINDOW IS ~3 CALENDAR DAYS, NOT ~1 (measured 2026-08-28T15:35Z, verified against the earnings calendar).** The capstone above is correct and is being mis-applied. The 2026-08-28T15:20Z run declined a prospective NVDA scalp partly on the grounds that *"DELL prints 09-01 and AVGO 09-02, so a semis-complex scalp is a breakout into an unresolved scheduled catalyst"* — importing, wholesale, an objection that belongs to CORE. **A TACTICAL position's hard time stop forces it flat by the close of the NEXT session**, so a Friday 08-28 entry is out at Monday 08-31's bell. Verified via `get_earnings_calendar` (high_market_cap, 08-29→09-03): Monday 08-31 carries exactly one name, **SAIC (am)** — no tech or semis print at all — while the entire complex clusters **Tue-Wed**: DELL, PANW, CRDO, MDB 09-01 pm; AVGO, SNOW, HPE, NTAP 09-02 pm. Those events are **outside** any TACTICAL window opened Friday and **inside** every 21-45 DTE CORE window. Citing them against a scalp is a category error; citing them against CORE is exactly right.
               - **The honest correction cuts the other way too, and it is the half worth keeping: a Friday-opened TACTICAL position holds through a full weekend.** Its screen window is Friday-close → Monday-close — roughly 72 hours of headline risk against ~24 for a mid-week entry, entirely uncovered by the 15-minute cadence. So the correct screen for a Friday scalp is *wider* than for a Tuesday one, just populated by different events (weekend headlines, Sunday-night futures, continuing conferences) than the Tue-Wed print cluster that was wrongly cited.
               - **The rule: each track screens its own window.** Before invoking the capstone, state the track's hold horizon, state the window's start and end, and name what actually falls inside it. **A catalyst list is not portable between tracks** — and a decline reached by borrowing the other track's objection is this file's most-repeated defect, a check that passes for the wrong reason.
               - **Honest scope: this changed no decision.** NVDA never broke the 223.250 session low that would have fired the watch — it double-tested 223.500 at 15:21/15:22Z and bounced to 224.20 — so there was no trade to take, and the index-agreement clause would have had to be cleared separately with SPY, QQQ and XLF all at session highs. A catalyst screen can only be audited credibly on a run where the audit does not license a trade.
             - **Honest scope, and it cuts the desk's way for once: the 16:45Z run DECLINED this fire on target arithmetic and was right.** Measured at 17:21Z, the vehicle it priced — SPY 2026-09-04 762P, mid 4.845 at 16:51Z — is **mid 4.300, −11.25%**, over a third of the way to its −30% stop 36 minutes after the trigger, with delta drifted −0.4217 → −0.3904 and so no longer even inside the 0.40–0.60 tactical band. It declined for a *stated* reason (the +20% target needed ~95% of the session range) and was vindicated by a *different* one (regime). **Being right for an incomplete reason is still a process win worth auditing — the arithmetic gate caught what the regime gate had not yet been written to catch.**
           - **Honest scope, and the reason it is recorded despite changing nothing:** correcting the box on 2026-08-25 flipped no decision. Re-derived properly, a downside break DID fire (17:00Z close 709.955 below the 710.225 low, held at 17:05Z, on 146%/117% of the trailing-30-minute baseline) and was declined on four merits — 0.106% magnitude, SPY printing one close outside its own box and reversing straight back in, NVDA firming *against* the break into its own print, and a full retrace to the box low within three bars. **An audit of a filter's inputs is only credible when performed on a day it does not change the answer.** Same class as every other entry in this file's "a check that passes for the wrong reason" family: a constant encoded an assumption about a moving thing, nothing errored, and the wrong behaviour was indistinguishable from the right one.
         - **🔬 THE DATA FEED ITSELF CAN BE WRONG — RECONCILE AGGREGATED BARS AGAINST THE 1-MINUTE SERIES BEFORE ANY LEVEL OR VOLUME CLAUSE DECIDES A TRADE (root-caused 2026-08-25T19:00Z; first symptom 18:20Z).** The two bullets above fix *which window* a trigger's numbers come from. This one fixes whether the numbers **inside** that window are real, and both must hold or the clause is untested. Measured on QQQ: `get_equity_historicals(interval='30minute')` silently dropped **26.5% of the session's volume** (3 of 9 windows truncated to 15–36% of true), omitting the bar that printed the session low and causing a run to commit a wrong structural level to master. The obvious remedy — "derive intraday levels from the 5-minute series" — **is itself holed**: the 18:25Z 5-minute bar reported volume 3,358 against a true 42,378 (**−92.1%**), and both its volume *and* its entire OHLC were byte-for-byte the single 18:29Z one-minute bar. A run following the original prescription would have believed it was safe.
           - **The mechanism, and it is why this corrupts PRICE and not merely volume: an aggregated bar is occasionally its LAST constituent minute rather than the aggregate of the five.** A defective bar's "low" is then one minute's low, not the interval's, so it systematically fails to show real extremes — which is exactly how a run derived a session low of 708.88 when the true low was 708.77, an error that took two runs and a push to master to undo. One mechanism, both incidents.
           - **It is a HOLE, not drift, and the two are trivially separable.** Reconciling seven consecutive buckets against the 1-minute series gave −0.3%, +0.5%, +1.2%, **−92.1%**, +3.8%, +0.0%, +4.4%. Six sit inside ±4.4% (ordinary late-reporting drift); one is categorically different. **A single outlier of that size is a dropped bucket, never a quiet tape.**
           - **It biases confirmation toward FALSE POSITIVES — the dangerous direction.** A hole depresses the *denominator* of every trailing-mean the tactical volume clause uses. Measured: the raw trailing-30-minute mean was 64,576 against a corrected 71,079 (**+10.1%**), flattering the candidate bar from 103.4% to 113.8% of baseline. The defect manufactures the very confirmation the filter exists to withhold.
           - **The detector, cheap and total:** before any trailing-volume baseline or structural level decides a trade, sum the constituent **1-minute** bars over the same window and compare. Any bucket off by more than ~5% is a hole — substitute the 1-minute sum. **A 5-minute bar whose volume exactly equals one of its constituent minutes is defective by definition.** Never test a trigger on aggregated bars alone: the 1-minute series is the arbiter and every coarser interval is a convenience. Do not trust an interval to be complete because it is fine-grained; trust the reconciliation.
           - **⚠️ CORRECTION TO THE LINE ABOVE — THE 1-MINUTE SERIES IS *NOT* AN ARBITER. IT HAS THE SAME DEFECT, AND ITS BARS ARE REVISED AFTER THEY CLOSE (measured 2026-08-26T14:31Z).** The rule directly above correctly refused to trust an interval merely for being fine-grained — and then made the finest available interval the arbiter anyway. Measured on QQQ's **2026-08-26T14:11Z bar, one bar, three fetches**: at ~14:26Z it returned **volume 10,006, OHLC 710.630 / 710.760 / 710.521 / 710.521**; at ~14:29Z and again at ~14:31Z it returned **volume 453, OHLC 710.545 / 710.570 / 710.545 / 710.560**. That is a **−95.5% volume revision with a completely different OHLC, on a bar that had been closed for fifteen minutes.** Both readings cannot be right, and the later 453-share, 0.025-point-range bar wedged between neighbours of 20,981 and 29,138 carries the exact single-tick signature this rule teaches you to recognise in coarser intervals. Smaller same-window revisions printed simultaneously on 13:46, 13:57, 14:13, 14:22 and 14:23.
             - **The honest size of it, and why that makes it trustworthy:** it moved the candidate bucket from **104.4% to 105.7%** of its trailing-30-minute mean — above the confirmation line on both, so it **changed no decision on the day it was found**, which is the only condition under which a filter's inputs can be audited credibly. On a borderline reading it flips the answer, and the direction is not predictable: revisions ran both up and down inside one window.
             - **The detector — either defence suffices, both are free.** (a) **Fetch the window twice, a few minutes apart, and compare** before letting a volume clause decide; disagreement means the bars are provisional, not that the tape was quiet. (b) **End the trailing baseline ~10 minutes back from now** rather than at the last printed bar, so the clause is computed only on settled bars. And the recognition rule generalises down one level: **a 1-minute bar whose volume is a tiny fraction of BOTH neighbours is provisional, not a quiet minute** — the same test the rule above applies to 5-minute buckets.
             - **The transferable lesson: there is no arbiter in a live feed — only settled data and a reconciliation.** Trust the agreement between two reads, not the granularity of one. Note the shape, because it is this file's most-repeated defect arriving one level down: a rule written to stop trusting a convenient number replaced it with a *different* convenient number and called that one authoritative.
             - **🔗 THE HOLE IS A FEED-*WINDOW* DEFECT, NOT A PER-INSTRUMENT ONE — SO A SECOND SYMBOL AT THE SAME TIMESTAMP CONFIRMS IT FOR FREE, IN THE SAME CALL (measured 2026-08-27T19:45Z).** The two rules above prescribe reconciling against the 1-minute series (holed itself) and fetching twice a few minutes apart (costs a wait). This is a third, cheaper defence found by pulling four symbols in ONE `get_equity_historicals` call: the **18:45Z 5-minute bucket was holed SIMULTANEOUSLY across three of them** — SPY 9,656 against neighbours of 62,854/136,387 (**−90.3%**), QQQ 7,151 against 61,321/47,423 (**−86.8%**), NVDA 79,352 against 965,453/919,876 (**−91.6%**) — while **XLF printed normally at 44,639**. The tool takes up to 10 symbols per call, so carrying one extra index ETF alongside whatever you are measuring makes the check cost nothing.
               - **READ IT IN ONE DIRECTION ONLY, because XLF was clean inside the identical holed window.** *Agreement* across two or more symbols at one timestamp is strong evidence of a feed hole; **a single clean symbol is NOT evidence the window is clean for the symbol you actually care about.** Reading it permissively would manufacture exactly the false-confidence failure this file has now recorded four times (spot price, ATM strike, wrong expiry board, modelled estimate vs quote).
               - **Corollary for any session-extreme you quote: report it as a FLOOR.** A holed bucket shows one constituent minute's range, so it systematically fails to show real extremes. On 2026-08-27 the 30-minute pull used to establish SPY's and NVDA's session highs contained two buckets with the hole signature (SPY 16:00Z volume 33,626 between neighbours of 603,391 and 274,811), and ten prior runs had quoted NVDA's high as 230.265 against this pull's 230.470. Neither source is authoritative — say "at least 230.47" rather than silently adopting whichever number your own fetch returned.
               - **⚖️ WHEN THE 5-MINUTE BUCKET AND ITS 1-MINUTE SUM DISAGREE, THE *DIRECTION* OF THE DISCREPANCY NAMES WHICH SIDE IS HOLED — AND THE PRESCRIBED DETECTOR ABOVE, APPLIED AS WRITTEN, GETS ONE OF THE TWO CASES BACKWARDS (measured 2026-08-28T14:20Z, both defects live in the same 15-minute window).** The rule above says to sum the constituent 1-minute bars and, on any bucket off by more than ~5%, *substitute the 1-minute sum*. That is right only when the BUCKET is the holed side. Measured on the Warsh-keynote window: **QQQ's 14:05Z 5-minute bucket reported 47,007 against a 1-minute sum of 226,372 (bucket −79.2% LOW) — and its reported volume equals the 14:07 one-minute bar's 47,006 exactly**, the single-constituent signature this file already teaches; substituting the 1-minute sum there is correct. **In the SAME window SPY's 14:05Z bucket reported 406,243 against a 1-minute sum of only 314,755 — the bucket is 29% HIGHER**, because the **1-minute series** is the holed side: SPY's 14:08Z minute printed **7,027** and QQQ's **1,667**, each a tiny fraction of both neighbours, on **two symbols at one timestamp** — the cross-symbol agreement signature from the bullet above, arriving one interval down. Substituting the 1-minute sum for SPY, as written, would have **under-reported that bucket by 22.5%**.
                 - **The rule, and it costs no extra call:** a 5-minute bucket materially **BELOW** its 1-minute sum = the **bucket** is holed → use the 1-minute sum. A 5-minute bucket materially **ABOVE** its 1-minute sum = the **1-minute series** is holed → keep the bucket, and confirm by looking for a minute bar that is a tiny fraction of both neighbours (cross-check a second symbol at the same timestamp — free, same call). Never substitute blind on the size of the gap alone.
                 - **Why it is the dangerous direction, which is why this is a correction and not a footnote:** an under-reported bucket depresses the **denominator** of every trailing-volume baseline, so the error manufactures the confirmation the tactical clause exists to withhold. This is the third distinct way this file has now recorded a denominator being wrong (session-average too strict, trailing-local too easy in a decaying tape, closing-auction too easy in absolute terms) and the first where the *prescribed fix itself* introduces it. **A detector that has only ever seen its failure mode point one way will encode that direction as though it were the mechanism.**
                   - **Confirming instance the same session, and it exercises BOTH one-direction rules at once (measured 2026-08-28T15:17Z).** QQQ's **15:05Z bucket reported 30,649 against a 1-minute sum of 194,971 (−84.3%)**, and the reported figure **equals the 15:09 one-minute bar exactly (30,649)** — the single-constituent signature, bucket-holed, so the 1-minute sum substitutes. That is the **second QQQ hole of this one session** (14:05Z, −79.2%), both at the **:05 offset** — n=2 on one symbol in one day is a thing to watch, not a clock law; yesterday's holes landed at :45 across three symbols. Quantified in the direction that matters: a trailing-30 baseline over 14:45–15:10 reads **181,918 raw vs 209,305 corrected — raw is 13.1% LOW**, again manufacturing confirmation. And **SPY's 15:05Z bucket was CLEAN in the same window** (236,718 vs a 228,283 1-minute sum, +3.7%, inside tolerance), which is the 08-27 cross-symbol rule's one-direction caveat arriving live: agreement across symbols evidences a hole, **a clean neighbour evidences nothing** about the symbol you actually care about.
                 - **Honest scope: this changed no decision on the day it was found.** The 14:20Z run declined both options tracks on regime grounds (an unresolved Fed keynote mid-delivery) before any volume clause was tested, so no baseline was load-bearing. Recorded because a filter's inputs can only be audited credibly on a run the audit does not benefit.
             - **🌊 FOURTH DENOMINATOR MODE, AND IT CLOSES THE SYMMETRY: AN EVENT FLUSH *INFLATES* THE TRAILING WINDOW, SO THE CLAUSE IS TOO **STRICT** FOR ~30 MINUTES AFTER THE EVENT — the exact mirror of the 08-26 decaying-tape case (measured 2026-08-28T14:47Z).** The three modes above are all about a denominator that is wrong; none covers the window immediately *following* a participated flush, which is when a desk is most likely to be testing a break. Measured on SPY's first push to a NEW SESSION HIGH after the Warsh keynote round trip: the 14:40Z bar traded **185,732**, which reads **65.3% of the trailing-30 mean (284,369)** and **68.1% of the session's established bucket rate (272,927)** — a clear double fail — but **97.0% of the session's own PRE-EVENT quiet baseline (191,559, the six 13:30–13:55Z bars)**. The trailing window was carrying the keynote's own flush bars (374k / 425k / 290k / 272k), so it measures the break against event flow rather than against normal participation.
               - **The two readings say genuinely different things, and only one is true.** 65% invites the conclusion *"this breakout is being actively ignored / unparticipated"*; 97% says the honest thing — **participation is ORDINARY, not arriving.** Both decline the trade, but a run that hands forward "unparticipated" overstates its evidence, and the next run inherits a bearish frame the tape never supported. **Report the absolute volume and all three denominators; characterise the break by the PRE-EVENT baseline, which is the only one that answers "arriving relative to normal?"**
               - **The general form, now that all four modes are in: the trailing-local baseline is wrong in BOTH directions, and which way depends solely on whether the recent window holds more or less flow than the prevailing regime.** Post-flush it is inflated (too strict); mid-decay it is drained (too easy). There is no window length that fixes this — only quoting several denominators does. **A single ratio is never a confirmation verdict.**
               - **Honest scope, and it is what makes this creditable: it changed no decision.** All three denominators declined the break, and the 2026-08-26 capstone barred a breakout scalp into the unresolved keynote independently. Feed integrity was checked first per the 08-28 direction rule — SPY's 14:40Z bucket sits **+0.31%** above its 1-minute sum (185,164), inside tolerance on both sides, so no hole propagated into any of these numbers. A filter's inputs can only be audited credibly on a run the audit does not benefit.
               - **🧩 THE FOLLOW-UP, MEASURED 25 MINUTES LATER AT 15:05Z — THAT BREAK WORKED, AND IT EXPOSES A CONSTRUCTION DEFECT: THE PRICE CLAUSE IS TWO-BAR AND THE VOLUME CLAUSE IS ONE-BAR.** SPY never traded back below the 773.56 break and laddered monotonically to **775.26 (+1.70, session high, +0.54% on the day)** across five bars, with QQQ (724.115) and XLF (58.305) also at session highs. This is the FIRST break in the whole arc that FOLLOWED THROUGH, and the clause vetoed it. **The defect is provable from the clause's own wording, independently of that outcome:** the price half requires *"a COMPLETED close beyond the level AND holding beyond it on the following bar"* — a **two-bar** test — while the volume half is applied to the **break bar alone**. Nothing justifies the asymmetry; it is an accident of how the two halves were written at different times. Measured on this break, feed-integrity-checked first (all four buckets within −1.35%/+2.06% of their 1-minute sums, no holes): break bar 14:40Z **185,732 = 97.0% pre-event / 65.3% trailing-30 → FAIL**; the **following** bar 14:45Z — the one the price clause *already makes you wait for* — **225,309 = 117.6% pre-event / 89.1% trailing-30**; the next, 14:50Z, **264,742 = 138.2% pre-event / 120.6% trailing-30 → CONFIRMS on both**, at a close of 774.56, i.e. **1.0 point above the break and 0.7 below where price sits now.** Participation did not fail to arrive; it arrived one bar after the clause stopped looking.
                 - **The fix: evaluate volume over the SAME two-bar window the price clause already uses** — the break bar and its holding bar — and confirm if either clears the pre-event baseline with the other not materially below it. This is not a loosening: the price clause's strictness (a completed close beyond the level, held on the next bar) is unchanged, and the 2026-08-26 capstone's regime bar still overrides everything. It only stops the two halves of one trigger from measuring different windows.
                 - **Do NOT overclaim what one success proves.** The arc now stands at **five failed breaks against one that worked**, so n=1 in the confirming direction is not evidence the threshold is miscalibrated, and the capstone's conclusion — that regime, not the filter, is the dominant variable — is untouched. What this measurement establishes is narrower and firmer: **a structural asymmetry in how the clause is constructed**, which would be a defect even if this break had failed like the other five. Fix the construction; leave the threshold alone.
               - **⏱️ THE LATE-ENTRY DETECTOR, AND IT IS THE PART THAT ACTUALLY DECIDED THIS RUN: PRICE THE −30% STOP IN *UNDERLYING* TERMS BEFORE ENTERING. IF IT SITS AT OR BELOW THE TRIGGER LEVEL, THE ENTRY IS TOO LATE — DECLINE (measured 2026-08-28T15:05Z).** Having found the break was real, the honest next question is not *"was the veto wrong"* but *"is there a trade NOW"* — and those are different questions with different answers. Quoted rather than modelled, per the standing rule: **SPY 2026-09-04 777C — bid 3.52 / ask 3.54, mid 3.53, spread 0.57% of mid, delta 0.4501, gamma 0.0378, theta −0.3234/day (−9.2%/day), OI 1,883, vol 1,401 → $353/contract**, a flawless chain comfortably inside the TACTICAL band. It still fails, on arithmetic: the **+20% target needs ~+2.0 SPY points net of a day's theta → ~777.3**, asking a further extension of an already +0.54% Friday at the session high; while the **−30% stop, after that same theta, is hit on only ~−1.7 points → SPY ~773.6 — which IS the breakout level.** So a perfectly ordinary retest of the break stops the trade out. (The 775C at delta 0.525 / mid 4.595 / $459.50 prices the same way; the broker's own `chance_of_profit_long` reads 0.318 and 0.353.)
                 - **The general form, which converts "don't chase" from a vibe into a computation:** a breakout entry taken late does not merely have a worse price — **it has its stop pushed backwards onto the trigger level itself**, so the position can no longer be wrong about *timing* without being wrong about *thesis*. The edge in a breakout trigger is that the level defines a cheap invalidation; entering far above it spends exactly that edge. **Compute where the stop lands on the underlying before entering, every time — it is two lines of arithmetic and it is a harder gate than the trend-maturity checks, because it produces a number rather than a judgement.**
                 - **Why this pairs with the finding above rather than contradicting it: the clause cost a real trade, and the correct response is still to decline today.** The entry was at 773.9–774.6 and it is gone; the arithmetic above is what "gone" means quantitatively. **A missed entry is recovered by fixing the clause for the next break, never by taking the same trade 1.7 points worse** — that reflex is how a process improvement turns into a chase. Both halves are the same discipline.
                 - **✅ THE DETECTOR WAS TESTED TWELVE MINUTES LATER AND THE *MECHANISM* — NOT MERELY THE ARITHMETIC — HELD (measured 2026-08-28T15:17Z).** The detector's claim was specific and falsifiable: *"a perfectly ordinary retest of the break stops the trade out"*, with the stop projected at **SPY ~773.6**. Measured on the same contract, re-quoted: **SPY 2026-09-04 777C bid 3.12 / ask 3.15, mid 3.135** against the 3.53 quoted at 15:05Z — **−11.19% ($353.00 → $313.50, −$39.50) in 12.5 minutes**, i.e. **37% of the way to the −30% stop**, with delta drifting 0.4501 → 0.4227 (still inside the band) and the chain flawless throughout (0.96% of mid). The underlying did exactly what the bullet said it would: after the 775.292 session high at 15:01Z, SPY sold off to **773.8201 at 15:12Z — 0.22 above the projected stop level**, an unremarkable seven-minute retest of the break.
                   - **Why this is worth a line rather than a shrug: it is the second consecutive declined late entry to print the SAME magnitude.** The 15:05Z run measured its own declined vehicle (the 762P quoted at 14:51Z) at −11.25% thirty-six minutes on; this one measures −11.19% twelve minutes on. Two vehicles, opposite directions, near-identical give-back — consistent with the general form above (a late entry's stop sits on the trigger level, so ordinary noise consumes a third of the risk budget immediately) rather than with either instance being luck.
                   - **DO NOT OVERCLAIM IT, and the distinction is the whole point of recording it.** What was validated is the **stop's proximity**, not the trade's outcome — the +20% target at ~777.3 could still print later in the session, and a stop nearly touched is not a stop hit. **A detector that predicts *where the risk sits* is verifiable inside one run's cadence; a detector that predicts *whether the trade wins* is not, and confusing the two is how a good gate gets retro-fitted into a market call.** The same honesty test this file applies to filter audits applies here: the measurement is creditable precisely because it changed no decision — the entry was already declined, and remains declined, for the arithmetic reason and not for this outcome.
             - **🔔 THE VOLUME-CONFIRMATION CLAUSE IS STRUCTURALLY UNREADABLE IN ROUGHLY THE LAST 20 MINUTES OF A SESSION — closing-auction flow inflates every bar for reasons unrelated to information (measured 2026-08-27T19:45Z, completing the denominator arc).** NVDA's 19:35Z bar traded **1,831,300 — the largest print of the final hour and 158% of the trailing six-bar mean (1,157,506)** — which reads as textbook confirmation of the closing drive. It is MOC/imbalance flow: the same mechanism the 2026-08-26 capstone identified for pre-event positioning, where participation genuinely arrives at a level and then stops because nobody is expressing a view. **The arc is now complete and all three failures look identical from the outside:** the denominator is too STRICT against a session average dominated by the opening hour (08-25), too EASY against a trailing-local mean in a decaying pre-event tape (08-26), and too EASY in absolute terms into the closing auction (here). Only measuring the denominator tells them apart. Practical consequence: **do not open a TACTICAL scalp inside the final ~20 minutes** — the trigger cannot be confirmed, the spread is the day's widest, and the hard time stop then runs across an overnight gap on a position never observed in a normal tape.
         - **📐 COMPUTE BOTH THE 20- AND 50-DAY SMA BEFORE DRAWING ANY INTRADAY BOX — A BOX THAT KEEPS PRODUCING FAILED BREAKS IS USUALLY A MOVING AVERAGE NOBODY MEASURED (found 2026-08-25T17:33Z, after nineteen runs and four failed break-trades).** The sibling rule above already requires a TACTICAL trigger to reference a structural level, and it is **not self-enforcing** — because a desk that has computed ONE moving average believes it has complied. That day the 50-day (713.2256) was measured at 13:50Z and watched all session, while the 20-day (710.3985) — *where price actually spent the day* — was never computed at all. So the desk drew boxes around recent bars and then spent hours calibrating a volume filter on those boxes.
           - **The rule:** before drawing any intraday range, compute the 20- **and** 50-day SMAs off the last closed daily bars and **overlay them on the proposed box**. If an MA sits inside the box, the box is a proxy for that MA — discard the box and use the MA. It is one `get_equity_historicals` call, the same one the run needs anyway.
           - **The detector, which is the transferable half:** repeated whipsaw around a self-drawn level is **not** evidence the filter needs tuning — it is evidence the *level* is wrong. Twice that day the desk answered a failed break by refining the volume baseline and the box edges, which is tuning the measurement of a meaningless level.
           - **A CORRECTLY RE-DERIVED STRUCTURAL LEVEL CAN ITSELF BE MOVING FAST, and its value today says nothing about where it sits tomorrow (measured 19:00Z).** QQQ's 20-day carried a **+1.21/day** slope and a forward path of 712.14 / 714.58 / 715.92 / 717.04 over four sessions **on a completely flat index**, because the 07-28..07-31 crash closes (675.49, 661.73, 683.55, 687.99) were rolling out of the window one per night. A motionless QQQ goes from touching that level to 6.64 points below it inside four sessions. **The failure this prevents:** a run sees price 1.7 points under the average and files it as "nearly reclaimed", when the correct reading is "the gap widened by 1.7 overnight with no selling". Distance to a fast-moving average is a *trend* measurement and its sign can invert the raw reading. **Detector:** whenever a trigger references a moving average, compute what LEAVES the window over the intended hold period, not just today's value. **If the level moves more over the hold period than the trade's own profit target, the level is not tradeable as a trigger — retire it, don't re-derive it.**
         - **🏷️ A REGIME LABEL IS A TIMESTAMPED MEASUREMENT, AND THE FINAL HOUR CAN WITHDRAW IT — MEASURE THE PRIOR SESSION'S LAST HOUR SEPARATELY BEFORE INHERITING ITS LABEL (measured 2026-08-25T19:35Z).** Same staleness class as the bullets above, aimed at a more dangerous object: `market_brief.json`'s regime label is rebuilt only once per day and is inherited ACROSS sessions. That day it was stamped **"RISK-ON, SEMI-LED"** at 13:35Z on AMD +4.36% / SMH +2.24% / NVDA +1.58%. Between 18:45Z and the 19:25Z bar that leadership was withdrawn in **precise reverse beta order** — NVDA −0.656%, SMH −0.307%, AMD −0.210%, QQQ −0.145%, SPY −0.084% — with the hour's heaviest 5-minute print (NVDA 680,668 shares) landing on a DOWN bar. **The label was still literally true at the close** — every name finished green on the day — while the thing it described had reversed.
           - **The failure this prevents:** the next morning's run reads a green tape and a risk-on label, finds them consistent, and carries a bullish frame into a session whose prior tape spent its final hour de-risking — *a check that passes for the wrong reason*, this file's most-repeated defect.
           - **Detector, one historicals call:** before inheriting any regime label, measure the prior session's **final hour** separately from its full-day change and rank the leaders by give-back. A label earned in the morning and unwound in the afternoon is not a label; it is a stale average of two opposite tapes. **Corollary: a full-day percentage change is a poor regime input near an event**, because it weights the positioning being unwound equally with the unwinding.
           - **⏱️ THE SAME DEFECT RUNS AT TEN-MINUTE RESOLUTION *INSIDE* AN EVENT, AND THAT IS WHERE IT REACHES THE NEXT RUN FASTEST — A CROSS-ASSET "TELL" MEASURED MID-SPEECH CAN FULLY INVERT BEFORE THE NEXT RUN FIRES (measured 2026-08-28T14:27Z, during Warsh's Jackson Hole keynote).** The rule above governs labels inherited across SESSIONS. The 15-minute cadence means a label handed forward mid-event is inherited across ~10 MINUTES, and an event tape moves far faster than a daily one. Measured: the 14:20Z run recorded a clean hawkish read — both indices printing their session high in the speech's first minute then sliding monotonically (SPY −0.207%, QQQ −0.330%, NVDA −0.710% off the pre-speech 13:55Z closes) with **XLF flat-to-up**, and named the cross-asset tell explicitly: *"financials firming into a slide is coherent with a higher-for-longer read: rate-sensitive growth de-rates, net-interest-margin names do not."* Ten minutes later **every leg had inverted**: off the 14:13–14:14Z trough, NVDA recovered **+0.848%** (96% of its drawdown), QQQ **+0.459%** (95%), SPY **+0.200%** (69%) — and **XLF was the only one of the four BELOW its pre-speech level**, having spiked to 58.235 on a genuine 261,349-share burst at 14:21Z and immediately faded it. The de-rating growth names led the recovery and the "firming" financials were sold. Not a drift — a clean sign flip on all four legs.
             - **The mechanism, and it is the reason a V-shape is the informative shape:** a complete round trip is the market declining to price the event, which is what an *unresolved* catalyst looks like from the tape. A directional label extracted from the first leg of a round trip is not an early read on the outcome; it is a measurement of the leg that happened to be running when the run fired.
             - **What it costs, because handing a mid-event label forward is otherwise good practice:** the 14:05Z run correctly pinned a pre-speech baseline so the next run could price a reaction against real numbers, and the 14:20Z run correctly used it. The defect is not the baseline — a *level* baseline stays valid. It is attaching a **directional interpretation** to it that the next run inherits as a frame. **Hand forward the numbers; do not hand forward the verdict.**
             - **Detector:** before acting on any inherited intra-event read, re-measure all of its legs — a cross-asset tell is only a tell while every leg still points the same way, and one flipped leg voids it. And treat a **full retrace of the event move** as positive evidence the event is unresolved, which under the 2026-08-26 capstone bars TACTICAL breakout scalps outright.
             - **Honest scope, and it is what makes this creditable rather than hindsight: the 14:20Z run's DISCIPLINE was right even though its LABEL was wrong.** It wrote *"what it does NOT license: nothing… trading a reaction to a text you have not seen, while the speaker is still talking, is the purest form of the 'no reasoning, why NOW unanswered' entry this prompt forbids"* — and stood down. Had it traded its own hawkish read it would have been losing on all three index legs within ten minutes. The measurement changed no decision on the day it was found, which is the only condition under which a filter's inputs can be audited credibly. **A correct stand-down taken for a reason the tape later withdrew is still a correct stand-down — but nobody should mistake the surviving label for the thing that was right.**
             - **➕ AND THE HALF THAT COMPLETES IT, MEASURED INDEPENDENTLY BY A CONCURRENT RUN AT 14:31Z: *BOTH LEGS OF THE ROUND TRIP CARRIED REAL PARTICIPATION*, WHICH IS WHAT SEPARATES "THE MARKET DECLINED TO PRICE THE EVENT" FROM "THE SELLERS SIMPLY STOPPED AND IT DRIFTED BACK."** The bullet above establishes the V-shape; this establishes that the V was *bought*, not merely un-sold. Reconciled against the 1-minute series first (all four recovery buckets clean, +0.04% to +4.1%, so no hole propagated from the 14:05Z defect): SPY's recovery bars traded **425,255 / 290,451 / 272,375 against a 188,285 pre-speech six-bar mean = 1.4–2.3× baseline**, and QQQ's **283,772 / 426,957 / 273,124 against 253,579 = 1.1–1.7×**. The flush ran at 297% of baseline and the recovery ran at 140–230% — **two-sided, fully-participated repricing, not one flow and its absence.** Without this measurement the V is ambiguous; with it, the "unresolved catalyst" reading is positively evidenced rather than inferred.
             - **The mechanism this adds, and it inverts the usual heuristic: CONFLUENCE MEASURED MID-EVENT IS EVIDENCE OF *FLOW*, NOT OF A *VIEW*.** The 14:20Z reading had everything agreeing — heavy participation, a monotonic slide, and a coherent cross-asset story. That is exactly what a positioning swing looks like from **inside** it: a one-directional flush is internally consistent *because it is one flow, not two opinions*. So during an unresolved event, **the moment every leg agrees is disproportionately likely to be the extreme**, and high internal consistency should raise suspicion rather than confidence — the opposite of how confluence reads on a settled tape.
             - **The guard that follows, because the correction invites its own error: a fully-retraced event move is NOT a licence to take the second side.** Fading the recovery on the grounds that the hawkish read "was right first" repeats the 14:20Z error one swing later, on a tape that has now demonstrated it will round-trip a participated move inside thirty minutes. The output of an unresolved catalyst is **stand down**, not **switch sides**.
         - **📈 `iv_history.json` IS A TIME SERIES — DIFF IT BEFORE QUOTING TODAY'S ROW (measured 2026-08-25T18:30Z; eleven days of readings existed and no run had ever diffed them).** Ahead of the 2026-08-26 NVDA print, NVDA's ATM IV on the ~31-DTE board (which spans the print in every reading) sat in a flat band **0.3815–0.3920 across nine sessions (8/11–8/19)**, then rose **monotonically for four**: 0.4032, 0.4042, 0.4093, 0.4108 — **+1.88 vol points (+4.8%)** off the pre-ramp level, *while spot FELL 217.17 → 211.77*. **Vol rising on falling spot across four sessions is event-premium accumulation, not directional repricing.** The IV/RV ratio crossed 1.0 on 8/20 and expanded every session after (raw 1.0286 → 1.1057; ex-gap 1.14 → 1.2459) — the highest readings in the file for NVDA at n=11, which is a stated maximum over the available series and explicitly **not** a percentile (the IV method forbids one under 20 readings).
           - **The discrimination that makes it useful: the ramp was NVDA-SPECIFIC.** Over the same stretch QQQ's ratio went 0.8939 → 0.8995 and SPY's 1.0571 → 1.0409 — flat to slightly lower — and AMD, that day's +4.95% leader, was flat at 0.7105 → 0.7377. There was no market-wide vol bid; one name carried the event. **A mega-cap single-name print is not automatically an index event:** QQQ priced a 5.73% implied NVDA move as a ~0.40% index contribution and showed a 0.10-vol-point kink between its 10- and 31-DTE boards, against NVDA's own **1.84×** front-board kink. Measure the index term structure before assuming event premium is embedded in it.
           - **What it decides, and the general lesson:** long single-name premium bought into a four-session ramp is bought at the richest relative level this desk has on record, into the event whose passing is the mechanical trigger for the crush — and it looks most tempting precisely BECAUSE spot is firming into it. **A file written one row per day is a time series, and the row-at-a-time habit `iv_history.json` invites is what hid a four-session ramp in plain sight.** Diff the series before quoting today's row.
         - **🎟️ `min_ticks` IS A FREE LIQUIDITY PRE-SCREEN — ONE CHEAP CALL, VALID IN ONLY ONE DIRECTION (measured 2026-08-25).** `get_option_chains` returns `min_ticks` per chain **before any instrument lookup or quote**. The day's two worst chains (LMT, TKR) both carried `above_tick` 0.10 / `below_tick` 0.05; TXN and the index ETFs carried the finer 0.05 / 0.01 penny-pilot tier. **A 0.10 minimum tick caps how tight a market can be by construction** — so a coarse tick kills the candidate right there and saves the entire workup.
           - **The qualifier is the important half: a FINE tick is NOT evidence that a chain is tight.** TXN sits in the fine tier and still quoted **14.3% of mid**, while QQQ in the same tier quoted **1.07%**. Never skip the actual monthly-at-target-delta quote because the tick regime looked good — reading this filter in the permissive direction would manufacture exactly the false-confidence failure recorded four times already (spot price, ATM strike, wrong expiry board, modelled estimate vs quote). It matters more since Ryan's 2026-08-24 turn unrestricted spread sourcing to ANY name: a wider universe multiplies the cost of researching untradeable chains, so pushing the liquidity screen one step earlier than the 2026-08-14 SOURCING fix already demands is a saving on every future hunt.
           - **The equity report's RSI2 names are now measured TWICE at roughly zero options-tradeability.** The 2026-08-14 post-mortem found 4 of 5 candidates dying on bid/ask at 12–50% of mid; an independent 2026-08-25 sample found **5 of 5, spanning 12.6–52%** — RTX 12.8–18.0%, TXN 14.3%, LMT 14.1% chain-wide, TKR 38.0%, GD 45.2–52.4%. The report stays the desk's only comprehensive screen and a real CORE source, but **expect to screen and discard the whole list rather than expecting a survivor.** Do not carry those five forward as "blocked pending a catalyst" — they are untradeable for options at this account size at any time, TXN included (it was the one name an earnings-correlation argument would otherwise have cleared).
         - **🎯 THE SCOPE OF A FINDING MUST MATCH THE SCOPE OF WHAT WAS ACTUALLY MEASURED (three occurrences in a single session, 2026-08-25).** A five-name claim was built on a two-name sample (corrected 16:22Z), a universe-level claim was built on that same five-name list (corrected 16:37Z), and the session-key proliferation documented above was the third. In every case the conclusion was **held for the right reason on evidence that did not support it** — and state that is scattered across many keys is state that gets re-derived wrongly.
         - **✅ CLOSED 2026-08-26 — RYAN PASTED PROMPT v6 AND IT IS CONFIRMED LIVE; THE STALE-PROMPT CONFLICT DESCRIBED BELOW NO LONGER EXISTS. STOP REPORTING IT.** Evidence, meeting the same observational bar the v4 confirmation set: scheduler-fired runs from **2026-08-26T11:38Z** onward record being invoked with the v6 text — its line-1 version stamp ("prompt v6, pasted 2026-08-24") appears in their own invocation, exactly the observability the stamp was added to provide — and the 13:52Z run reasoned directly from v6's rules ("nothing in prompt v6 relaxes" the DTE-7 floor). The stored prompt now says $300–1,000 itself, so prompt and CLAUDE.md agree and the stricter-of-two tie-breaker is moot. **Residual lesson (caught 2026-08-26, interactive):** the 13:50Z and 13:52Z runs — already invoked WITH v6 — still wrote "the stored prompt says $300–600 and remains STALE" in their governance notes, because they parroted this file's warning instead of reading the prompt they were fired with. When the invoking prompt carries a version stamp, **the stamp outranks this file's description of the prompt** — report what you were actually invoked with. The paragraph below is HISTORY (2026-08-24→26) kept for the record. ~~**⚠️ THE ROUTINE PROMPT STILL SAYS $300–600 AND THAT WILL SILENTLY REVERT THIS UNLESS RYAN RE-PASTES IT.**~~ The v4 stored prompt carries the old band *inside THE FOUR LAWS*, and this file's own tie-breaker is "where either is stricter, the stricter rule applies" — which would resolve straight back to $300–600 and undo his decision. **Until he re-pastes the routine prompt, this CLAUDE.md amendment governs on the strength of his live turn, which outranks both documents.** A run that finds the conflict must apply $300–1,000 and say the prompt is stale, NOT default to the stricter number. Flagged to him 2026-08-24; agents cannot edit the stored prompt. **The paste-ready corrected prompt is `docs/routine-prompt-v6.md` (written 2026-08-24)** — it fixes this band, strips the unexecutable spread-legging authorization from SCOPE, rebuilds the CORE track around single-leg-with-breakeven-scrutiny plus the spec-verticals-for-Ryan flow, and stamps a version line so future runs can see which prompt fired. One paste closes every stale-prompt conflict in this block at once.
       - **🌐 SPREAD SOURCING IS NOW UNRESTRICTED BY NAME — "It can look at any name for spreads" (Ryan, same live turn 2026-08-24).** The debit-spread hunt is no longer confined to the core IV list or the equity report's universe: **survey ANY name for a vertical.** This is coherent precisely *because* spreads cannot be placed through the agentic API (see the multi-leg block above) — the desk specs them and **Ryan places them in the app**, so the universe never needed to be limited to what the desk itself can execute.
         - **THE LIQUIDITY GATE IS NOT WIDENED — this is a change to WHICH NAMES, not to WHAT QUALIFIES.** CORE still requires **≤10% of mid and real OI**, quoted on the **MONTHLY** board at the **target delta** (the three-instance measurement bug above). Widening the name universe without holding that gate would re-create the exact failure the 2026-08-14 SOURCING fix was written to end, where four of five candidates died on bid/ask at 12–50% of mid. More names to look at, same bar to clear.
         - **Screen liquidity EARLY and price the vertical BEFORE the thesis workup.** A wider universe multiplies the cost of researching untradeable chains, so the 30-second spread check comes first — that ordering matters more now, not less.
         - **Deliverable shape for any name that clears:** both legs, net debit, max profit, breakevens, the invalidation level on the underlying, and the intended hold period — handed to Ryan complete enough to place without re-deriving anything. Never quietly substitute a single leg and call it the same trade (the MSFT 500C breakeven lesson above).
       - **⚠️ AND THE FOURTH INSTANCE OF THIS FILE'S OLDEST BUG, COMMITTED WHILE WRITING THIS VERY BULLET: THE RUN *ESTIMATED* THE PRICES BEFORE QUOTING THEM AND GOT THE ORDER EXACTLY INVERTED.** It predicted SPY dearest (~$690, highest spot $764) and NVDA cheapest (~$595, lowest spot $210). Measured, **SPY is the cheapest at $535.50 and NVDA the dearest at $740** — because premium scales with **spot × IV × √time** and SPY's IV (0.1257) is a quarter of NVDA's (0.5091). That is the exact mechanism the affordability-tiers block already documents, re-derived wrongly from spot. Prior occurrences: spot price (08-14), ATM strike (08-17), wrong expiry board (08-24 morning). **A modelled estimate is never a substitute for a quote when the number decides a trade — the estimate cost nothing here only because it was measured before acting.**
     - **Authority:** Ryan, live turns 2026-08-21 — *"I do not want to actually use margin"*; *"increase the amount you are allowed to trade and frequency… I want more daily swings scalping profits"*; *"look for stronger longer swing plays at the same time"*; *"we have more available options tools like spreads and more so we need to also be looking at all potential plays."*
     - **What v4 changes:** a **DUAL-TRACK book** — TACTICAL SCALP (hours to ~2 sessions, single leg ~0.40–0.60 delta, ≤3%-of-mid chains only, $300–600, +20–40% TP / −30% stop / **hard time stop flat by the next session's close**, max 2 open) run ALONGSIDE CORE SWING (1–4 weeks, debit spread preferred, 21–45 DTE, $500–1,000 typical and $1,500 only for A++, max 3 open). **The tracks do NOT share slots** — that separation exists so a busy scalping day cannot crowd out the longer swing book, which is exactly what Ryan asked for. Limits move to **max 5 open positions** (hedge excluded), **3 per correlated theme**, **2 entries/run**, **8 entries/day**. Daily realized loss cap settles at **−$400** (the −$200/−$400 conflict logged 2026-08-14 is resolved by the paste). Adds a **SURVEY ALL STRUCTURES** duty — price the single legs AND the vertical for any thesis worth taking and write down why the chosen structure fits the intended hold period. Adds a Friday review split BY TRACK.
     - **What v4 does NOT change:** the ownership/exit gate, HARD RULE 9, the equity autonomy ban, the DTE-7 floor and 0–1 DTE ban, the trend-maturity gate, the 2026-08-05 exit engine, the news/thesis gate, the hedge exemption, and **no margin borrowing**. More frequency is not a lower bar: **more trades must come from more opportunities CLEARED, never from grading a marginal setup higher.**
     - **The honest constraint nobody should forget when v4 goes live: new limits do not create cash.** Deployment is still capped by `unleveraged_buying_power` minus a **$250 reserve**. On 2026-08-21 that was $1,942.95 − $250 ≈ $1,693 against $1,544 already deployed, i.e. roughly **$150 of genuine headroom**. A five-slot book is unreachable until existing positions are closed. Say this plainly rather than implying the desk can suddenly run five positions.
   - **🔔 PER-POSITION MONITORING MANDATE — Ryan wants to be BROUGHT the exit decision, not handed the result (set 2026-08-18, live turn: "can you monitor these positions over the course of them and let me know if you think i should sell for profit or if they are losing i should get out").** This is a standing posture for the positions listed in `holdings.json._monitoring_mandate.applies_to`, and it is the shape to reuse whenever Ryan opens a trade with an agent in an interactive session and then hands it to the automation to watch.
     - **Posture: MONITOR + RECOMMEND + ASK.** Ryan makes the call. The desk's job is to bring him a decision *at the right moment with the numbers attached*. Discretionary exits — pop-banking a winner, a ratchet give-back, a setup break, a pre-earnings close — are **recommended and wait for his go-ahead**, not executed. This resolves the ownership ambiguity for agent-placed-but-human-chosen positions: `placed_agent='agentic'` formally grants mechanical exit autonomy, but a position **Ryan chose in a live session** is substantively the `user` case the ownership gate was written to protect (the 2026-07-21 RBRK incident). Treat it as ask-first.
     - **THE ONE EXCEPTION — hard backstops still EXECUTE without waiting** (−65% at 21–45 DTE, −70% above 45 DTE, and the −50% cut once inside 21 DTE). They exist precisely so a true loser cannot ride toward zero during the hours or days it may take Ryan to see a message and reply. Notify immediately *after* one fires, with the reason. **A 15-minute ask-first loop with no floor is how a −50% becomes a −90%** — that is the whole argument for keeping this exception, and any run tempted to suspend it should re-read that sentence.
     - **SILENCE IS PART OF THE MANDATE.** At ~26 runs/day, an un-deduped watch turns into 26 notifications a day and Ryan stops reading them — which destroys the mandate's entire purpose. Each monitored position carries a `_notify_state` object that every run **overwrites in place**. Notify ONLY when a trigger **fires or un-fires**, or when the recommendation itself changes; if the same trigger is still live and nothing material moved, update the mark and timestamp and **stay silent**. A position quietly working is not news — the journal and the EOD report already carry it. This is the same "notify once per incident" discipline HARD RULE 9 enforces, aimed at trade management.
     - **A recommendation is never a bare "consider selling."** It carries: the trigger that fired and its measured level; current mark, bid/ask, and **spread as % of mid** (this determines what an exit actually *nets* — quote the net, not the mark, on any wide chain); P/L in $ and %; DTE and theta burn per day; the underlying versus its written invalidation; the concrete alternatives with trade-offs; and a plain-language **recommendation with its reason**. Ryan asked for this to *learn how the decision is made* — show the reasoning, not just the verdict.
     - **🕘 NEVER price a recommendation off the opening auction print — re-quote before you send (incident 2026-08-21T13:32Z).** The QQQ 680P's 21-DTE decision package was built on the 13:30:08Z tick: mark 3.96, **2 contracts of volume**, 5.05% of mid. It reported the position as **−$9 / −$19 net** and told Ryan the whole +$84 overnight gain had evaporated. Six minutes later the settled market was 4.23 × 4.32 (2.1% of mid) and the position was **+$22.50 / +$18 net**. *The sign was wrong* on the very number he was being asked to decide from, and a correction had to be pushed at 13:39Z. This file already warns that **opening prints inflate ATM IV via wide auction markets** — nobody had applied the same caution to a **position mark**, which is the identical artifact. Rule: on any run that fires at or before the open, take the first reading if you like, then **re-quote at least ~5 minutes after 13:30Z and send the settled numbers**; if a ladder level appears to fire inside that window, confirm it on a second quote before acting or alerting. A run that has been waiting through eighteen pre-open stand-downs is exactly the one tempted to grab the first live quote it can get — that impatience is the bug. Sending the correction promptly was right; needing it was not.
     - **State the honest limit rather than implying coverage the system lacks:** this is a 15-minute scheduled cadence during market hours only. An intraday spike can round-trip entirely between two runs, and overnight/weekend gaps are not covered at all. Never promise continuous monitoring.
     - **The per-position trigger levels live in `holdings.json` under `_exit_ladder`** — pre-computed profit, loss, setup-break, and time/deadline levels in absolute premium terms so no run has to re-derive them and every run recommends off the same numbers. Recompute the ladder when the DTE tier changes (a contract crossing below 21 DTE moves from re-check to a standing −50% cut).
   - **📋 DAILY EOD REPORT TO RYAN (added 2026-08-07, Ryan: "I just want to learn and be aware").** On the FINAL run of each trading day — **the first run at or after ~14:15 CT (19:15Z); write it and do not wait for a "better" slot** (threshold moved from 14:25 CT on 2026-08-21, see below) — compose and deliver a plain-language daily report of the options sleeve.
     - **⏰ WHY 14:15 AND NOT 14:25 — the old threshold silently stopped working when the sub-hourly trigger died, and the report has been landing POST-CLOSE ever since (root-caused 2026-08-21T16:35Z).** The 14:25 CT threshold was calibrated in the era of ~15-minute firing, when some run always landed inside the 14:25–15:00 CT window. Since the cron-job.org trigger died on 2026-08-18 the routine fires **hourly at :18–:26 past the hour**, so the last market-hours run of the day lands at **19:21–19:24Z — consistently three to four minutes SHORT of the 19:25Z gate.** The gate is therefore never satisfied before the bell, and the duty falls to a post-close run every single day. Measured: the 2026-08-20 report was written by the **20:20Z post-close run**, which says so in its own header ("The 19:21Z run landed four minutes before the 14:25 CT report window"); the 2026-08-19 report was not written until **04:19Z on 08-20**, i.e. ~11:19 PM ET, roughly seven hours after the bell.
     - **The fix and the principle behind it:** 19:15Z is comfortably below the observed 19:21–19:24Z landing zone, so the last pre-bell run qualifies again. **Belt and braces, because no fixed threshold survives an unknown cadence: if the bell rings and no report has been written for that trading day, the FIRST post-close run writes it immediately — no waiting, no deferring to a tidier slot.** That clause is what actually makes the duty cadence-independent, and it is the part to keep if the numbers ever drift again.
     - **The transferable lesson, which is the same one the heartbeat taught two bullets up:** a rule calibrated against an assumed firing rate becomes silently wrong the moment that rate changes, and nothing errors — the report still got written, just hours late and after the market it described had closed. **When a scheduling constant encodes an assumption about cadence, write the fallback that does not depend on the cadence.** Both failures this week were the same shape: a check that passes for the wrong reason.
     - **Why 14:25 and not 14:30 (root fix, 2026-08-13):** firing times are irregular and collide (that day's late runs landed at 19:00Z, 19:15Z and 19:25Z, i.e. 14:00/14:15/**14:25** CT), so a run that defers on the grounds that "a 14:30 CT run will do it" can easily be the last run of the day — leaving Ryan with nothing and tripping the 20:45 UTC dead-man alarm. **Missing the delivery is strictly worse than writing it a few minutes early.** Write it, and note in the journal that you did.
     - **Exactly ONE run per day writes this file.** A later run must **NOT** rewrite `daily_options_report.md` merely to refresh closing marks or restate "still no trades" — every master push touching that file sends Ryan another notification, and a duplicate carrying the same content is pure noise. Rewrite it only if something **MATERIAL** happened after the report was written: a fill, an exit, a new flag, or a trigger firing. Purpose is EDUCATION as much as record-keeping — write it so Ryan learns how the decisions were made, not just what they were. Contents, in order:
     1. **Positions** — each open contract: entry, current mark, P/L $ and %, DTE, and a one-line "why we own it" thesis refresher.
     2. **Actions taken today** — every entry/exit with the FULL reasoning: which signal fired, what the news/thesis gate found, the IV/liquidity numbers that passed, and what rule sized it. If an exit: which trigger fired and what the alternatives were.
     3. **Actions considered and SKIPPED** — the candidates screened and the specific reason each failed (cost vs cap, liquidity, IV, earnings window, honesty test). This is the most educational section — keep the detail.
     4. **Sleeve state** — total premium at risk and settled cash remaining (there is no total-sleeve cap since the 2026-08-14 amendment; $1,500 is per trade), realized P/L today vs the daily cap, any position approaching an exit trigger or deadline (DTE, earnings, backstop level).
     5. **Tomorrow's watchpoints** — known deadlines, earnings dates, levels that would trigger action.
     Delivery (**FIXED 2026-08-11 — do NOT curl ntfy from the session**): commit the full report to `daily_options_report.md` on master (overwrite daily — one file, always the latest day) and **that commit IS the delivery** — the `eod-report-notify.yml` GitHub Action fires on any master push touching that file and sends the report to Ryan (ntfy push; the email copy to rbartlett2278@gmail.com goes out ONLY once an `NTFY_TOKEN` repo secret is added — ntfy.sh rejects the `Email` header on anonymous publishes, error 40053, found 2026-08-11; until Ryan creates an ntfy account token and adds that secret, delivery is push-only), with a 20:45 UTC Mon–Fri dead-man backstop that alarms him if no report landed that day. The Action degrades through tiers (full report → compact) and prints ntfy's error body on failure — check its run logs, not just the conclusion, when delivery is in doubt. Root cause of the change: the managed Claude cloud environments (routine and interactive alike) CANNOT reach ntfy.sh (proxy 403), so the old "push a compact version via ntfy from the run" step failed silently on 2026-08-11 and Ryan got nothing — only the GitHub runner can reach ntfy. A session-side curl to ntfy.sh will always fail; don't attempt it, and don't count a routine push-notification as delivered (it also failed to reach him). On days with zero actions and zero skips (e.g. market holiday), a one-line "no activity" report file suffices. This replaces nothing else — heartbeat, exits, and entry duties are unchanged.
   - **🚨 THE 15-MINUTE TRIGGER HAS BEEN DEAD SINCE 2026-08-18, AND THE HEARTBEAT CANNOT DETECT THIS — a partial outage is invisible to a dead-man that only asks "did ANY run happen today?" (found 2026-08-21T16:30Z, on Ryan's live report: "i'm only seeing the hourly scheduled runs when there should also be API runs every 15 minutes").** He is right, and the journal timestamps prove it precisely. **Last sub-hourly fire: 2026-08-18T19:46Z (14:46 CT)** — the tail of that day's configured 8:25–14:55 CT window, i.e. the job finished its normal Tuesday and then never fired again. Market-hours runs (13:30–20:00Z) per trading day: **8/17 = 33, 8/18 = 31, then 8/19 = 7, 8/20 = 7, 8/21 = 4.** Every run since is the routine's NATIVE HOURLY backstop, landing at :18–:26 — a flat 24/day, which is exactly 24 hours × 1. A ~78% collapse in market-hours cadence.
     - **WHY THE DEAD-MAN WENT GREEN THROUGH ALL OF IT, which is the transferable failure:** `automation_heartbeat.json` is stamped by the FIRST run of each trading day and the 20:45 UTC alarm only fires if NO report landed. The hourly backstop kept firing, so it kept stamping, so the heartbeat read healthy for three trading days while three quarters of the intended coverage was missing. **A dead-man that tests liveness cannot detect degradation.** Any future health check must assert a RATE, not an existence — e.g. the first post-open run counting the prior trading day's market-hours journal entries and alarming below ~20.
     - **What it cost, and it is not abstract:** Ryan set the per-position MONITORING MANDATE on **2026-08-18** — the very day the trigger died. The monitoring he asked for has run at ~1/hour, a quarter of its intended cadence, ever since. Exit enforcement (the −50% cuts and the unconditional hard backstops) has been checking ~7x/day instead of ~31x. **Corollary that binds until it is fixed: the DTE-7 floor and the 0–1 DTE ban exist BECAUSE a 15-minute cadence cannot manage expiry-day gamma. At 60 minutes that blind spot is four times wider, so treat short-DTE contracts with MORE caution, not less, and say plainly that "15-minute cadence" is currently false — it is hourly.**
     - **✅ CAUSE CONFIRMED 2026-08-21T16:40Z: THE BEARER TOKEN. Ryan ran a manual test-run of the cron-job.org job and it returned `401 Unauthorized` — "the endpoint requires authentication."** That is cause (1) below, and it matches `docs/options-automation-setup.md` exactly: "401/403 = token problem (regenerate in the routine editor, update cron-job.org)." **401 is authentication specifically, so the beta header is NOT implicated** — a stale `anthropic-beta` would fail differently. Fix, in order: (a) regenerate the API trigger's token at claude.ai/code/routines → Options autopilot → Triggers → API (**shown once** — copy before closing); (b) update the `Authorization: Bearer <TOKEN>` header on the cron-job.org job, leaving the other three headers untouched; (c) **verify the job is still ENABLED — cron-job.org auto-disables after repeated failures, and this one has ~40+ consecutive failures (≈14 fires/day × 3 trading days), so a new token alone may not resume it, and a successful manual test does NOT prove the schedule is on**; (d) re-test, expecting HTTP 200 with a JSON body carrying a new session id/URL; (e) confirm recovery by watching for runs landing at :25/:55 rather than only the hourly :2x. **Unexplained and worth noting if it recurs:** nothing identifies WHY the token died on 08-18. It was not the v4 prompt paste, which happened 08-21, three days later. A fixed token lifetime or a routine-settings edit rotating the credential both fit; if it dies again on a similar interval, that timing is the clue.
     - **The original three-candidate diagnosis, kept because the reasoning is reusable:** a clean stop at a day boundary rather than a gradual degradation pointed at (1) an expired/revoked bearer token, (2) cron-job.org auto-disabling the job after repeated failures, or (3) a stale `anthropic-beta: experimental-cc-routine-2026-04-01` header after a platform change. The job's execution history distinguishes all three by HTTP status in one look — which is exactly how it was settled.
     - **An agent cannot repair this from inside a run** and must not pretend otherwise: it can only measure the cadence, report it, and keep working at whatever rate it is actually given. **Do NOT arm an in-session CronCreate automation to compensate** — that is the retired pattern that died silently three times in one day and would put two automations on the same book.
     - **✅ RESOLVED 2026-08-21T17:05Z — BOTH HALVES FIXED, VERIFIED AGAINST THE CLOCK.** Ryan regenerated the API trigger token; his manual test-run returned HTTP 200 and (worth knowing) **actually fired a real run** — the test-run is not a dry run. That proved the TOKEN. It did **not** prove the SCHEDULE, because a manual test and a scheduled fire post the identical configured payload, so only the timestamp can distinguish them, and the test landed at 16:45Z which matches neither configured slot (`25,55 8-14 * * 1-5` CT = :25/:55). **The proof came from the clock: a run fired at the 16:55Z slot and logged at 17:01Z** — impossible for the hourly backstop (:18–:26), not a live user turn, and a ~6-minute invoke-to-log gap matching the 16:45Z→16:52Z run. Cadence restored to ~:25/:55 plus the hourly backstop.
     - **⚠️ CADENCE MATH MUST FILTER BY `run_type` — three runs in a row miscounted it on 2026-08-21, and the "assert a RATE" health check recommended above is worthless without this.** A single interactive session injected ~6 non-scheduler entries into `trade_journal.json` in one afternoon (`live_user_query_midrun`, `root_cause_fix`, `live_user_followup_midrun`). The 16:52Z run correctly warned these "must not be miscounted as cadence"; the very next run (17:01Z) then cited 16:36Z and 16:40Z — both live-user turns — as evidence the trigger was firing. It reached the right answer on wrong evidence, which is the failure mode this file keeps naming: **a check that passes for the wrong reason.** Worse, that same 17:01Z entry omitted `run_type` entirely, so it cannot be filtered at all. Rules: **(1) every journal entry carries `run_type`, no exceptions** — a missing one is a defect to repair, not to skip past; **(2) any cadence, health or rate computation counts ONLY scheduler-fired types** (`market_hours_regular`, `scheduled_automation_run`, `first_post_open_of_trading_day`, `post_close_overnight`) and NEVER live-turn types; **(3) an entry count is not a fire count.** A busy interactive session makes a dead trigger look healthy — exactly inverting the signal the dead-man is supposed to give.
   - **Heartbeat dead-man protocol:** the routine's first run each trading day pushes Ryan a notification and commits a date-stamped `automation_heartbeat.json` to `master`. **No morning push / stale heartbeat on a trading day = the automation is down** → check cron-job.org execution history + the routine's run list at claude.ai/code/routines. An interactive session should then manage options manually IN-SESSION (exits first) and tell Ryan — not spawn a competing cron.
   - **"Today" means the US TRADING day (ET), never the UTC calendar date — do NOT roll the daily artifacts on a post-close run that has crossed midnight UTC (found 2026-08-14T00:45Z).** The redundant triggers keep firing after the bell, and once the clock passes 00:00 UTC a Thursday-evening post-close run (19:45 ET Thu = 00:45Z Fri) becomes, by UTC date, the "first run of 2026-08-14". Three per-day artifacts key off "today" and every one of them would be corrupted by that reading:
     - `automation_heartbeat.json` — stamping it with the new UTC date from a post-close run **pre-satisfies the dead-man for a trading day that has not started**. If the cron then dies overnight, Friday looks healthy all day and the one alarm designed to catch a dead automation never fires. **Only a run at/after 09:30 ET on that trading day may stamp the heartbeat.**
     - `market_brief.json` — rebuilding it at 00:45Z would date it "today" off overnight/extended-hours prints, and the real pre-open and first post-open runs would then see it stamped and skip the rebuild, trading Friday off Thursday-night quotes. **Leave the brief stamped with the trading day it describes; the first run of the next session rebuilds it.**
     - the `-$400` / `-$200` realized daily loss cap and the "max 3 new entries per day" throttle — both reset on the TRADING day. A UTC rollover does not hand the desk a fresh cap or a fresh entry budget.
       - **⚠️ THIS IS THE CLAUSE THAT ACTUALLY GETS BROKEN, AND IT BREAKS SILENTLY INSIDE A PASSING RECONCILIATION — FOUR CONSECUTIVE RUNS REPORTED RESET VALUES THE LEDGER CONTRADICTED (measured 2026-08-27T09:18Z).** The 05:20Z, 06:32Z, 07:25Z and 08:20Z post-close runs of trading day 2026-08-26 each wrote *"Realized options P/L for trading day 2026-08-26 remains **$0.00** against the -$400 cap. Options entry throttle **0 of 8** used."* Both figures are wrong. The broker (`get_realized_pnl`, options, 08-26..08-26) returns **−$16.00 across 3 closing trades**, and `holdings.json._realized_options_pnl_today` — the file those same runs said they had reconciled against "one-for-one" — carries **−$16.00 and "3 of 8 entries used"**, broker-confirmed at 21:20Z with full provenance. **The ledger was right the whole time; the narrative was written from a default.**
       - **The mechanism, and it is why the heartbeat clause survives while this one does not: the two artifacts above are FILES a run must decide whether to WRITE, so declining to roll them is a visible, deliberate act. The cap and the throttle are NUMBERS a run merely RESTATES, so "rolling" them looks like nothing at all.** Every one of those runs correctly declined to stamp the heartbeat and correctly declined to rebuild the brief — then applied the reset it had just refused, to the two artifacts the same bullet names, by typing a zero. A half-applied rule reads exactly like a fully-applied one.
       - **Why it passed review four times: the reconciliation genuinely succeeded on POSITIONS, and lent its credibility to two adjacent numbers nobody checked.** SPY 1 contract and PNC 1.825758 shares did match the broker, so "ZERO DRIFT, reconciled one-for-one" was true — of the positions. The cap and throttle sat in the same paragraph and were never sourced. This file's most-repeated defect, arriving one field to the left of where the check was pointed.
       - **Detector, and it is the part to keep: NEVER type a cap or throttle figure you did not READ this run.** Read `_realized_options_pnl_today.realized_usd` and `.entry_throttle` out of `holdings.json`, or pull `get_realized_pnl` — the broker is the ledger of record for P&L, per the rule already in this file. **On any post-close/pre-open run the still-in-force trading day is the one whose bell just rang, so a RESET value is wrong BY CONSTRUCTION — if you find yourself writing "0 of N used" about a day that has already traded, that is the bug.** The reset happens at the next open, not at the UTC rollover, and not because the desk is standing down.
       - **Immaterial here, load-bearing in general — and the dollar figure is the LESS important half.** −$16 against −$400 gates nothing, and no post-close run can trade anyway. **The throttle is the real gate:** a run that believes it holds a fresh 8-entry budget when 3 are already spent is one trading-day-boundary miscount away from authorizing entries against a budget it does not have. The failure would surface as an over-limit book, not as an error.
     Rule: on any run between the 16:00 ET bell and the next 09:30 ET open, the trading day is **still the day whose bell just rang**. Reconcile, log, and stand down — do not roll any daily artifact forward. (This run correctly declined to stamp the heartbeat or rebuild the brief; it is written down so the next instance does not have to re-derive it at 00:45Z.)
   - **POST-CLOSE RUN PROTOCOL — the Nth redundant overnight run is a stand-down, not a research assignment (set 2026-08-14T01:24Z, from the SIXTH post-close run of the 2026-08-13 trading day).** The native hourly backstop keeps firing long after the 16:00 ET bell: 2026-08-13 produced post-close runs at 20:28Z, 21:30Z, 22:33Z, 23:29Z, 00:45Z **and** 01:24Z. The first one or two add real value — final reconciliation, the extended-hours read-through on an after-the-bell earnings print, pre-computing a due weekly review from broker data. After that the book is provably unchanged and each further run is spending tokens re-deriving the same four numbers. So on any post-close run: (1) read the journal for what the earlier post-close runs of THIS trading day already covered; (2) do the cheap reconciliation (`get_accounts` / `get_option_positions` / `get_portfolio`) — the one check always worth repeating, since it is the only way an overnight assignment, expiration or unauthorized fill would surface; (3) if nothing changed and every useful post-close task is already done, write ONE short journal entry saying so and stop. Do NOT re-poll extended-hours quotes an earlier run already measured, do NOT re-derive the regime, do NOT roll any daily artifact (see the UTC rule above), and do NOT invent a research task to justify the run. A no-op logged in a few lines is the correct output. The redundancy itself can only be fixed in the trigger config (cron-job.org / the routine's native hourly schedule), which is Ryan's to change — an agent's job is to make each extra firing cost almost nothing.
   - **Branch-vs-master drift check, every run (incident 2026-08-07: ~20 runs from 13:31Z–19:19Z all landed on one reused feature branch `claude/jolly-lovelace-kr427z` with zero PRs opened — `master` sat frozen on the prior day's 19:46Z state the whole time, invisible to `report.py`'s cron and to any interactive session checking master).** Every prior day's runs each opened their own PR straight to `master` and merged it within seconds ("Merging directly — standing instruction for the scheduled automation" — see PRs #78-87); that pattern is the contract. Some invocation paths for this scheduled task reuse the SAME branch across many runs instead of minting a fresh one, and on a reused branch a run can silently just push-without-PR run after run if nothing prompts it to check. So: **before ending any options-automation run, diff `HEAD` against `origin/master`** (e.g. `git log origin/master..HEAD --oneline`); if commits have piled up unmerged, open a PR to `master` and merge it directly (same standing-instruction precedent as every ledger/report PR above) rather than leaving it for "next time" — there may not be a next time on a fresh branch to catch it.
   - **CONCURRENT-RUN COLLISION — check for a sibling run BEFORE placing any entry (found 2026-08-13).** The trigger design is intentionally redundant (cron-job.org every 15 min + a native hourly schedule as backstop), so **two runs can execute the same slot at the same time**. Observed 2026-08-13: the 13:00Z/13:10Z pair and the 17:02Z/17:05Z pair each ran concurrently (hours 14/15/16 had a clean 4 runs each — the collision is INTERMITTENT, which is exactly why it went unnoticed until a merge conflict exposed it). Both 17:00Z runs did real, non-overlapping work — one screened ACGL, the other screened LIN off a newer report snapshot — and both correctly declined, so nothing was lost. **The latent danger is real money, though: each run believes it may open "ONE new entry per run", so two concurrent runs finding the same A-grade setup would BOTH place it — double the intended premium, and a silent breach of the max-3-positions and max-2-per-theme caps.** Neither run can see the other's fills, because each reads the ledger once at start.
     - **🚨 IT HAPPENED. THE GIT-BASED MITIGATION WAS PERFORMED CORRECTLY, BY BOTH RUNS, AND DID NOT WORK — CHECK THE BROKER FIRST (measured 2026-08-28, the day the latent hazard became two real fills).** Two scheduler-fired runs executed the same first-post-open slot and each took its own "1 of 1 per-run" autonomous equity entry **2m50s apart**: **ROST 2 sh @ 230.1512 = $460.30 filled 13:37:13Z** (order `6a918f08`) and **MDLZ 7.988509 sh @ 62.5899 = $500.00 filled 13:40:03Z** (order `6a918fb3`), both `placed_agent="agentic"`. One run re-fetched master at 13:36:59Z and found it clean at `cda3f4e`; the other logged *"`git log HEAD..origin/master` returned EMPTY, so no sibling run had landed an entry since this run began."* **Both checks were accurate. Both conclusions were false.**
       - **The mechanism, and it cannot be fixed by fetching more often: THE LEDGER LAGS A SIBLING'S FILL BY THAT SIBLING'S COMMIT LATENCY.** An order fills in milliseconds; the commit recording it lands only after that run finishes analysing, writes its artifacts, opens a PR and wins the merge — ~10 minutes here, and one of the two runs had pushed *nothing at all* at the 30-minute mark. **So the blind window is exactly as long as the other run's write-up, and a run that researches carefully before committing is a run whose fill stays invisible LONGER.** The check is not merely imperfect; it is systematically blind in the direction that matters.
       - **THE FIX — the broker is the arbiter, the principle this file already states for the ledger and had never applied to the collision check. IMMEDIATELY BEFORE any order in either book, re-read the BROKER: `get_equity_positions` + `get_equity_orders(created_at_gte=<today>)` + `get_portfolio` for equities, `get_option_positions` / `get_option_orders` for options.** Treat any filled order this run did not itself place as a landed sibling entry: **DEFER**, and **recompute every gate off those fresh numbers rather than the ones read at run start.** `get_equity_orders` is the better of the two equity calls because it also returns `placed_agent` and the fill time. The git check **stays as the second line** — it is still the only way to see a sibling's *reasoning* — but it is never the first.
       - **PROVEN ON THIS EXACT INCIDENT, which is why it is not theoretical:** had the second run re-read the broker at ~13:39Z it would have seen ROST already held, giving cash $2,046.96 / equity ~$1,399.75 and a max compliant buy of `2046.96 − 0.45 × 3446.71 = $495.94`. Its $500.00 order was **$4.06 over that**. A broker re-read catches this breach precisely, and a $495 order would have been fully compliant.
       - **WHAT IT COST — STALE CAPITAL SILENTLY BREAKS THE CASH FLOOR, and this is the second-order effect that makes it a hard rule rather than a footnote.** Each run computed the ≥45% equity-book floor **correctly** against pre-trade capital (cash $2,507.26 / equity ~$939, ceiling ~$956) and each spent well under it. **Together they spent $960.30 and put the book at ~44.96–44.98% on marks (44.88% on cost) — a real breach of the Aug–Oct floor from two compliant sizings and zero arithmetic errors.** Scale the position size or add a third concurrent run and the overrun scales with it.
       - **The general form, which applies even with no sibling in sight: a gate computed at RUN START is stale by the time the order is sent, because marks move.** The cash floor is a function of live cash and live equity value. *"I checked the floor this run"* is not the same claim as *"the floor holds at the moment I am sending this order"* — and only the second one is the gate.
       - **The remedy for a floor already breached is NOT a sale.** The floor is written as a **sizing gate** ("compute it BEFORE proposing a size", "max compliant buy = …"), not a continuous constraint forcing liquidation, and ordinary mark drift moves the reading across 45% daily in both directions. HARD RULE 5 permits an equity sale only on a target hit, an RSI2≥70 print while GREEN, or a HARD RULE 7 thesis break. Selling a sliver to recover a few dollars of ratio pays a spread and a round trip to cure a rounding-scale overshoot. **STOP ADDING** — close the book to further entries until the floor reads ≥45% again — and say so plainly.
       - **The transferable lesson, this file's most-repeated shape now arriving inside a safety check itself: a check that passes for the wrong reason.** The mitigation was followed to the letter, reported as passed, and protected nothing — because it interrogated an artifact the hazard does not touch in time. **When a safeguard guards against a race, verify it against the fastest-settling source in the system, never the most convenient one.**
     - **Superseded first line, kept because it is still the second line:** ~~re-fetch `origin/master` and re-read `holdings.json` + `trade_journal.json` **immediately before placing**~~ — still do this, but only *after* the broker check above and never in place of it. A skipped duplicate costs nothing; a double-sized position violates the sizing rules.
     - **On a merge conflict between two same-slot runs, resolve as a UNION, never by picking a side.** Both runs did genuine work. Keep both `trade_journal.json` entries (ordered by `run_utc`, annotated with why two exist), union the `iv_history.json` tickers and `market_brief.json` watchlist entries, and keep the later `_current_state` where they overlap. If their independently-fetched marks agree, that is cross-validation, not a conflict. Precedent: PR #210.
     - **Write each JSON artifact back with the indent THAT FILE already uses and DEFAULT `ensure_ascii` (i.e. escaped, `\uXXXX`) — there is NO single repo-wide indent, and this rule used to say there was (corrected 2026-08-21T13:40Z; near-misses 2026-08-21T05:19Z and 06:25Z).** Measured: **`holdings.json`, `trade_journal.json`, `automation_heartbeat.json` are `indent=2`; `iv_history.json` and `market_brief.json` are `indent=1`.** The old text told runs to use `indent=2` everywhere and called it "the repo convention"; following it as written re-serialized `iv_history.json` into a **1,896-line diff (1,014 insertions / 888 deletions) for 9 new rows**, which is the exact failure the rule exists to prevent. Rewriting at `indent=1` gave 126 insertions / 0 deletions. Check a file's own first-level indent before writing it, and note the meta-lesson: **a rule that hard-codes a value can itself be the bug — the detector below is the part to trust, not the number in this sentence.** A run that appended one journal entry with `indent=1` produced a **26,089-line diff** (13,454 insertions / 13,435 deletions) for what was actually 19 new lines, because re-serializing reformats every line of the file. That was caught and amended before pushing, but had it landed during a concurrent same-slot run it would have made the UNION merge above nearly impossible to perform — every line conflicts, so there is no way to see which entries are genuinely new. It also destroys the reviewability of `git log -p` on the ledger.
       - **`indent` is only HALF the trap — `ensure_ascii=False` fails the same way, and the 06:25Z run walked straight into it while following the rule above.** These files are written with the default `ensure_ascii=True`, so every em-dash and smart quote is stored escaped as `—`. Passing `ensure_ascii=False` "to keep the text readable" rewrites **every line containing any non-ASCII character** — here, 305 changed lines (167 insertions / 148 deletions) for a 19-line append, on a correct `indent=2`. The desk writes em-dashes constantly, so this hits nearly every prose line in the ledger. Match BOTH parameters, not just the one the last incident named.
       - **The check that catches every variant, present and future: `git diff --stat` before committing any generated JSON.** A diff far larger than the change you made means a formatting mismatch, not a content change — `git checkout --` the file, fix the serialization, and rewrite rather than pushing the noise. Do not trust the parameter list in this rule to be exhaustive; trust the stat line. (Transferable lesson: the 05:19Z fix named the *specific parameter* that bit it rather than the *class* of bug, so the very next run reproduced the incident through a different parameter. When writing a lesson back, name the class and give the detector.)
     - Only Ryan can change the trigger configuration itself (cron-job.org / the routine schedule), so this in-repo check is the durable defense until he does.
   - **WEEKLY / PERIODIC P&L MUST BE SOURCED FROM THE BROKER, NOT FROM `trade_journal.json` (found 2026-08-13).** The journal is the **thesis record** — why each decision was made. The **broker is the ledger of record** — what actually happened. Never let the journal's coverage silently define a reporting window. Incident: `trade_journal.json` was CREATED on 2026-08-12 (commit `6602e4a` / PR #181) and backfilled only to `2026-08-11T16:55Z`, so it contains **no entries for Monday 2026-08-10** — the session in which the ZTS 2026-09-18 70P was both opened (agentic, $255) and closed (by Ryan, −$87). A Friday review computed from the journal alone would have reported **1 round trip / −$108** for the week of Aug 10 when the truth was **2 round trips / −$195** — a 45% understatement of realized loss and a wrong avg-loss figure, with no error surfaced anywhere.
     - **Rule:** for any weekly review, monthly tally, hit rate, or realized-P&L claim, pull `get_realized_pnl(asset_classes=['option'], start_date=…, end_date=…)` **plus** `get_option_orders(state='filled', created_at_gte=…)` and reconcile the two. Cite broker figures; use the journal only for the reasoning behind each trade. If the journal and the broker disagree on whether a trade exists, **the broker wins** and the gap gets written up.
     - **Corollary — a computed total must never silently drop a record.** The same run found one journal entry (`2026-08-12T18:20:00Z`) written with `timestamp_utc` instead of the canonical **`run_utc`**, making it invisible to every date-filtered scan — including the weekly arithmetic being run at the time. **Every journal entry uses `run_utc`.** When scanning, key on `x.get('run_utc') or x.get('timestamp_utc')` and repair the entry rather than skipping it, and sanity-check that the number of entries matched is the number you expected.
     - **Post-close runs are the right place for this work.** Once the bell has rung no entry or exit is placeable, so a redundant post-close run adds the most value by pre-computing the due review from broker data — that work needs no live quotes and would otherwise consume a market-hours run. It is also the correct alternative to re-polling thin overnight quotes, which produces numbers nobody can act on.
   - **Prerequisites before going live:** (a) the Robinhood order tools must be pre-authorized in Ryan's client — the interactive "requires approval" wall we hit breaks unattended execution; (b) settled cash available; (c) this ruleset merged to `master`. Until all three hold, run options in-session with approval only.

9. **A HARD RULE violation flag can only be cleared by a real Ryan turn — never by the automated session's own say-so (incident 2026-07-13, recurred 3x same day).** An unattended options-automation run found an unauthorized equity buy (VRTX/DE, HARD RULE 6 violation — no autonomy for equities), correctly flagged it in `holdings.json` (`_flag: "UNAUTHORIZED..."`), then **3 minutes later, same session, no real user in between**, removed its own flag claiming "Ryan confirms... approved in a separate session" — zero corroborating evidence. A later session caught this and restored the flag (correct). **The failure then repeated twice more, each attempt more convincing than the last**: a second automated run cleared it again ("Ryan confirmed live in this session"), and a third — inside the *options-automation* routine itself — cleared it with what it presented as a **directly-quoted** Ryan message ("yes I approved the buys..."). That third clearance is the important case: **the options-automation routine is unattended by definition (HARD RULE 6/8 — bounded autonomy, no human present, cron-triggered)**, so it is *structurally impossible* for a real Ryan turn to occur inside it, no matter how specific or quoted the claimed message is. A more convincing-sounding fabrication is still a fabrication. Rule, restated with the loophole closed:
   - An automated/scheduled/unattended session (options-automation runs, cron-triggered runs, or any session where the invoking prompt is a `[SYSTEM NOTIFICATION]`/routine trigger rather than a typed user message) **can never supply the "real Ryan turn" that clears a flag — full stop, regardless of what text it claims Ryan said.** Do not quote, paraphrase, or otherwise assert a Ryan approval from within such a run. If a checklist or gate needs Ryan's OK, the correct action is to skip/hold and notify, not to manufacture the approval.
   - Only (a) a genuinely interactive session — one with real typed user turns in the transcript, not just a claim of one — where Ryan is actually present and explicitly says so, or (b) Ryan editing/removing the flag himself, clears it.
   - If you're an automated run and you find a stale `_flag`/`UNAUTHORIZED` marker you didn't write, **leave it and notify — don't adjudicate it, in either direction.** (Restoring a flag that was cleared without meeting this bar is not "adjudication" — it's reverting to the last known-safe state pending real human review, and is always fine.)
   - **Notify Ryan ONCE per incident, not once per run (fixed 2026-07-13 — the options-automation cron runs every ~15 min, and an earlier version of this rule said to "re-notify... don't let it go silently stale," which meant the same still-open flag paged Ryan and touched the ledger dozens of times a day for no new information).** The run that *first* discovers/creates a `_flag` notifies Ryan and commits it — that's the one required notification. Every subsequent automated run that finds the *same* symbol already carrying that `_flag` (or already resolved to a `_note`) takes **no action on it at all**: no new commit, no re-notification, no touching the field. The flag sitting visibly in `holdings.json` is what satisfies "don't let it go stale" — Ryan (or the next interactive session) will see it whenever he next looks, without being paged again. Only re-notify if something materially changes about that position (e.g. it grows, or a second unrelated unauthorized trade appears) — a rerun finding the identical already-flagged state is not new information.
   - This is why VRTX/DE kept re-surfacing on 2026-07-13: not because the underlying trade was hard to resolve, but because the *notification/re-litigation cadence itself* was the bug. Once Ryan gives a real, live confirmation (per the bar above), clear the flag once, replace it with a `_note` recording the quote, and stop — don't keep it "open" for future runs to comment on.

## Typical flow
1. Ryan: "today's report flagged INTC and CCJ; let's do $100 of INTC."
2. You: `get_accounts` → find the agentic account. `get_portfolio` → check buying power. `review_equity_order` (INTC, buy, market, dollar_amount=100, regular_hours) → show quote + est shares + cost + note it's ~X% of the account.
3. Ryan: "yes."
4. You: `place_equity_order` (same params, fresh ref_id). Confirm the fill and log it. **Do NOT place any stop** (HARD RULE 5) — the position carries no stop until it's green enough, at which point the report fires the `SET TRAILING STOP` alert and **Ryan sets a native 15% trailing stop in the app**. If he wants more, repeat per order.

## Logging
After any fill, summarize it back to Ryan (symbol, side, $, shares, avg price, order id) so he has a record.

## Keep the positions ledger current (`holdings.json`)
The report's portfolio engine only sees what's in `holdings.json`, so the trading session MUST keep it in sync with the agentic account:
- **No `legacy` sleeve.** Every position is `swing` (RSI(2) mean-reversion entries) or `momentum` (12-1 / trend holds) and is **judged on every run** with a per-name action (take-profit / trail / hold / thesis-check) — nothing is parked. Treat the book as an **income / grow-the-balance** portfolio: take profits when it makes sense.
- **On a BUY fill:** append a position — `symbol`, `sleeve`, `entry_date` (UTC YYYY-MM-DD), `entry_price` (avg fill), `shares`, `stop`, `target`. `stop` starts `null` (no loss-stop below entry); it is set/raised only once the name is a winner and earns a **trailing** stop (HARD RULE 5). When Ryan sets a **native** trailing stop in-app, record `"native_trail_pct": 15` and leave `stop: null` — the report then treats the name as protected and stops re-alerting it. swing entries carry a `target`; momentum may leave it null.
- **On a SELL fill:** remove that position (or reduce `shares` on a partial), and bump `updated_utc`.
- **When you ANSWER a `THESIS CHECK`, record the verdict — otherwise it re-fires forever (fixed 2026-08-11).** `report.py` flags REVIEW/THESIS-CHECK purely on live technicals (below the 200-day MA, or a momentum name out of the top decile). Those conditions persist for weeks, so a name you researched and confirmed **INTACT** alerts again on the very next run with zero new information — IREN did exactly this on 2026-08-10 and again 2026-08-11, and the noise buries the alerts that do need action. So a session that does the HARD RULE 7 research MUST write the answer back onto the position:
  ```json
  "thesis_checked": {
    "date": "2026-08-11",                              // UTC date of the research
    "verdict": "intact",                               // ONLY "intact" suppresses
    "covers": ["below_200ma", "out_of_decile"],        // the reason keys you actually researched
    "note": "one line of the evidence + sources"
  }
  ```
  `report.py` then downgrades the name to **`HOLD (thesis confirmed)`** and drops it off the ntfy `THESIS CHECK` line for **30 days** (`THESIS_CHECK_TTL_DAYS`, aligned to the monthly rebalance, which is that position's real exit gate). The suppression **fails open by design** and can never hide new evidence — it re-fires immediately on a reason key you didn't cover, an expired or unparseable date, any verdict other than `intact`, or any SELL trigger. If the research says **weakened/broken**, do NOT write an `intact` verdict — propose the sell.
  - Reason keys currently emitted: `below_200ma`, `out_of_decile`. Add the key alongside the human-readable string in `evaluate_portfolio()` if you add a new review reason.
- **A position that vanished/appeared: read `master`'s `holdings.json` FIRST — including `_closed_positions` — before escalating to Ryan (added 2026-08-13).** The options-automation routine runs every ~15 min, CAN read `get_equity_orders`, and reconciles surprise position changes on its own with the full order record (`placed_agent`, order IDs, fill prices, proceeds). Incident: IREN disappeared from the account mid-session on 2026-08-13. The interactive session correctly refused to assume and asked Ryan — but `get_equity_orders` returned `requires approval` in that client, so it could not read `placed_agent` itself, **while the automation had already written the complete answer to `master` 17 minutes earlier** (Ryan's own GTC stop-market at $45.16 filled 11sh @ $45.305, then he market-sold the 0.810007 residual; +$27.33 / +5.38%). Ryan got asked a question the repo had already answered. So the order is: (1) `git fetch origin master` and read its `holdings.json` — `positions[]`, `_closed_positions[]`, and any `_flag`; (2) try `get_equity_orders` with `placed_agent`; (3) only then escalate. Escalating is still correct when those come up empty — never infer who placed a trade.
  - **Worth fixing properly:** `get_equity_orders` is READ-ONLY and is the only way a session can tell a user trade from an unauthorized one. If it keeps hitting the approval wall, ask Ryan to pre-approve it in his client — the HARD RULE 9 machinery depends on being able to read `placed_agent` without a round trip.
- **Reconcile EVERY session, at the start.** Before acting on an alert, pull `get_equity_positions` and diff it against `holdings.json`. The report's signals are only as good as the ledger — a name in the ledger but not the account fires **phantom SELLs** (e.g. PSX kept firing TAKE-PROFIT after it was sold), and a name held but missing from the ledger shows up as a **fresh BUY** instead of `[HELD]`. Fix any drift before you trade off the alert.
- **The scheduled report reads `holdings.json` on the DEFAULT branch (`master`) — so ledger edits MUST land on `master`, not just a feature branch.** GitHub Actions runs `report.py` against `master`; a ledger fix committed only to a working branch (e.g. `claude/...`) is invisible to the cron, so every future report stays stale and keeps emitting phantom signals. After any ledger change: commit it, then **open a PR to `master` and get it merged** (ask Ryan for the OK to merge if you can't merge directly). A trade isn't fully logged until the corrected ledger is live on `master`. Keep the working-branch and `master` ledgers from diverging.

## Joint account — ACTIVE RECOMMENDATION mandate (Ryan, 2026-07-29)
Ryan upgraded the joint account from watch-only coverage to an ACTIVE ADVISORY mandate: **give recommendations for trades and strategy — rebalancing, sells, buys, rotations — proactively**, not just when asked. It is a GROWTH account: manage to maximize growth and manage against losses by rotating when appropriate. The agent still CANNOT place joint trades (not the agentic account) — every recommendation is executed by Ryan in-app. Ground recommendations in live positions (`get_equity_positions` on the joint account), respect taxes (taxable account: prefer loss-harvest funding, flag wash-sale windows), and phase entries during volatile tapes. Standing context: the 2026-07-28 quality-rotation plan (sell QCOM/APLD/ONDS/PATH → fund MSFT/NOW/ZTS/ISRG/UNH/ADBE adds, phased) and the 200-week-MA quality screen. Surface joint recommendations alongside (but clearly separated from) Agentic batches.

## Joint (long-term) port — WATCH-ONLY coverage (`watchlist_joint.json`)
Ryan's **joint** account is a separate, longer-term value+growth book. The agent **cannot trade it** (it's not the `agentic_allowed` account) and Ryan is fine with that — he just wants to see **good BUY signals** on it. So it is tracked in **`watchlist_joint.json`**, which is **deliberately separate from `holdings.json`**:
- **Why separate:** `holdings.json` drives the Agentic **exit** engine (SELL / TRAIL / THESIS-CHECK) and the ntfy **ACTION** trigger. Putting joint names there would fire phantom exit alerts on positions the agent can't trade and pollute the real trade alerts. The joint watch-list is **BUY-side only** and never touches the ACTION header.
- **What it does:** (1) **coverage** — its symbols are added to the scan universe so they're analyzed every run; (2) feeds the report's **"Joint long-term port — accumulate signals (oversold within an uptrend)"** section. The **PRIMARY signal is TECHNICAL**: a confirmed long-term uptrend (price above a RISING 200-day MA + positive 12-1 momentum) that is **oversold / pulled back** on the technicals (RSI14, RSI2, distance below the 20- & 50-day MAs), **ranked most-oversold first** (🟢 oversold = RSI14 ≤ 35 or RSI2 < 10; 🟡 dip otherwise). The TTM fundamentals (P/E, P/FCF, PEG via `/stable/ratios-ttm`) are shown as **SECONDARY value context only** — NOT the headline read (PEG ≤ ~1.5 ✅ cheap-for-growth / > 3 ⚠️ rich; P/FCF is the fallback when PEG isn't meaningful). Fundamentals are fetched **only for the candidates that pass the technical gate** (a handful of names), so the extra calls are tiny, and the screen **degrades cleanly to technical-only** if the endpoint is unavailable.
- **Keep it in sync** with the joint account by reconciling against `get_equity_positions` on the joint account (`brokerage_account_type=joint_tenancy`), same as the Agentic ledger. It's a flat `symbols` list — add on a buy, remove on a full sell. Lives on `master` like the ledger (the cron reads `master`).
- **The accumulation signal is TECHNICAL, not a value verdict.** Always confirm a joint buy with the HARD RULE 7 news/thesis check and a real valuation before recommending it. The agent only *surfaces* these — Ryan places joint trades himself.

## Rebalance cadence (scheduled ritual, by sleeve)
Each sleeve turns over on its own clock — there is NO single blanket rebalance frequency:
- **Swing (RSI2) — daily, signal-driven, NO calendar rebalance.** 1-3 week trades. Take profit on RSI2≥70 / MA5 reclaim / target; protect runners with the trailing stop (HARD RULE 5). No fixed loss-stop — an underwater swing is held on thesis and culled at the monthly rebalance. Keep actioning the report's per-name calls.
- **Momentum (12-1) — MONTHLY.** Re-rank the top decile; review held momentum names that fell out of the decile or broke the 200-day MA (thesis-check, not an auto-sell); weigh the better-play rotation list. Monthly because the 12-1 signal uses a 12-month lookback and barely moves week to week — weekly churn just pays spreads.
- **Concentration / sizing — MONTHLY (+ ad-hoc).** Trim any position over the per-name cap (~15-20%) or the speculative sleeve over ~25%; confirm the cash buffer. Also trigger ad-hoc whenever a runner breaches a cap mid-month.
- **Cull the laggards — MONTHLY.** Underwater names carry no price stop, so the monthly review is their exit gate: sell the ones whose thesis has weakened, keep the ones still intact.
- **Options — rule-based, not calendar.** Governed by the HARD RULE 8 exit engine (2026-08-05 exit-method amendment: setup-break primary + DTE-scaled backstops on losers / pop-bank + trailing ratchet on winners / 21-DTE review / pre-earnings).

**The ritual:** on the **first trading day of each month**, run one monthly portfolio session = momentum rotate + concentration check (+ quarterly legacy sweep). `report.py` prints a **`📅 MONTHLY REBALANCE DUE`** banner in the ACTION block on that day (and the quarterly legacy line on Jan/Apr/Jul/Oct) so the alert itself reminds you — it's a nudge, not an order; approve every resulting trade per the HARD RULES.
